#! Example build preset configuration.

# Canonical SystemTarget filename slug.
target = "example"
# Workspace-relative KernelConfig input.
kernel-config = "conf/.defconfig"
# Kernel Cargo build profile only; app and rootfs tasks do not consume it.
profile = "release"
