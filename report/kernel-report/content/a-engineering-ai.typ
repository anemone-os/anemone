#import "../components/figure.typ": report-figure

= AI 使用情况

在 Anemone 的开发过程中，我们使用了 AI 辅助工具，但没有把核心判断交给 AI。我们的基本原则是：AI 可以帮助我们更快地阅读材料、整理候选方案、生成重复性代码和发现可能的问题，但内核设计、ABI 取舍、并发语义和最终合入必须由队员理解、审查和验证。

Anemone的开发路线比较具备特征。开发前三个月，我们几乎纯古法手写代码、阅读 Linux / LTP 行为和自己调试来推进内核。但到了最后一个月，比赛压力变大，我们不得不开始学习vibe coding。但随着逐渐系统地使用AI，我们逐渐意识到，AI 生成内容本身不等于低质量；真正决定质量的是我们是否能给出清楚的上下文、是否能识别错误方向、是否能把输出放回源码和测试中验证。

我们主要使用的工具包括 `ChatGPT 5.4 / 5.5`、`DeepSeek V4`，以及Codex CLI, Claude Code。不同工具的使用方式略有差异，但在我们的流程里，它们都只是辅助工具：提出候选解释、生成初稿、做局部审查或整理材料，没有替代队员对 Anemone 机制本身的判断。

== 使用范围

我们使用 AI 的场景主要有四类类。第一类场景是阅读规范、代码和测试。实现 Linux 兼容接口时，仅凭 syscall 名字很容易误判语义；我们会让 AI 帮助整理 man page、Linux 行为、LTP 测例和 libc 调用习惯之间的关系，再由队员回到源码或测例中核对。这实际上是完全的苦力活。利用AI，我们缩短了搜索时间，但不会直接把 AI 的解释当作结论。

第二类场景是生成候选实现和重复性代码。比如一些简单的文件对象、测试程序骨架、错误码分支或报告初稿，AI 可以根据我们给出的示例快速生成相似结构。我们通常会先手写一版能表达真实语义的代码或文字，再让 AI 补齐相似部分。

第三类场景是调试分析和代码审查。我们会把日志、失败现象或 diff 摘要交给 AI，让它列出可能的风险点，再由人工判断哪些问题成立。这个过程比较适合发现遗漏的路径、边界条件和不一致的接口，但 AI 也经常给出听起来合理、实际不符合当前代码结构的建议，所以我们不会让它直接决定修复方案。

第四类场景是文档表达。报告、RFC、transaction devlog 和小迭代记录中，AI 可以帮助我们把口语化草稿整理成更清楚的工程说明。但每一段最终进入公开文档前，都要由队员检查是否符合实际实现，是否夸大完成情况，是否把私人调试材料写成公开事实，以确保团队合作的边界。

== 生成内容范围

我们把 AI 适合参与的内容限制在几个范围内：重复性样板代码、局部候选实现、测试程序思路、日志分析摘要、代码审查清单和文档润色初稿。这些内容有一个共同点：它们可以被人工快速核对，也能通过编译、测试、源码审查或文档事实检查来验证。

我们不会直接接受 AI 对核心架构的决定。进程拓扑、调度等待协议、虚拟内存对象、VFS 与设备模型边界、信号现场恢复、跨架构 HAL 这类设计，都需要队员先明确 owner、状态来源和外部可见语义。AI 可以参与讨论，但不能替我们决定模块边界。

我们也不会直接合入未经验证的 AI 代码。只要涉及锁、生命周期、地址空间、用户指针、信号返回、页表维护或跨模块回调，我们都会把 AI 输出当作候选 patch，而不是最终实现。能否进入代码库，取决于人工 review、构建检查和对应测试，而不是取决于回答看起来是否完整。

== 人工审查与验证

我们使用 AI 时最重要的约束是人工审查。一个建议是否成立，首先要回到 Anemone 当前代码，而不是回到 AI 记忆中的某种通用内核结构。我们会检查它是否落在正确的 owner 里，是否扩大了不该扩大的接口，是否引入了新的状态副本，是否改变了 Linux ABI 可观察行为。

#report-figure(
  image("../assets/ai-code-review.png", width: 100%),
  caption: [一次 AI 代码审查结果示例。我们把这类输出作为风险清单和审查入口，最终结论仍由队员结合源码、构建和测试确认。],
)

图中展示的是一次 AI 辅助代码审查结果。它把问题分成较高风险和次级风险，并给出可能涉及的代码位置。我们不会直接把这类输出当作事实，而是把它当作审查清单：先判断问题是否真的存在，再决定是否需要修改草案、实现或验证计划。对于不成立的建议，我们会明确丢弃；对于成立的建议，也要经过人工修改和后续验证。

在较大的修改中，我们还会用文档和流程约束 AI 输出。`AGENTS.md` 记录了仓库级编码规则和边界要求；skill 文件把反复出现的工作方式沉淀为可复用说明；RFC、transaction devlog 和小迭代记录则用于约束实现范围、停止条件、验证方式和回写路径。这样，agent 不只是“会写代码的工具”，而是被纳入一个可审查的工程流程。

验证方式随任务不同而变化。代码修改至少需要构建、格式或空白检查；涉及 ABI 的修改需要和 Linux / LTP / libc 行为核对；涉及并发和生命周期的修改需要检查锁顺序、唤醒顺序、资源释放和失败路径。AI 可以帮我们列出需要检查的点，但检查是否通过必须来自实际命令、源码阅读和人工判断。

== 幻觉与纠正案例

AI 最大的风险是幻觉。第一类常见问题是误判 Linux 或 LTP 语义。它有时会根据 syscall 名称给出一个通用解释，却忽略 man page 的 NOTES、libc 包装层行为或 LTP 测例中的具体断言。遇到这种情况，我们会先停止按回答修改代码，转而阅读 Linux 行为、测例源码和当前内核路径，再决定问题到底在 syscall wrapper、对象语义还是测试环境适配上。

第二类问题是沿着过期代码或错误 owner 边界给出建议。Anemone 在开发过程中经历了多次模块调整，旧路径、旧抽象或半废弃代码有时仍然能被搜索到。AI 如果只看到片段，很容易建议在错误层次修补，比如把应该属于 VFS、调度或信号核心的状态塞进 syscall 层。我们的纠正方式是先确认当前调用路径和 owner，再决定 write set；如果建议要求越过模块边界，就必须重新审查设计，而不是为了让 patch 看起来小就强行兼容。

这些经历让我们形成了一个比较直接的判断：AI 能缩短定位和起草时间，但不能替代内核开发中最关键的因果判断。尤其是并发、生命周期、ABI 和跨架构适配问题，真正困难的部分不是写出几行代码，而是知道这些代码应该属于哪个对象、在什么不变量下运行、失败时如何恢复、用户态能观察到什么。

== 方法论反思

我们最开始使用 AI 时，方式比较简单：给一个 prompt，拿到一个回答，再人工修改。随着项目推进，这种方式很快暴露出问题。内核工程的上下文太长，单次对话很容易遗漏已经确定的约束；agent 如果不知道当前 write set、验证要求和模块 owner，也可能把一个局部问题改成跨模块扩散。

后来我们逐渐把经验沉淀到工程材料里：把仓库规则写进 `AGENTS.md`，把反复使用的审查和调试方法写成 skill，把较大的实现拆成 RFC、transaction devlog 或小迭代记录。这样做不是为了让流程显得复杂，而是为了让 AI 的参与可追踪、可审查、可回滚。我们希望 AI 成为开发中的“加速器”，而不是“驾驶员”；它可以帮助我们更快地搜索、总结、生成和检查，但 Anemone 的设计责任、验证责任和最终解释责任仍然在我们自己手里。
