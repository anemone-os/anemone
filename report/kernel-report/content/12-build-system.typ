#import "../components/figure.typ": code-block, report-figure

= 构建系统

构建系统并不运行在内核中，却决定了一个内核项目能否被稳定地配置、构建、启动和验证。
在不少教学或竞赛内核中，一份 Makefile 加上若干辅助脚本已经足以完成单一架构、单一平台的构建。
但当项目扩展到双架构、虚拟机与真实开发板、多套用户环境和不同验证场景以后，平台常量、编译选项、
磁盘路径和启动参数很容易散落在环境变量与脚本调用顺序中。此时，开发者虽然仍能“把内核编出来”，
却很难准确回答这次构建最终描述的是哪一个完整系统。

我们把 Anemone 的构建系统设计成*从系统描述到可启动产物的类型化编排层*。内核功能、系统目标、
硬件平台、用户程序、Nemophila 模块、根文件系统和运行时输入分别建模，再由统一的解析器检查并组合这些事实。这样，
Anemone 的可移植性从 HAL 接口延伸到了“如何稳定地产生并验证一个完整系统”这一工程交付边界。

== 从隐式脚本到显式系统模型

我们在设计构建模型时坚持一个基本原则：*每一类事实只由一个配置层拥有*。例如，物理内存起始地址
属于平台，内核是否启用某项功能属于 KernelConfig，根设备和首个用户程序属于系统目标；它们不会因为
某个脚本恰好需要这些值，就被复制到脚本参数或另一份配置中。图中给出了各层配置之间的主要关系。

#report-figure(
  image("../assets/build-config-model.png", width: 100%),
  caption: [Anemone 构建配置的职责与组合关系。每种配置拥有不同事实，并由 xtask 统一解析和编排。],
)

这套模型由以下几类对象组成。

- *KernelConfig*描述内核功能、策略与容量参数，例如调试设施、文件系统支持、调度策略和各类资源上限。
  我们保留了内核开发者熟悉的 Kconfig 使用方式；目标平台、构建 profile 和宿主机路径由其它层负责。

- *Platform*描述一个硬件或虚拟平台，包括目标架构、启动环境、物理地址常量、设备树交付方式、QEMU
  机器描述，以及该平台要求发布的内核产物。RISC-V64、LoongArch64、QEMU virt、VisionFive 2 和
  Loongson 2K1000 的差异都在这一层收束。

- *SystemTarget*描述我们准备启动的系统：它选择 Platform，并确定根文件系统类型、根设备、静态网络
  部署、首个用户程序和需要随系统启动的 Nemophila 模块。相同平台因而可以承载普通 rootfs、性能环境、
  内嵌初始化程序或不同动态扩展组合，而不需要复制平台配置。

- *BuildPreset*把 SystemTarget、KernelConfig 和内核 Cargo profile 组成一个可复用的构建入口。preset
  固化一套完整选择，任何字段都不会由其它隐藏状态补齐。

- *App manifest*描述用户程序支持的目标架构、构建驱动和需要导出的产物；*Rootfs manifest*则描述基础
  目录、需要安装的程序与文件，以及最终文件系统镜像。应用构建和 rootfs 组装由此也进入同一套
  可检查的交付链，省去了内核构建之外的手工准备过程。

- *Module manifest*描述 Nemophila 模块的源码、构建方式和 WebAssembly 产物。模块构建拥有独立入口，
  SystemTarget 只选择模块身份，不复制工具链命令或模块配方。

这些配置用于解决规模扩大以后最容易出错的问题：哪些值应该共同变化，谁负责解释这些值，以及下游
拿到的究竟是声明过的当前产物，还是某次旧构建偶然留下的文件。我们为关键配置定义了封闭的字段集合
和解析类型，并在解析阶段检查跨层引用，
从而尽可能在启动编译器和 QEMU 以前暴露错误。

== 单一入口与统一解析

Anemone 仓库使用 `Justfile` 提供简洁、可发现的统一入口，复杂逻辑则由 Rust 编写的
`scripts/xtask`承担。我们可以通过 `just build`、`just app`、`just module build`、`just rootfs`、`just qemu` 和
`just test`完成日常操作。这些入口共享同一套配置类型、路径解析与产物规则，无需分别维护 shell
逻辑。

一次普通构建可以写成：

#code-block(
  ```text
  just build --preset qemu-virt-rv64-release
  ```.text,
  caption: [使用显式 preset 构建 RISC-V64 QEMU 系统],
)

这条简短命令选择了一份完整的系统描述。解析器会得到确定的 SystemTarget、Platform、
KernelConfig 与 Cargo profile，并报告本次选择的来源。对于不适合预先固定的场景，我们也允许调用者
显式给出完整的 `target + kernel-config + profile` 元组。缺少其中一部分的选择会被拒绝，解析器也不会从
“上一次构建结果”、开发者本地默认值或当前目录下恰好存在的文件中静默补齐。构建与 QEMU 使用同一个
解析器，因此不会出现编译时选择了一个平台、运行脚本却又用另一套机器参数启动的平行配置。

我们还把宿主机相关的运行值与可复用配置分开。Platform 可以声明 QEMU 需要哪些绑定以及它们如何展开
为参数；磁盘镜像、内核产物等具体路径由本次调用显式提供。这样，仓库可以记录“这个平台需要什么”，
却不会把某位开发者机器上的绝对路径写进公共配置。缺失、重复或没有被当前动作消费的绑定都会在启动
QEMU 前被拒绝。

Rust 实现让 xtask 的构建配置与任务共享同一组枚举和结构体。架构、
初始程序来源、应用与模块构建驱动和设备树交付方式都必须经过类型化解析；跨字段检查可以给出带配置身份的
诊断，单元测试也能覆盖选择、路径约束、参数展开和失败清理。`Justfile`因此保持为面向人的薄入口，
xtask 则成为配置解释和构建副作用的唯一编排者。

== 从配置到可启动系统

我们把构建成功定义为：所选系统需要的输入已经生成，内核已经编译，平台要求的公开产物也已完整发布。
图中展示了一次构建与验证的主要阶段。

#report-figure(
  image("../assets/build-pipeline.png", width: 100%),
  caption: [从显式系统选择到内核产物、运行平台和端到端验证的构建流水线。],
)

xtask 首先解析完整选择并检查配置关系，随后为内核生成必要输入，包括 KernelConfig 对应的 Rust 定义、
平台链接脚本、系统根挂载信息、静态网络配置、初始程序描述和启动模块目录。内核源码不需要读取 TOML，也不需要知道
BuildPreset 或 SystemTarget 的存在，只消费构建系统生成的窄而明确的定义。这一边界让部署模型留在
构建侧，同时避免在内核中建立第二套配置解析逻辑。

设备树同样属于平台交付的一部分。对于固件提供设备树的平台，我们不生成一份看似权威的副本；对于
需要内嵌设备树的真实硬件平台，我们从受版本控制的规范 DTS 编译；对于由 QEMU 提供拓扑的场景，构建
系统只使用机器类型、CPU、SMP 和内存等平台拓扑生成本次构建的 DTB，而不会意外引入测试磁盘、网络
后端等运行参数。设备树由此和平台来源保持一致，各类脚本无需重复推断来源。

用户程序也走统一的产物导出路径。每个 app manifest 声明自身支持的目标、构建方式和产物；rootfs
构建按清单调用相同的应用构建器，将产物安装到 staging tree，再生成 Ext4 镜像。如果系统选择内嵌
初始程序，内核构建同样复用这条应用导出路径，并检查结果确实是唯一、可执行的普通文件。我们直接
使用构建系统导出的产物，不从 Cargo `target/` 目录猜测路径。`build/apps` 与 `build/rootfs` 中的结果
是面向后续动作的公开产物。

Nemophila 模块形成另一条同样受管理的交付路径。模块构建器根据 manifest 调用对应工具链，从本次
构建产生的新候选中取得 WebAssembly 制品，并完成格式、接口和执行环境检查。SystemTarget 选择启动
模块时，系统构建会按声明顺序调用这条路径，并立即把本次得到的模块内容固定为当前内核构建的输入。
这样，内核启动时装载的正是本次系统描述选择并验证过的制品，而不是工作目录中偶然残留的旧文件。

内核编译完成后，xtask 统一发布 `build/anemone.elf`、link map 和平台要求的附加格式。启用内核符号表
时，构建系统会执行发现与最终链接两遍构建，使嵌入内核的符号表与最终 ELF 相互核验；面向 U-Boot 的
平台则按 Platform 描述执行 `objcopy` 和 `mkimage`，只在完整成功后留下最终镜像。这样，我们可以继续
使用 Cargo、链接器、dtc 和 QEMU 等成熟工具，同时由 Anemone 自己定义它们之间的输入、输出和失败边界。

== 双架构与端到端交付

这套构建模型从一开始就服务于多架构，RISC-V 与 LoongArch 共享同一套编排流程。
应用清单明确声明支持的目标，Platform 选择对应的 target specification、工具链与启动产物，SystemTarget
再选择根设备、首个用户程序和启动模块。我们用同一条内核构建路径覆盖 RISC-V64 与 LoongArch64，并将 QEMU virt、
VisionFive 2 和 Loongson 2K1000 的平台差异限制在各自配置与必要的后处理步骤中。

模块制品自身不绑定内核架构。RISC-V64 与 LoongArch64 的系统构建会从同一模块源码分别得到内容一致的
WebAssembly 产物，再把它交给各自内核中的 Nemophila 运行环境。这条路径同时证明了模块构建的可复现性
和运行时扩展的跨架构价值。

对于比赛测例和完整回归，我们在通用构建能力之上提供了仓库级端到端 wrapper。它们会选择明确的
rootfs manifest，重新构建用户环境，把调用者指定的只读测试盘复制为本次运行的工作副本，选择对应的
系统与 QEMU 拓扑，构建内核并启动虚拟机，最后把串口输出保存为日志；需要自动判定的测试链还会检查
完成标记。初赛、决赛、RISC-V64 和 LoongArch64 的输入因此能够被明确区分；每次成功结果都对应一条
可以重复执行的完整路径。

与常见的脚本式组织相比，这套系统的优势来自显式关系。构建目标由 preset 或完整选择元组确定，平台
事实集中在 Platform，用户环境由 app 与 rootfs manifest 描述，动态扩展由 module manifest 与 SystemTarget
共同接入，编译和 QEMU 共享目标解析结果。接入新
架构时，我们复用编排流程，只增加相应的架构与平台事实；端到端 wrapper 固定输入、产物和日志边界。

构建系统把通常散落在 Makefile、环境变量和启动脚本中的隐式知识，转化为职责明确、可组合、可检查且
可复现的系统描述。从配置到验证的稳定闭环，使我们能够在快速演进内核功能的同时持续维护双架构、
多平台、多种用户环境和动态扩展模块，避免工程复杂度沉积为一组只有原作者才能正确使用的脚本。
