= 架构硬件抽象层

== 架构差异

本章说明 RISC-V 与 LoongArch 的启动、异常、中断、上下文和用户态返回差异，以及通用内核代码如何通过架构硬件抽象层保持共享。架构差异被限制在 arch-owned 文件和接口上，避免向通用内核代码扩散。

== 启动流程

启动流程包括早期汇编入口、页表或地址空间切换、设备树 / platform 信息解析、BSS 初始化、内核主函数进入和第一个用户态进程启动。两个架构的入口最终汇入共享初始化路径。

== Trapframe 与上下文

Trapframe、context switch frame 和用户态返回路径属于架构层与调度层的交界。寄存器保存、系统调用参数、返回值写回、信号返回和抢占切换在这里收束为通用内核可使用的上下文接口。

== 中断与硬件时钟入口

本节整理 timer interrupt、外部中断、PLIC 或平台中断控制器、SBI / 固件接口和架构相关屏蔽语义。调度策略放在第三章，clock、soft timer 和用户可见时间对象放在第八章；这里只说明硬件事件如何进入通用内核路径。

== 平台验证

RISC-V、LoongArch、QEMU 和开发板验证共同限定本章的平台覆盖范围；只完成部分平台构建或运行验证时，报告只陈述对应范围。
