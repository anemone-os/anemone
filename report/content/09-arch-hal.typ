= 架构硬件抽象层

== 架构差异

本章说明 RISC-V 与 LoongArch 的启动、异常、中断、上下文和用户态返回差异，以及通用内核代码如何通过架构硬件抽象层保持共享。正式正文应避免只列差异表，而要说明差异被限制在哪些 arch-owned 文件和接口上。

== 启动流程

启动流程包括早期汇编入口、页表或地址空间切换、设备树 / platform 信息解析、BSS 初始化、内核主函数进入和第一个用户态进程启动。正文需要分别列出两个架构的入口文件和共享初始化路径。

== Trapframe 与上下文

Trapframe、context switch frame 和用户态返回路径属于架构层与调度层的交界。正文应说明寄存器保存、系统调用参数、返回值写回、信号返回和抢占切换之间的边界。

== 中断与硬件时钟入口

本节整理 timer interrupt、外部中断、PLIC 或平台中断控制器、SBI / 固件接口和架构相关屏蔽语义。调度策略放在第三章，clock、soft timer 和用户可见时间对象放在第八章；这里只说明硬件事件如何进入通用内核路径。

== 平台验证

正式正文应列出 RISC-V、LoongArch、QEMU 和开发板验证入口。若最终只完成某些平台的构建或运行验证，需要明确范围和限制。
