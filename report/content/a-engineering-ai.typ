= AI 使用情况

== 使用范围

本项目使用 AI 辅助阅读规范、分析测试、生成候选实现、进行代码审查和改善文档表达。所有 AI 输出都需要由队员核对源码、测试、日志或文档事实后才能进入代码和报告。本节按赛方要求披露工具名称、使用场景、生成内容范围、人工修改情况、交互记录摘要和验证方法。

== 工程约束

项目通过 RFC、transaction devlog、小迭代 devlog、register 和 current limitations 记录设计、执行事实和开放问题。代表性事务说明这些记录如何约束实现范围、停止条件、验证方式和回写路径。

== 人工审查与验证

AI 和 agent 可以参与阅读规范、分析日志、起草重复代码、生成测试思路和改善文档表达，但不能替代队员做核心架构判断。对于影响模块边界、并发语义、生命周期、ABI 取舍和共享状态的修改，人工审查、write set、review 和 validation floor 共同约束 agent 输出。

== 幻觉与纠正案例

附录保留 1-2 个 AI 幻觉或错误方向案例，说明团队如何通过源码、日志、Linux/LTP 行为、编译测试或人工 review 纠正。案例需要具体，避免写成泛泛的“AI 可能出错”。
