We use xtask for more complex build and development tasks that are not easily handled by simple scripts.

Note: We prefer using `xshell` instead of std::process::Command since it'll be way more easy to find and fix errors.