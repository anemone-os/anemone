= 版本说明

本书源文件保持单一演进。初赛版、决赛版和后续版本通过 git tag、提交点和 PDF artifact 固化，不长期维护多套正文目录。

当前 PDF 是初赛快照，冻结日期为 2026-06-24，对应封面提交点 `86cfec1cc13c`。封面提交点用于定位本书文字、图、引用和版本边界，不表示 Anemone 内核行为只能由本书定义。

冻结后的本版只修事实错误、错别字、引用错误和排版问题。新的设计进展、accepted limitations 关闭、RFC 状态变化和模块结构变化进入后续快照；事实来源仍以代码、RFC、devlog、register 和 current limitations 为准。
