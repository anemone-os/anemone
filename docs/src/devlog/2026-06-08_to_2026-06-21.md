# 2026-06-08 至 2026-06-21

## 使用说明

- 本文件覆盖 2026-06-08 至 2026-06-21 的开发日志。
- 新条目按时间顺序追加。
- 条目格式使用模板页中的开发日志模板。

## Entries

## 2026-06-08 - `pselect6 exceptfds` 兼容回退

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / iomux / pselect6 / lmbench / docs

**Summary:** `pselect6` 重新接受 `exceptfds` 作为兼容 stub：仍校验置位 fd、清空 exception 输出并打日志，但不再因为没有内部 `POLLPRI` readiness source 而在 register scan 阶段返回 `ENOTSUP`。

**Validation:** `just build` 通过；`git diff --check` 通过。未在本轮 agent 侧重跑 lmbench / QEMU。

**Related:** [pselect6 exceptfds 小迭代记录](./changes/2026-06-08-pselect6-exceptfds-compat.md), [当前限制：pselect6 exceptfds stage-1](../register/current-limitations.md#ane-20260608-pselect6-exceptfds-stage1)

## 2026-06-08 - 空 iomux 超时睡眠修复

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / iomux / ppoll / pselect6 / scheduler

**Summary:** `ppoll` / `pselect6` 的 shared iomux wait helper 新增空集合 / 无 source timeout sleep 路径：`timeout == 0` 仍立即返回，正 timeout 或 NULL timeout 会进入 wait-core interruptible wait，避免 `select(0, ..., 1us)` 在 iozone-glibc 启动同步中退化为纯 busy loop。

**Validation:** `just fmt kernel` 通过；sandboxed `just build` 在 vendored `lwext4_rust` C 编译处命中已知 `Bad system call`；unsandboxed `just build` 通过，仅保留既有 `sync/mono.rs` unused-import warning。未在本轮 agent 侧重跑 iozone QEMU profile。

**Related:** [空 iomux 超时睡眠小迭代记录](./changes/2026-06-08-iomux-empty-timeout-sleep.md)

## 2026-06-08 - Fanotify 事务启动

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / VFS / syscall ABI / task-fd / iomux / LTP

**Summary:** fanotify 公开 RFC 进入实现事务。总控完成前置检查并建立事务日志；首个目标是按
agent orchestration 的 D1/D2 依赖拓扑推进 Gate A，使 task/fd 通用能力与 `fs::fanotify`
owner module 合流，闭合 Stage 0 + Stage 1 合并 gate。

**Validation:** 本条为事务启动记录；未运行 `just build`、QEMU 或 LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [RFC-20260604-fanotify](../rfcs/fanotify/index.md)

## 2026-06-09 - Fanotify Gate C corrective repair

**Date:** 2026-06-09
**Authors:** doruche, Codex
**Area:** fs / fanotify / task-fd / lifecycle

**Summary:** D4 后 `fanotify07` 暴露 group fd semantic teardown 可能从 deferred task / fd-table
`Drop` 进入 fanotify registry mutex。`FANOTIFY-041` 修复将 final-release 移到 exit、
fd-table replacement / unshare 和 clone 失败等显式生命周期路径；`FilesState::drop()` 只保留
遗漏清理断言，fanotify final-release 入口断言 interrupts-enabled / sleepable context。

**Validation:** `git diff --check` 通过；`just build` 通过，仅保留既有 `sync/mono.rs`
unused-import warning。未由 agent 侧运行 QEMU / LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [fanotify tracking issues](../rfcs/fanotify/tracking-issues.md)

## 2026-06-09 - User-test staged 工具通道

**Date:** 2026-06-09
**Authors:** doruche, Codex
**Area:** anemone-apps/user-test / competition runner / fixture staging

**Summary:** `user-test` 新增第一版 staged test-tool 通道：在 `/dev/vdb` 挂载到 `/mnt`
后、`chroot("/mnt")` 前，把启动盘 staging 目录中的工具按 source/dest manifest
二进制安全复制到测试盘。本轮 rv64 先覆盖 `/bin/mkfs.ext4`，用于区分测试材料缺口和
后续 block / loop / mount / filesystem / syscall 语义问题。

**Validation:** `file anemone-apps/user-test/staged/riscv/mke2fs` 确认为静态链接
RISC-V ELF；`just fmt user-test` 通过；`just xtask app build user-test --arch riscv64`
通过；`just xtask app build user-test --arch loongarch64` 通过；agent 侧 `just xtask rootfs
mkfs -c rootfsconfig-rv` 完成 staging 后在 `virt-make-fs` / `supermin` 主机环境层失败，
用户环境可成功构建镜像；staging 目录下的 `/fixtures/user-test/tools/mkfs.ext4` 已确认
存在且可执行；`git diff --check` 通过。未在本轮 agent 侧运行 QEMU 或 LTP。

**Related:** [User-test staged 工具通道小迭代记录](./changes/2026-06-09-user-test-staged-tools.md)

## 2026-06-10 - Fanotify D6 negative gate

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** fs / fanotify / syscall ABI / LTP

**Summary:** fanotify D6 收口为负向边界与 LTP 分类：FID/name report flags、pidfd、permission、
FS error、evictable mark、dirent/name/self/metadata masks 和 `FAN_MARK_IGNORE` 继续在
`fanotify_init()` / `fanotify_mark()` parser 边界稳定 fail closed；未引入 FID/name 正向 ABI、
permission response、procfs fdinfo 或 dirent hook。

**Validation:** 使用源码级 grep、LTP fanotify 源码比对和 D5 完成后的完整 rv64 fanotify profile
日志完成分类；本条记录时未重新运行 QEMU / LTP。`git diff --check`、`mdbook build docs` 和
`just build` 作为收口 gate 运行。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [RFC-20260604-fanotify](../rfcs/fanotify/index.md)

## 2026-06-10 - Fanotify D7 bypass audit

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** fs / fanotify / VFS / task-fd / docs

**Summary:** fanotify D7 完成旁路审计与收口。审计未发现 syscall / VFS / task-fd / procfs
层 downcast fanotify private state，也未发现暂缓 feature silent success。用户指出的
`FanPathKey` 未使用问题已收口：当前阶段删除该占位，避免把 inode mark 错误收窄成安装时
path mark；后续 FID/name/rename 等 path-specific gate 如需 mount+dentry identity 再重新引入。

**Validation:** `just fmt kernel`、`git diff --check`、`mdbook build docs` 和 `just build` 通过；
build 仅剩既有 `sync/mono.rs` unused-import warning。未在本轮 agent 侧重跑 QEMU / LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [fanotify tracking issues](../rfcs/fanotify/tracking-issues.md)

## 2026-06-10 - FileOps status ctx 边界清理记录

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** VFS / FileOps / fcntl / openat / pipe

**Summary:** 完成 FileOps status ctx 边界清理：opened file description 仍是 file
status flags 的唯一真相源，`FileOps` data I/O 通过 `FileIoCtx` 观察 normalized status
snapshot，`check_status_flags` 承接候选状态检查，pipe 私有 `nonblock` 同步状态已移除。

**Validation:** `just fmt kernel`、`git diff --check`、`just build` 通过；build 仅剩既有
`sync/mono.rs` unused-import warning。`mdbook build docs` 通过。未在本轮 agent 侧运行
QEMU / LTP。

**Related:** [FileOps status ctx 边界清理小迭代记录](./changes/2026-06-10-fileops-status-ctx.md), [当前限制：open status flags stage-1](../register/current-limitations.md#ane-20260528-open-status-flags-stage1)

## 2026-06-13 - VFS stream file mode 边界清理

**Date:** 2026-06-13
**Authors:** doruche, Codex
**Area:** VFS / File / pipe / fanotify

**Summary:** `File` 新增 open-time immutable 的 `FileMode::STREAM` 行为标记；pipe rx/tx
和 fanotify group fd 显式标为 stream，普通 `read` / `write` / `read_exact` 不再为这些
fd 持有 VFS cursor lock，`STREAM + O_APPEND` 也不再进入 generic append-to-EOF helper。

**Validation:** `git diff --check` 和 `just build` 通过；build 仅剩既有 `sync/mono.rs`
unused-import warning。未在本轮 agent 侧运行 QEMU / LTP 或 pipe/fanotify runtime profile。

**Related:** [VFS stream file mode 小迭代记录](./changes/2026-06-13-vfs-stream-file-mode.md), [当前限制：open status flags stage-1](../register/current-limitations.md#ane-20260528-open-status-flags-stage1)

## 2026-06-13 - eventfd2 anonymous fd

**Date:** 2026-06-13
**Authors:** doruche, Codex
**Area:** VFS / FileOps / iomux / syscall ABI / LTP

**Summary:** 新增 `eventfd2(count, flags)` syscall 与 anonymous `STREAM` eventfd file：
counter state 由 eventfd 私有 owner 管理，read/write 支持 blocking、`O_NONBLOCK`、
`EFD_SEMAPHORE` 和 poll/select readiness；fd 创建映射 `EFD_CLOEXEC` / `EFD_NONBLOCK`，
并为 LTP fixture 声明 `CONFIG_EVENTFD=y`。

**Validation:** 实现 agent 运行 `just fmt kernel` 和 `git diff --check`；review agent 未发现
Apollyon / Keter 阻塞项，零长度 I/O 精确兼容保留为记录内 deferred issue。用户侧确认除
libaio/AIO provider 相关测例外，其余 LTP eventfd 测例均已通过。主控最终 `git diff --check`、
`mdbook build docs` 和 `just build` 均通过；新增文件 `git diff --no-index --check` 无
whitespace warning 输出；`just build` 仅保留既有 `sync/mono.rs` unused-import warning。

**Related:** [eventfd2 anonymous fd 小迭代记录](./changes/2026-06-13-eventfd.md)

## 2026-06-14 - timerfd anonymous fd

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** VFS / FileOps / timer / iomux / syscall ABI / LTP

**Summary:** 新增 `timerfd_create`、`timerfd_settime` 和 `timerfd_gettime`，提供
anonymous `STREAM` timerfd file：私有 state 负责 expiration counter、schedule、
blocking read、readable poll/select 和 generation-based stale callback filtering；
`CLOCK_BOOTTIME` 作为 monotonic-backed stage-1 alias，`TFD_TIMER_CANCEL_ON_SET` 作为
documented no-op 接受。

**Validation:** `just fmt kernel`、`git diff --check`、`mdbook build docs` 和 `just build`
通过；新增文件 whitespace 检查无 warning 输出；review agent 首轮发现的 no-IRQ stale
trigger drop 与 periodic tick overflow 已由主控修复并通过窄 recheck。`just build` 仅保留
既有 `sync/mono.rs` unused-import warning；未在本轮 agent 侧运行 QEMU / LTP timerfd profile。

**Related:** [timerfd anonymous fd 小迭代记录](./changes/2026-06-14-timerfd.md)

## 2026-06-14 - waitid exited-child syscall bridge

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** task / wait / syscall ABI / process group / LTP

**Summary:** 新增 `waitid` syscall bridge，复用 wait-family exited-child core 支持
`P_ALL` / `P_PID` / `P_PGID` 与 `WEXITED` / `WNOHANG` / `WNOWAIT`；`wait4.rs`、
`waitid.rs` 和共享 wait core 统一放到 `task/api/wait/` 目录，LTP wait group 启用
当前支持的 `waitid01` 至 `waitid06`、`waitid09`、`waitid11`。

**Validation:** `just fmt kernel`、`just build` 和 `git diff --check` 通过；review agent
发现的 `siginfo_t` 整块写回与 reap race stale scan 风险已修复。
`./scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64-waitid.log`
完成，glibc / musl 的 waitid 已启用测例均 PASS；wait group 总结为
`attempted=38 passed=34 failed=4 infra_failed=0 skipped=0`，剩余失败仍是
`wait402` / `wait403` 的 `/proc/sys/kernel/pid_max`、`/proc/sys/kernel/tainted`
缺失。

**Follow-up:** stopped / continued child reporting、core-dump reporting、`P_PIDFD` wait
和完整 job-control 仍未实现；`waitid07`、`waitid08`、`waitid10` 继续留作后续边界。

**Related:** [waitid 小迭代记录](./changes/2026-06-14-waitid.md), [当前限制：进程组与会话 stage-1](../register/current-limitations.md#ane-20260527-process-group-session-stage1)

## 2026-06-14 - procfs sysctl PDE 静态树

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** procfs / sysctl / pseudo inodes / LTP

**Summary:** `ProcDirEntry` 从 `/proc` root 扁平静态表扩展为 procfs-owned PDE 静态树；
root lookup / readdir 统一先走 PDE children 再进入动态 `<tgid>` 区间，并新增
`/proc/sys/kernel/{pid_max,shmmax,shmall,shmmni,cap_last_cap}` 只读观察面。

**Validation:** `just build` 和 `git diff --check` 通过；build 仅保留既有
`sync/mono.rs` unused-import warning。review agent 未发现 Apollyon / Keter；首轮发现的
PDE kind / mode split 风险已用 seed-time assert 修复并通过窄 recheck。
`just fmt kernel --check` 仍只在 generated `kconfig_defs.rs` / `platform_defs.rs` 上报告
既有格式差异。未在本轮 agent 侧运行 QEMU / LTP runtime profile。

**Follow-up:** 第一批 sysctl 文件只覆盖只读观察面；可写 sysctl save / restore、
`/proc/sysvipc/shm`、kernel config fixture、`getrlimit(RLIMIT_CORE)`、random / pipe /
fanotify / vm knobs 仍未关闭。

**Related:** [procfs sysctl PDE 静态树小迭代记录](./changes/2026-06-14-procfs-sysctl-pde-tree.md), [当前限制：SysV shm LTP infra](../register/current-limitations.md#ane-20260529-sysv-shm-ltp-infra-stage1), [当前限制：Signal LTP infra](../register/current-limitations.md#ane-20260607-signal-ltp-infra-stage1)
