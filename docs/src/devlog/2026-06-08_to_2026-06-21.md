# 2026-06-08 至 2026-06-21

## 使用说明

- 本文件覆盖 2026-06-08 至 2026-06-21 的开发日志。
- 新条目按时间顺序追加。
- 条目格式使用模板页中的开发日志模板。

## Entries

## 2026-06-08 - `pselect6 exceptfds` 兼容回退

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / iomux / pselect6 / lmbench / docs

**Summary:** `pselect6` 重新接受 `exceptfds` 作为兼容 stub：仍校验置位 fd、清空 exception 输出并打日志，但不再因为没有内部 `POLLPRI` readiness source 而在 register scan 阶段返回 `ENOTSUP`。

**Validation:** `just build` 通过；`git diff --check` 通过。未在本轮 agent 侧重跑 lmbench / QEMU。

**Related:** [pselect6 exceptfds 小迭代记录](./changes/2026-06-08-pselect6-exceptfds-compat.md), [当前限制：pselect6 exceptfds stage-1](../register/current-limitations.md#ane-20260608-pselect6-exceptfds-stage1)

## 2026-06-08 - 空 iomux 超时睡眠修复

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / iomux / ppoll / pselect6 / scheduler

**Summary:** `ppoll` / `pselect6` 的 shared iomux wait helper 新增空集合 / 无 source timeout sleep 路径：`timeout == 0` 仍立即返回，正 timeout 或 NULL timeout 会进入 wait-core interruptible wait，避免 `select(0, ..., 1us)` 在 iozone-glibc 启动同步中退化为纯 busy loop。

**Validation:** `just fmt kernel` 通过；sandboxed `just build` 在 vendored `lwext4_rust` C 编译处命中已知 `Bad system call`；unsandboxed `just build` 通过，仅保留既有 `sync/mono.rs` unused-import warning。未在本轮 agent 侧重跑 iozone QEMU profile。

**Related:** [空 iomux 超时睡眠小迭代记录](./changes/2026-06-08-iomux-empty-timeout-sleep.md)

## 2026-06-08 - Fanotify 事务启动

**Date:** 2026-06-08
**Authors:** doruche, Codex
**Area:** fs / VFS / syscall ABI / task-fd / iomux / LTP

**Summary:** fanotify 公开 RFC 进入实现事务。总控完成前置检查并建立事务日志；首个目标是按
agent orchestration 的 D1/D2 依赖拓扑推进 Gate A，使 task/fd 通用能力与 `fs::fanotify`
owner module 合流，闭合 Stage 0 + Stage 1 合并 gate。

**Validation:** 本条为事务启动记录；未运行 `just build`、QEMU 或 LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [RFC-20260604-fanotify](../rfcs/fanotify/index.md)

## 2026-06-09 - Fanotify Gate C corrective repair

**Date:** 2026-06-09
**Authors:** doruche, Codex
**Area:** fs / fanotify / task-fd / lifecycle

**Summary:** D4 后 `fanotify07` 暴露 group fd semantic teardown 可能从 deferred task / fd-table
`Drop` 进入 fanotify registry mutex。`FANOTIFY-041` 修复将 final-release 移到 exit、
fd-table replacement / unshare 和 clone 失败等显式生命周期路径；`FilesState::drop()` 只保留
遗漏清理断言，fanotify final-release 入口断言 interrupts-enabled / sleepable context。

**Validation:** `git diff --check` 通过；`just build` 通过，仅保留既有 `sync/mono.rs`
unused-import warning。未由 agent 侧运行 QEMU / LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [fanotify tracking issues](../rfcs/fanotify/tracking-issues.md)

## 2026-06-09 - User-test staged 工具通道

**Date:** 2026-06-09
**Authors:** doruche, Codex
**Area:** anemone-apps/user-test / competition runner / fixture staging

**Summary:** `user-test` 新增第一版 staged test-tool 通道：在 `/dev/vdb` 挂载到 `/mnt`
后、`chroot("/mnt")` 前，把启动盘 staging 目录中的工具按 source/dest manifest
二进制安全复制到测试盘。本轮 rv64 先覆盖 `/bin/mkfs.ext4`，用于区分测试材料缺口和
后续 block / loop / mount / filesystem / syscall 语义问题。

**Validation:** `file anemone-apps/user-test/staged/riscv/mke2fs` 确认为静态链接
RISC-V ELF；`just fmt user-test` 通过；`just xtask app build user-test --arch riscv64`
通过；`just xtask app build user-test --arch loongarch64` 通过；agent 侧 `just xtask rootfs
mkfs -c rootfsconfig-rv` 完成 staging 后在 `virt-make-fs` / `supermin` 主机环境层失败，
用户环境可成功构建镜像；staging 目录下的 `/fixtures/user-test/tools/mkfs.ext4` 已确认
存在且可执行；`git diff --check` 通过。未在本轮 agent 侧运行 QEMU 或 LTP。

**Related:** [User-test staged 工具通道小迭代记录](./changes/2026-06-09-user-test-staged-tools.md)

## 2026-06-10 - Fanotify D6 negative gate

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** fs / fanotify / syscall ABI / LTP

**Summary:** fanotify D6 收口为负向边界与 LTP 分类：FID/name report flags、pidfd、permission、
FS error、evictable mark、dirent/name/self/metadata masks 和 `FAN_MARK_IGNORE` 继续在
`fanotify_init()` / `fanotify_mark()` parser 边界稳定 fail closed；未引入 FID/name 正向 ABI、
permission response、procfs fdinfo 或 dirent hook。

**Validation:** 使用源码级 grep、LTP fanotify 源码比对和 D5 完成后的完整 rv64 fanotify profile
日志完成分类；本条记录时未重新运行 QEMU / LTP。`git diff --check`、`mdbook build docs` 和
`just build` 作为收口 gate 运行。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [RFC-20260604-fanotify](../rfcs/fanotify/index.md)

## 2026-06-10 - Fanotify D7 bypass audit

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** fs / fanotify / VFS / task-fd / docs

**Summary:** fanotify D7 完成旁路审计与收口。审计未发现 syscall / VFS / task-fd / procfs
层 downcast fanotify private state，也未发现暂缓 feature silent success。用户指出的
`FanPathKey` 未使用问题已收口：当前阶段删除该占位，避免把 inode mark 错误收窄成安装时
path mark；后续 FID/name/rename 等 path-specific gate 如需 mount+dentry identity 再重新引入。

**Validation:** `just fmt kernel`、`git diff --check`、`mdbook build docs` 和 `just build` 通过；
build 仅剩既有 `sync/mono.rs` unused-import warning。未在本轮 agent 侧重跑 QEMU / LTP。

**Related:** [Fanotify 事务日志](./transactions/2026-06-08-fanotify.md), [fanotify tracking issues](../rfcs/fanotify/tracking-issues.md)

## 2026-06-10 - FileOps status ctx 边界清理记录

**Date:** 2026-06-10
**Authors:** doruche, Codex
**Area:** VFS / FileOps / fcntl / openat / pipe

**Summary:** 完成 FileOps status ctx 边界清理：opened file description 仍是 file
status flags 的唯一真相源，`FileOps` data I/O 通过 `FileIoCtx` 观察 normalized status
snapshot，`check_status_flags` 承接候选状态检查，pipe 私有 `nonblock` 同步状态已移除。

**Validation:** `just fmt kernel`、`git diff --check`、`just build` 通过；build 仅剩既有
`sync/mono.rs` unused-import warning。`mdbook build docs` 通过。未在本轮 agent 侧运行
QEMU / LTP。

**Related:** [FileOps status ctx 边界清理小迭代记录](./changes/2026-06-10-fileops-status-ctx.md), [当前限制：open status flags stage-1](../register/current-limitations.md#ane-20260528-open-status-flags-stage1)

## 2026-06-13 - VFS stream file mode 边界清理

**Date:** 2026-06-13
**Authors:** doruche, Codex
**Area:** VFS / File / pipe / fanotify

**Summary:** `File` 新增 open-time immutable 的 `FileMode::STREAM` 行为标记；pipe rx/tx
和 fanotify group fd 显式标为 stream，普通 `read` / `write` / `read_exact` 不再为这些
fd 持有 VFS cursor lock，`STREAM + O_APPEND` 也不再进入 generic append-to-EOF helper。

**Validation:** `git diff --check` 和 `just build` 通过；build 仅剩既有 `sync/mono.rs`
unused-import warning。未在本轮 agent 侧运行 QEMU / LTP 或 pipe/fanotify runtime profile。

**Related:** [VFS stream file mode 小迭代记录](./changes/2026-06-13-vfs-stream-file-mode.md), [当前限制：open status flags stage-1](../register/current-limitations.md#ane-20260528-open-status-flags-stage1)

## 2026-06-13 - eventfd2 anonymous fd

**Date:** 2026-06-13
**Authors:** doruche, Codex
**Area:** VFS / FileOps / iomux / syscall ABI / LTP

**Summary:** 新增 `eventfd2(count, flags)` syscall 与 anonymous `STREAM` eventfd file：
counter state 由 eventfd 私有 owner 管理，read/write 支持 blocking、`O_NONBLOCK`、
`EFD_SEMAPHORE` 和 poll/select readiness；fd 创建映射 `EFD_CLOEXEC` / `EFD_NONBLOCK`，
并为 LTP fixture 声明 `CONFIG_EVENTFD=y`。

**Validation:** 实现 agent 运行 `just fmt kernel` 和 `git diff --check`；review agent 未发现
Apollyon / Keter 阻塞项，零长度 I/O 精确兼容保留为记录内 deferred issue。用户侧确认除
libaio/AIO provider 相关测例外，其余 LTP eventfd 测例均已通过。主控最终 `git diff --check`、
`mdbook build docs` 和 `just build` 均通过；新增文件 `git diff --no-index --check` 无
whitespace warning 输出；`just build` 仅保留既有 `sync/mono.rs` unused-import warning。

**Related:** [eventfd2 anonymous fd 小迭代记录](./changes/2026-06-13-eventfd.md)

## 2026-06-14 - timerfd anonymous fd

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** VFS / FileOps / timer / iomux / syscall ABI / LTP

**Summary:** 新增 `timerfd_create`、`timerfd_settime` 和 `timerfd_gettime`，提供
anonymous `STREAM` timerfd file：私有 state 负责 expiration counter、schedule、
blocking read、readable poll/select 和 generation-based stale callback filtering；
`CLOCK_BOOTTIME` 作为 monotonic-backed stage-1 alias，`TFD_TIMER_CANCEL_ON_SET` 作为
documented no-op 接受。

**Validation:** `just fmt kernel`、`git diff --check`、`mdbook build docs` 和 `just build`
通过；新增文件 whitespace 检查无 warning 输出；review agent 首轮发现的 no-IRQ stale
trigger drop 与 periodic tick overflow 已由主控修复并通过窄 recheck。`just build` 仅保留
既有 `sync/mono.rs` unused-import warning；未在本轮 agent 侧运行 QEMU / LTP timerfd profile。

**Related:** [timerfd anonymous fd 小迭代记录](./changes/2026-06-14-timerfd.md)

## 2026-06-14 - waitid exited-child syscall bridge

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** task / wait / syscall ABI / process group / LTP

**Summary:** 新增 `waitid` syscall bridge，复用 wait-family exited-child core 支持
`P_ALL` / `P_PID` / `P_PGID` 与 `WEXITED` / `WNOHANG` / `WNOWAIT`；`wait4.rs`、
`waitid.rs` 和共享 wait core 统一放到 `task/api/wait/` 目录，LTP wait group 启用
当前支持的 `waitid01` 至 `waitid06`、`waitid09`、`waitid11`。

**Validation:** `just fmt kernel`、`just build` 和 `git diff --check` 通过；review agent
发现的 `siginfo_t` 整块写回与 reap race stale scan 风险已修复。
`./scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64-waitid.log`
完成，glibc / musl 的 waitid 已启用测例均 PASS；wait group 总结为
`attempted=38 passed=34 failed=4 infra_failed=0 skipped=0`，剩余失败仍是
`wait402` / `wait403` 的 `/proc/sys/kernel/pid_max`、`/proc/sys/kernel/tainted`
缺失。

**Follow-up:** stopped / continued child reporting、core-dump reporting、`P_PIDFD` wait
和完整 job-control 仍未实现；`waitid07`、`waitid08`、`waitid10` 继续留作后续边界。

**Related:** [waitid 小迭代记录](./changes/2026-06-14-waitid.md), [当前限制：进程组与会话 stage-1](../register/current-limitations.md#ane-20260527-process-group-session-stage1)

## 2026-06-14 - procfs sysctl PDE 静态树

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** procfs / sysctl / pseudo inodes / LTP

**Summary:** `ProcDirEntry` 从 `/proc` root 扁平静态表扩展为 procfs-owned PDE 静态树；
root lookup / readdir 统一先走 PDE children 再进入动态 `<tgid>` 区间，并新增
`/proc/sys/kernel/{pid_max,shmmax,shmall,shmmni,cap_last_cap}` 只读观察面。

**Validation:** `just build` 和 `git diff --check` 通过；build 仅保留既有
`sync/mono.rs` unused-import warning。review agent 未发现 Apollyon / Keter；首轮发现的
PDE kind / mode split 风险已用 seed-time assert 修复并通过窄 recheck。
`just fmt kernel --check` 仍只在 generated `kconfig_defs.rs` / `platform_defs.rs` 上报告
既有格式差异。未在本轮 agent 侧运行 QEMU / LTP runtime profile。

**Follow-up:** 第一批 sysctl 文件只覆盖只读观察面；可写 sysctl save / restore、
`/proc/sysvipc/shm`、kernel config fixture、`getrlimit(RLIMIT_CORE)`、random / pipe /
fanotify / vm knobs 仍未关闭。

**Related:** [procfs sysctl PDE 静态树小迭代记录](./changes/2026-06-14-procfs-sysctl-pde-tree.md), [当前限制：SysV shm LTP infra](../register/current-limitations.md#ane-20260529-sysv-shm-ltp-infra-stage1), [当前限制：Signal LTP infra](../register/current-limitations.md#ane-20260607-signal-ltp-infra-stage1)

## 2026-06-14 - SysV shm credentials permission hook

**Date:** 2026-06-14
**Authors:** doruche, Codex
**Area:** mm / SysV shm / credentials / LTP

**Summary:** SysV shm 接入 shm-local credential permission hook：创建段记录 effective
uid/gid，keyed `shmget`、`shmat`、`IPC_STAT` / `SHM_STAT`、`IPC_SET` / `IPC_RMID` 和
`SHM_LOCK` / `SHM_UNLOCK` 分别走 DAC 或 owner/capability gate；`SHM_STAT_ANY` 保持
Linux `/proc/sysvipc/shm` 风格的传统 DAC bypass。

**Validation:** implementation worker 运行 `just fmt kernel`、`just fmt shm-test`、
`just xtask app build shm-test --arch riscv64`、
`just xtask app build shm-test --arch loongarch64`、`git diff --check` 和 `just build`
通过；review agent 未发现 Apollyon / Keter；主控补充 `SHM_UNLOCK` denial smoke 后重跑
`just fmt shm-test` 和 shm-test 双架构 app build 通过；`mdbook build docs` 和主控最终
`just build` 通过，build 仅保留既有 `sync/mono.rs` unused-import warning。未在本轮
agent 侧运行 QEMU / LTP runtime profile。

**Follow-up:** `shmget04`、`shmat02`、`shmctl02` 仍需 user-test profile 验证后再关闭
permissions residual limitation；真实 `SHM_LOCK` page residency / memlock 和
`/proc/sysvipc/shm` 仍属于既有后续项。

**Related:** [SysV shm credentials permission hook 小迭代记录](./changes/2026-06-14-sysv-shm-cred-permissions.md), [当前限制：SysV shm permissions residual](../register/current-limitations.md#ane-20260525-sysv-shm-permissions-pending-credentials), [当前限制：SysV shm lock residency stage-1](../register/current-limitations.md#ane-20260525-sysv-shm-lock-residency-stage1), [当前限制：SysV shm LTP infra](../register/current-limitations.md#ane-20260529-sysv-shm-ltp-infra-stage1)

## 2026-06-15 - backend-aware fcntl pipe-size 分发

**Date:** 2026-06-15
**Authors:** doruche, Codex
**Area:** VFS / FileOps / fcntl / pipe

**Summary:** `F_GETPIPE_SZ` / `F_SETPIPE_SZ` 不再由 generic `fcntl` syscall
adapter 直接调用 pipe 私有 helper。完整 `FcntlCmd` parse 留在 syscall 边界；
VFS 只暴露收窄后的 `FileFcntlCmd` 子集和可选 `FileOps::fcntl` hook，用于
file-object command。

**Validation:** `just fmt kernel`、`git diff --check`、`mdbook build docs` 和
`just build` 通过；`just build` 只报告既有 `sync/mono.rs` unused-import warning。
主控代码审查未发现 owner boundary 或 pipe handoff 相关的 Apollyon / Keter 问题。
本轮未运行 QEMU / LTP runtime。

**Follow-up:** memfd seals、POSIX/OFD locks 和真实 pipe capacity growth 都仍是独立
后续工作；本轮只用既有 pipe-size 语义验证 backend fcntl dispatch 边界。

**Related:** [backend-aware fcntl 小迭代记录](./changes/2026-06-15-backend-aware-fcntl.md), [FileOps status ctx 边界清理](./changes/2026-06-10-fileops-status-ctx.md)

## 2026-06-15 - kthread 与 inode shrinker RFC 追补

**Date:** 2026-06-15
**Authors:** EDGW, Codex
**Area:** docs / task / kthread / VFS / inode cache / ext4

**Summary:** 根据 commit `2b0e3900279895c3d8eb604e463249a02c3bddc9` 追补两个目录级 RFC：[KThread](../rfcs/kthread/index.md) 记录 `kthreadd` 创建代理、typed entry、cooperative stop/park 生命周期和 `KThreadService` 合同；[Inode Shrinker](../rfcs/inode-shrinker/index.md) 记录自循环 `io_shrink_threshold` gate 的 inode cache shrinker、superblock eviction path、ext4 shrinkable icache 和 backing file cache page counter。

**Validation:** 同日 follow-up 增加 shrinker 阈值 gate 和 kconfig 参数；构建/检查结果见本轮 agent 输出。未运行 QEMU 或 LTP runtime。

**Related:** [KThread 事务日志](./transactions/2026-06-14-kthread.md), [Inode Shrinker 事务日志](./transactions/2026-06-14-inode-shrinker.md)

## 2026-06-15 - OOM Killer RFC 实现启动

**Date:** 2026-06-15
**Authors:** EDGW, Codex
**Area:** mm / frame allocator / user-space VMO / task / signal / kthread

**Summary:** `oom_kill_threshold` 进入 kconfig，frame allocator 成功分配后通过 `FrameAllocatorStats::exceeds_oom_kill_threshold()` 唤醒 `oom-killer-0` kthread；worker 醒后重查阈值，按 `UserSpace` 独占物理页 snapshot 选择最大 eligible thread group 并投递 kernel-origin `SIGKILL`。

**Change:** 本轮纠正 victim score 为“独占物理页数”而不是 resident-like 总量；`ShadowObject` 在 parent/backing VMO 只被当前 shadow 链持有时递归计入 parent 链。`FrameAllocatorStats` 只公开 IO shrinker 和 OOM killer 两个语义 threshold 函数，裸 percentage helper 保持私有。

**Validation:** Docker `gallant_lamarr` 内以 `ubuntu` 用户运行 `just build` 通过；以 `root` 用户运行 `just rootfs mkfs -c rootfsconfig-rv` 通过，并确认 `oom-killer-test` 被 staging 到 rv rootfs。QEMU/user-test runtime 尚未运行。

**Related:** [OOM Killer 事务日志](./transactions/2026-06-15-oom-killer.md), [RFC-20260615-oom-killer](../rfcs/oom-killer/index.md)

## 2026-06-16 - KThread Core 事务启动

**Date:** 2026-06-16
**Authors:** doruche, Codex
**Area:** task / topology / procfs / kthread

**Summary:** `kthread-core` 公开 RFC 进入实现前置检查事务。主控建立事务日志并记录当前代码落点：legacy `KThreadService`、weak `KThreadRef`、park/unpark、未固定的 `kthreadd` TID、ordinary `TaskBinding::Leader`、entry 尾部完整 `kernel_exit()` 和尚未分流的 user-facing API 仍是待纠偏基线。

**Validation:** 本条为事务启动和 preflight 记录；未运行 `just build`、QEMU 或 LTP。docs whitespace 与导航构建验证结果见事务日志后续条目。

**Related:** [KThread Core 事务日志](./transactions/2026-06-16-kthread-core.md), [RFC-20260616-kthread-core](../rfcs/kthread-core/index.md)

## 2026-06-16 - KThread Core Phase 1

**Date:** 2026-06-16
**Authors:** doruche, Codex
**Area:** task / kthread / mm / fs

**Summary:** 完成 `kthread-core` 阶段 1：删除 `KThreadService` 源文件和 kthread park/unpark surface，把 lifecycle 状态收窄为 stop / wake / exited；inode shrinker 与 OOM killer consumer loop 改为只检查 `should_stop()` 和自身压力 / victim predicate。未触碰 TID allocator、topology binding、exit path、procfs 或 user-facing API。

**Validation:** `git diff --check` 通过；`just build` 通过，仅有既有 `anemone-kernel/src/sync/mono.rs` unused import warning；Phase 1 scoped source audit 无 service/park 源码残留；只读 reviewer 未发现阻塞 gate 的问题。未运行 QEMU / LTP。

**Related:** [KThread Core 事务日志](./transactions/2026-06-16-kthread-core.md), [RFC-20260616-kthread-core](../rfcs/kthread-core/index.md)

## 2026-06-16 - KThread Core Phase 2 Checkpoint

**Date:** 2026-06-16
**Authors:** doruche, Codex
**Area:** task / topology / kthread

**Summary:** 完成 `kthread-core` 阶段 2 checkpoint：固定 `Tid::KTHREADD == 2`，普通 TID allocator 从 3 到 `MAX_PROCESSES`，TID 1 / 2 由内部 one-shot handle 消费；引入 `ThreadGroupType::{User,KThread}`、`TaskBinding::{UserLeader,KThread,Member}`、User-only accessor panic 与 topology shape assertion。`TaskBinding::KThread` 仍是未启用 scaffolding；负责人接受将 `kthreadd` 实际 `KThread` publish 与 ordinary kthread binding、专用 exit、topology/procfs unpublish 和 user-facing API 分流并入阶段 4 同 gate。

**Validation:** `just fmt kernel`、`git diff --check`、`just build` 和 Phase 2 source audit 通过；`phase2-reviewer` 确认 write set 未越界、ordinary kthread 未提前切换、TID allocator 范围与 `/proc/sys/kernel/pid_max` 一致。未运行 boot / procfs smoke，因为当前尚未启用 `KThread` publish。

**Related:** [KThread Core 事务日志](./transactions/2026-06-16-kthread-core.md), [RFC-20260616-kthread-core](../rfcs/kthread-core/index.md)

## 2026-06-17 - KThread Core Phase 6 Closeout

**Date:** 2026-06-17
**Authors:** doruche, Codex
**Area:** task / topology / procfs / kthread / mm

**Summary:** `kthread-core` 第一阶段 implementation gates 关闭：kthread core 已迁移到 strong handle / control、fixed `kthreadd` TID 2、`TaskBinding::KThread` topology、专用 `kthread_exit()`、procfs/topology unpublish 和 user-facing API fail-closed 分流；consumer closeout 确认 inode shrinker 与 OOM killer 持有 `KThreadHandle`，OOM victim selection 也先按 `ThreadGroupType::User` 过滤，不再只依赖 kernel task flag。

**Validation:** 阶段 6 按用户指示未运行 `git diff --check`、`just build`、QEMU、user-test、LTP 或 focused smoke；本条只记录源码审计与文档收口事实。

**Related:** [KThread Core 事务日志](./transactions/2026-06-16-kthread-core.md), [RFC-20260616-kthread-core](../rfcs/kthread-core/index.md)

## 2026-06-17 - splice family copy-backed stage-1

**Date:** 2026-06-17
**Authors:** doruche, Codex
**Area:** VFS / pipe / syscall ABI / LTP splice

**Summary:** 注册 `splice` / `tee` / `vmsplice` generic syscall 入口；`splice` 采用页大小内核
buffer 做 copy-backed file/pipe/pipe 搬运，并覆盖 bad fd、access mode、append output、
neither-pipe、pipe offset pointer 和 same-pipe errno。`tee` / `vmsplice` 本轮只覆盖错误语义，
valid functional path 明确返回 unsupported。

**Validation:** implementation worker 运行 `just fmt kernel`、`git diff --check` 和新增文件
`git diff --no-index --check` 通过；主控重跑 `just fmt kernel`、`git diff --check` 和
`just build` 通过，build 仅保留既有 `sync/mono.rs` unused-import warning。未运行 QEMU /
user-test / LTP runtime profile。

**Follow-up:** 需要定向运行 `splice03`、`splice07`、`tee02`、`vmsplice02`；真实 zero-copy
splice、socket splice、functional `tee` / `vmsplice`、per-call `SPLICE_F_NONBLOCK` 和动态 pipe
capacity 仍按 stage-1 limitation 跟踪。

**Related:** [splice family copy-backed stage-1 小迭代记录](./changes/2026-06-17-splice-copy-stage1.md), [当前限制：splice family copy-backed stage-1](../register/current-limitations.md#ane-20260617-splice-family-copy-backed-stage1)

## 2026-06-18 - RFC workflow feedback loop

**Date:** 2026-06-18
**Authors:** doruche, Codex
**Area:** docs / RFC workflow / agent workflow

**Summary:** RFC 工作流补充“受控反馈”规则：文档层闭合不再被解释为实现前清空所有未知，而是要求 accepted contract、验证 floor、停止条件和反馈入口明确；高风险点可以通过 probe / vertical slice gate 验证，implementation feedback 按影响回写 transaction devlog、`implementation.md`、RFC canonical 文本、tracking issues 或 register / current limitations。后续格式收口明确不默认新增通用 `feedback.md` / `probe.md`，计划写在 `implementation.md`，执行反馈写在 transaction devlog；同时追加 anti-hacking 边界，禁止以反馈为名缩小目标、调低验证集合、隐藏失败路径或削弱不变量。

**Validation:** `git diff --check` 通过；新增小迭代记录的 no-index whitespace 检查无 warning 输出；`mdbook build docs` 通过。

**Related:** [RFC workflow feedback loop 小迭代记录](./changes/2026-06-18-rfc-feedback-loop.md), [RFC 工作流](../rfc-workflow.md), [RFC 模板](../rfc-template.md)

## 2026-06-18 - Mount Tree Legacy API 事务启动

**Date:** 2026-06-18
**Authors:** doruche, Codex
**Area:** fs / VFS / mount / LTP

**Summary:** `mount-tree-legacy-api` 公开 RFC 进入实现事务。主控建立事务日志并关闭阶段 0：RFC 当前无 active design blocker，lazy-detach cleanup owner 与 detached / moved `PathRef` namei 语义保留为阶段 6 Gate P1/P2 受控反馈；阶段 1 只允许启动 UAPI parser、flag 分层、legacy data 和 syscall alias worker。

**Validation:** `git diff --check` 通过；新增事务文件 no-index whitespace 检查无 warning 输出；`mdbook build docs` 通过。未运行 `just build`、QEMU 或 LTP。

**Related:** [Mount Tree Legacy API 事务日志](./transactions/2026-06-18-mount-tree-legacy-api.md), [RFC-20260604-mount-tree-legacy-api](../rfcs/mount-tree-legacy-api/index.md)

## 2026-06-18 - Mount Tree Legacy API Phase 1

**Date:** 2026-06-18
**Authors:** doruche, Codex
**Area:** fs / VFS / mount / syscall ABI

**Summary:** 完成 `mount-tree-legacy-api` 阶段 1：补齐 legacy mount/umount UAPI 常量；将 mount-family syscall adapter 目录化到 `fs/api/mount/`；raw mount flags 在 syscall 边界分成 operation bits、unsupported attrs、harmless `MS_SILENT` 和 per-mount `RDONLY`；legacy `MountData` 作为 parser input 透传到 backend，当前 backend 只接受 NULL / 空 data；`tmpfs` 与 LTP 临时 `ext2` / `ext3` / `vfat` alias 仍只停在 syscall adapter；unsupported mount/umount flags、non-empty data 和 `-o loop` 稳定拒绝并打日志。

**Validation:** `just fmt kernel`、`just build`、`git diff --check` 和 `mdbook build docs` 通过；新增 `fs/api/mount/*.rs` no-index whitespace 检查无 warning；只读 reviewer 未发现代码层 blocker，devlog stale finding 已通过 transaction 条目中和。未运行 QEMU / user-test / LTP runtime 或 runtime KUnit runner。

**Related:** [Mount Tree Legacy API 事务日志](./transactions/2026-06-18-mount-tree-legacy-api.md), [mount tree legacy API 实施计划](../rfcs/mount-tree-legacy-api/implementation.md)

## 2026-06-19 - Mount Tree Legacy API Phase 7 Closeout

**Date:** 2026-06-19
**Authors:** doruche, Codex
**Area:** fs / VFS / mount / LTP / docs

**Summary:** `mount-tree-legacy-api` 第一版实现事务完成阶段 7 收口。`mount-legacy`
LTP group 按用户要求保持宽覆盖，作为评分 / 观测 profile 使用；`fs_bind*`、
`fs_bind_move*`、`fs_bind_rbind*` 和 `test_robind*` 不因部分子功能未实现而缩窄。
phase 6 日志中的 glibc `mount-legacy` 原始汇总为 `attempted=155 passed=7 failed=148
infra_failed=0 skipped=0`，其中 KUnit 为 `Running 95 tests...` / `All tests passed!`；
phase 7 将 whole-case PASS、partial TPASS、accepted limitations、TCONF 和环境 blocker
分开登记。

**Validation:** `just fmt user-test`、`just xtask app build user-test --arch riscv64`、
`mdbook build docs`、`git diff --check` 和 `just build` 通过；本条使用既有
`build/mount-legacy-phase6-rv64.log` 做结果分类，未重新运行 QEMU / LTP。`user-test`
runner 已把纯 LTP `TCONF` 退出码 `32` 计为 skipped，混合 `TCONF|TFAIL/TBROK` 仍按失败处理。

**Follow-up:** 后续按 current limitations 分拆 gate：shared/slave/unbindable propagation、
mount flag / `statfs` matrix、fstype alias bridge、ROFS mmap/writeback、unmount cleanup /
`MNT_EXPIRE` 和 fanotify observer cleanup。

**Related:** [Mount Tree Legacy API 事务日志](./transactions/2026-06-18-mount-tree-legacy-api.md), [当前限制](../register/current-limitations.md)

## 2026-06-20 - Threaded Timer Event 事务启动

**Date:** 2026-06-20
**Authors:** doruche, Codex
**Area:** time / timer / scheduler / kthread / timerfd / signal

**Summary:** `threaded-timer-event` 公开 RFC 进入实现事务。主控建立事务日志，关闭实现前 Euclid proof gaps：Gate P1 运行证据和 P1/P2/P3 exit path、`ITIMER_REAL` signal action commit point、以及具体 itimer/alarm LTP validation floor 均折回 canonical 文本。阶段 0 preflight 确认当前 IRQ timer 调用面只有 wait-core timeout、`timerfd` 和 `ITIMER_REAL`；第一批迁移仍限定为 `timerfd` / `ITIMER_REAL`，wait-core timeout 留在 IRQ lane。

**Validation:** 本条为事务启动和 preflight 记录；Phase 1 代码与文档导航验证见事务日志后续条目。

**Related:** [Threaded Timer Event 事务日志](./transactions/2026-06-20-threaded-timer-event.md), [RFC-20260620-threaded-timer-event](../rfcs/threaded-timer-event/index.md)
