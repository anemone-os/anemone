# 2026-07-29 - Flock

**Status:** Completed / R0 Closed / Stage 0-1 Closed / FLOCK-CUTOVER Completed
**Date:** 2026-07-29
**Owner:** doruche, Codex
**Canonical Plan:** [RFC-20260728-flock R0](../../rfcs/flock/index.md),
[目标与不变量](../../rfcs/flock/invariants.md),
[Stage 1 Ready definition](../../rfcs/flock/implementation.md#7-stage-1-readyacceptance-closure-与-flock-cutover)
**Canonical Revision:** R0
**Contract Impact:** Preserve `OPENED-DESC-001/002/003`、`OPENED-DESC-LIVENESS-001` 与
`SCHED-WAKE-001..004`；`OPENED-DESC-RETIRE-001`、`FLOCK-DOMAIN-001`、`FLOCK-WAIT-001`、
`FLOCK-LIFECYCLE-001` 已由 `FLOCK-CUTOVER` 原子 Introduce / Effective

## Scope and authorization

用户于 2026-07-29 建立唯一 GOAL，明确授权完成 Checkpoint 0S，并要求执行其前置 review、activation、
frozen write set、validation、退出与 write-back。本授权满足 Stage 0 从 `Ready` 进入 `Active` 的开发者
authority，但只允许从 0S 开始并在 0S closure 后停止；不得自动进入 0A、Stage 1 或
`FLOCK-CUTOVER`，也不授权 target、owner、public API、shared contract、ABI、visible semantics、
acceptance 或 resolved manifest 扩张。

后续用户建立新的唯一 GOAL，明确授权只完成 Checkpoint 0A，并要求重新执行该 checkpoint 的前置读取、frozen
write set、review、validation、退出与 write-back；0B及后续 gate 仍未授权。用户在 implementation review 中
进一步明确：IRQ-off OOM 是当前内核设计的已知局限，后续另行修正；0A 直接假设该问题不存在，不得为规避它
扭曲 flock 代码形状。该决定只解析 0A 的 allocation implementation preference，不改变 R0 target、ABI、owner、
contract或acceptance boundary。

本轮用户建立新的唯一GOAL，明确授权只完成Checkpoint 0B，并要求重新执行canonical前置读取、frozen write
set、review、validation、退出与write-back；0C、Stage 1与`FLOCK-CUTOVER`仍未授权。本授权不改变R0 target、
owner、ABI、visible semantics、acceptance或resolved manifest。

随后用户建立新的唯一GOAL，明确授权只完成Checkpoint 0C，并要求重新读取canonical RFC、implementation、
tracking issues、register与本transaction，执行0C的runtime、observability、完整diff review、validation、退出与
write-back；`0 -> 1 Implementation Resolution Gate`、Stage 1与`FLOCK-CUTOVER`仍未授权。本授权不改变R0
target、owner、public API、shared contract、ABI、visible semantics、acceptance或resolved manifest。

Stage 0关闭并完成独立`0 -> 1` resolution后，开发者分别授权Checkpoint 1A acceptance assets与Checkpoint 1B
runtime/review/cutover。1B授权允许读取开发者运行产生的双架构日志、恢复validation-only profile、执行完整diff
review，并在全部停止/退出条件闭合后原子更新冻结的task/VFS contract与closure docs；不允许修改production
kernel、ABI、scheduler、signal、filesystem backend、record-lock、Kconfig、register或acceptance boundary。

## R0 acceptance and activation preflight

Preflight 在 clean `dev/drc/omega@c69e2143` 读取 AGENTS/LOCAL、flock canonical RFC 正文、
implementation、tracking issues、register、opened-description 与 asynchronous-wake current contracts，
以及 live `task::files`、`File::inode() / InodeRef`、`Event::listen/publish`、syscall registration、
`anemone-rs` wrappers、两个 pretest rootfs manifest、user-test runner 与两条 user-test wrapper。
启动时没有 transaction 或 dirty change，故 frozen manifest 无重叠归属。

`just --list`、`just fmt kernel --help`、`just app build --help` 与 `just build --help` 均与
canonical commands 一致。R0 review 确认 cooperative retirement、grant-state convergence、ordinary
restart、generic local default、Contract Impact 与 acceptance boundary 自洽；tracking 中没有当前
Apollyon/Keter/Euclid。register 的 Event wake race 保留为后续 runtime 外部 blocker，IRQ-off allocation
风险保留为 0A source audit，不改变 0S 路线。review 接受 R0 / Accepted for Implementation，但没有执行
contract cutover。

## Execution log

### 2026-07-29 - Stage 0 activated at Checkpoint 0S

**Write-set lock:** 0S production subset只允许删除 `anemone-kernel/src/task/files.rs`，新增
`task/files/{mod.rs,opened_description.rs,descriptor.rs,table.rs}`，并使用canonical Stage 0
transaction/docs write-back subset。current contracts、register、scheduler、filesystem backend、signal、
record-lock、Kconfig 与全部 0A/0B code path 保持只读。

**Activation-time cutover:** None。全部新 contract ID 保持 Not Effective；0A未激活。

### 2026-07-29 - Checkpoint 0S implementation and review

单文件 `task::files` 按同一 owner 内的稳定角色拆为四个文件：`mod.rs`只声明子模块并维持既有
`task::files::*` re-export；`opened_description.rs`拥有`ProcFile` publication lifecycle、
capability/lease、`FileDescOps` context与lifecycle KUnit；`descriptor.rs`拥有`FileDesc`、flags与I/O
facade；`table.rs`拥有`Fd`/reservation、`FilesState`、`Task` fd-table facade。

拆分没有加入flock字段、retirement context、新抽象、语义修复或外部路径重命名。只把 sibling 协作必需的
`ProcFile`字段/方法、`FileDesc` publication helper与lifecycle KUnit调用的四个table helper提升为
`pub(super)`；既有`pub`/`pub(crate)` surface由`mod.rs`原路径重新导出。

Checkpoint review逐项对照搬迁前后声明与caller：`description_refs`仍是唯一
`Unpublished -> Live(n) -> Retired` truth；`Live(1) -> Retired`与existing static
`final_release`顺序未改；fd-table仍先publish/unpublish并在private guard外调用release；dup/fork/close/
close-on-exec/table teardown路径未改；三个既有KUnit body原样迁移。Apollyon/Keter/Euclid/Safe均为0。

### 2026-07-29 - Checkpoint 0S validation and closure

- `just fmt kernel --check`：已运行，exit 1；0S四个新文件无formatter diff，剩余diff全部位于frozen
  manifest外的既有vendored smoltcp三处baseline。未为此扩大write set。
- `just build --preset qemu-virt-rv64-release --bind smp=1 --bind memory=1G`：沙箱内lwext4编译被
  `Bad system call` / SIGSYS阻断；相同命令在沙箱外通过，证明production与三个既有KUnit body编译。
  本checkpoint未运行KUnit/QEMU runtime。
- 每个untracked新文件的`git diff --no-index --check -- /dev/null <path>`与tracked
  `git diff --check`：通过；new-file命令exit 1且无diagnostic只表示存在diff。
- `mdbook build docs`：通过。

Checkpoint 0S交付、R0/activation前置、定向source review、validation floor与write-back已关闭。
Stage 0保持Active；Checkpoint 0A仍Not Activated / Unauthorized。Stage 0 contract cutover为None，
current contracts与register未修改，全部新ID继续Not Effective，当前代码不提供partial flock capability。

### 2026-07-29 - Checkpoint 0A authorization and write-set lock

0A 从 clean `dev/drc/omega@332da9a9` 激活。production write set 冻结为
`fs/{flock.rs,inode.rs,mod.rs}` 与 `task/files/opened_description.rs`，另只允许 canonical Stage 0
transaction/docs write-back subset。scheduler、signal、filesystem backend、record-lock、Kconfig、current
contracts、register 与全部 0B code/oracle path 保持只读；activation-time cutover 为 None。

### 2026-07-29 - Checkpoint 0A implementation and review

新增 private `fs::flock` owner，并让每个 `Inode` 构造一份
`SpinLock<Vec<FlockGrant>> + Event` domain。grant collection 是 holder × inode mode/conflict 的唯一真相，entry
只保存 `OpenedDescriptionCapability + FlockMode`；没有 fd/path/pid key、waiter/candidate queue、backend hook、
mode mirror或公共 lock framework。normalized request在入口取得operation-local lease，每次commit在domain guard
下重验liveness与conflict；same-mode幂等，unlock只删除same owner，conversion先删除old mode并在guard外drop/
broadcast，再独立竞争target mode，后续nonblocking conflict、signal或retirement都不恢复旧mode。

blocking conflict使用non-exclusive interruptible `Event::listen(false, predicate)`；listener publication后predicate
短暂取得同一domain guard并只重验retired/conflict，notification从不代表grant success。unlock、conversion与
terminal cleanup都先从domain撤销grant，再在guard外drop与broadcast；terminal retirement即使没有grant也提交
recheck。`OpenedDescriptionRetirementCtx`只向VFS借出目标`File`和即时same-identity predicate；首次
`Live(1) -> Retired`后fixed cleanup exactly once运行并先于existing static hook，不暴露完整`ProcFile`、lifecycle
word、live lease、fd-table lock或callback registry。

开发者决定直接沿用现有分配器语义，故`Vec` growth保持自然实现，不增加fixed capacity、补偿状态或新errno。
source review确认domain single truth、CAS/domain-lock convergence、guard-out drop/publish、listener publication
闭环、retirement/static-hook顺序以及`FilesState`无flock policy。独立只读change-review subagent复核完整0A diff，
未修改文件、未进入0B，Apollyon/Keter/Euclid/Safe均为0；它同时确认signal分支与已取得旧lease的并发commit
只属于source-review evidence，不能记为KUnit/runtime PASS。

### 2026-07-29 - Checkpoint 0A validation and closure

- `just fmt kernel --check`：已运行，0A文件无formatter diff；命令仍因frozen manifest外三处既有vendored
  smoltcp baseline exit 1，未扩大write set。
- `git diff --check`与`git diff --no-index --check -- /dev/null anemone-kernel/src/fs/flock.rs`：通过；new-file
  命令exit 1且无diagnostic只表示存在diff。
- `just build --preset qemu-virt-rv64-release --bind smp=1 --bind memory=1G`：沙箱内同一lwext4
  `Bad system call` / SIGSYS环境阻断已复现；相同命令在沙箱外通过，证明production与0A KUnit body编译。
- owner-local KUnit/QEMU runtime与userspace flock ABI oracle：Not Run；后者属于未授权0B/0C。
- `mdbook build docs`：通过。

Checkpoint 0A交付、定向source review、validation floor与write-back已关闭。Stage 0保持Active；Checkpoint 0B
仍Not Activated / Unauthorized。Stage 0 contract cutover为None，current contracts与register未修改，全部新ID
继续Not Effective；0A代码只是未cutover纵切的一部分，不能作为partial flock capability宣称或单独进入下一gate。

### 2026-07-29 - Checkpoint 0B authorization and write-set lock

0B从clean `dev/drc/omega@544cd99c`激活。production write set冻结为`anemone-abi`的fs与双架构syscall tables、
`fs/api/{flock.rs,mod.rs}`、`anemone-rs` raw/typed Linux wrappers、`flock-test` app、user-test local runner与两个
pretest rootfs manifests，另只允许canonical Stage 0 transaction/docs write-back subset。scheduler、signal、
filesystem backend、record-lock、Kconfig、current contracts、register与0C runtime inputs保持只读；activation-time
cutover为None。

### 2026-07-29 - Checkpoint 0B implementation and review

`anemone-abi`在RV64/LA64 syscall tables登记asm-generic `SYS_FLOCK = 32`，并在唯一
`fs::linux::flock` module定义四个raw flag。kernel adapter先把完整flag set解析为唯一basic operation与可选
nonblocking modifier，再转换/lookup fd；invalid fd与`O_PATH`返回`EBADF`，其它local `File`不按kind或access
mode筛选。adapter只做Linux boundary与errno translation，VFS core继续独占grant/conflict/liveness truth；
`Interrupted`的关键注释固定conversion不回滚、ordinary replay重新lookup fd且不保存旧identity。

`anemone-rs`增加raw syscall与typed operation wrapper。新`flock-test`复用已有fork、raw thread、signal、fd与
file APIs，以八组stable case覆盖0B矩阵：flags/admission、conflict/idempotence、dup/fork/final close、blocking与
multiple shared waiters、双向non-atomic conversion、`CLONE_FILES` no-grant terminal wait、non-restart/
`SA_RESTART`（含handler关闭并复用fd后的ordinary relookup）和concurrent final-close outcome set。race case只接受普通success/`EBADF`，每轮再由independent owner
确认无retired grant残留；没有test-only lifecycle getter、production control plane、fixed winner或通用test
framework。两个pretest manifests安装该app，user-test local phase在既有epoll-test前调用它。

Change review按architecture、owner、ABI containment、concurrency、resource lifetime与test truth逐项审计；raw
常量只有ABI owner一份，fd/syscall adapter没有mode mirror，filesystem backend与`FilesState`没有新增flock
policy，wrapper不复制常量，focused cases不把notification当success或固定close/signal winner。Apollyon、Keter、
Euclid、Safe均为0。

### 2026-07-29 - Checkpoint 0B validation and closure

- `just fmt flock-test --check`与`just fmt user-test --check`：通过。
- `just fmt kernel --check`：已运行；0B文件无formatter diff，命令仍只因frozen manifest外三处既有vendored
  smoltcp baseline exit 1，未扩大write set。
- RV64/LA64的`flock-test`与`user-test`四次repository app build：通过。首次新app无lockfile构建在沙箱内尝试
  crates.io并因DNS失败；外部egress未获准，随后从相同唯一依赖图的既有app lockfile建立manifest允许的
  `Cargo.lock`，repository build使用本地cache通过并规范化lock顺序。
- RV64 kernel build：沙箱内复现既有lwext4 `Bad system call` / SIGSYS；相同repository command在沙箱外通过。
  LA64 kernel build在沙箱内通过。两者只证明production、syscall registration与KUnit body编译。
- tracked `git diff --check`与五个untracked新文件各自的`git diff --no-index --check`：通过；new-file命令exit 1
  且无diagnostic只表示存在diff。
- focused oracle、KUnit、rootfs composition与guest runtime：Not Run，属于0C，不由双架构build推断。

Checkpoint 0B交付、change review、validation floor与write-back已关闭。Stage 0保持Active；Checkpoint 0C仍
Not Activated / Unauthorized。Stage 0 contract cutover为None，current contracts与register未修改，全部新ID
继续Not Effective；0B纵切尚无runtime evidence，不能作为partial flock capability宣称或自动进入下一gate。

### 2026-07-29 - Checkpoint 0C authorization and write-set lock

0C从clean `dev/drc/omega@7f354ca9`激活。production code write set为None；只允许Stage 0 resolved manifest的
transaction与RFC状态write-back subset。完整Stage 0 source、两条wrapper、两个pretest rootfs manifest、当前
`sys` profile、测试盘master、scheduler/register/current contracts与build/runtime产物均为validation-only输入；
不得修改scheduler、signal、filesystem backend、record-lock、Kconfig、current contracts或register。
activation-time cutover为None，全部新contract ID继续Not Effective。

### 2026-07-29 - Checkpoint 0C runtime and composition evidence

- `./scripts/run-user-test-rv64.sh etc/preliminary/images/sdcard-rv.img build/flock-stage0-rv64.log`：通过。
  wrapper从只读shared master复制worktree-local runtime image，经repository-owned rootfs/build/QEMU路径启动并
  正常PowerOff；master未作为QEMU运行盘。enabled KUnit共264项全部通过，其中opened-description/flock三项均
  为`ok`。focused oracle的`flags-admission`、`conflict-matrix`、`alias-fork-final-close`、
  `blocking-shared-waiters`、`bidirectional-conversion`、`shared-files-terminal-wait`、`signal-restart`与
  `concurrent-final-close`八项全部PASS，summary为`FLOCKTEST:SUMMARY:PASS:8`。
- 当前`sys` profile的四个LTP case均由runner分类为PASS，case级汇总为PASS 4、FAIL 0、TCONF 0、BROK 0；
  子结果原样为glibc `confstr01` TPASS 34 / TCONF 0、`sysconf01` TPASS 45 / TCONF 11，musl
  `confstr01` TPASS 32 / TCONF 0、`sysconf01` TPASS 36 / TCONF 20，全部FAIL/BROK均为0。日志中的
  `FAIL LTP CASE ... : 0`是既有runner打印返回码的标签；同一case summary与group classification均为PASS，
  未把它误记为flock failure。
- `just rootfs mkfs --config conf/rootfs/pretest-la64.toml`在沙箱内及非sudo宿主路径均完成manifest解析、init、
  user-test、epoll-test、flock-test的LA64构建与staging，随后被supermin读取宿主`/boot`权限阻断。按仓库入口
  加`--sudo`重跑后完整通过并生成`build/rootfs/pretest-la64/rootfs.img`。结合0B已通过的LA64 kernel与双app
  build，LA64 app/kernel/rootfs composition证据关闭；LA64 QEMU runtime明确Not Run，未由build推断。
- runtime没有命中register中的scheduler wake race，没有临时trace、test hook、behavior-affecting debug flag或
  production control plane；normal contention、wake与普通errno路径没有新增日志或counter。

### 2026-07-29 - Checkpoint 0C closure audit and full-diff review

完整Stage 0 diff以`c69e2143..7f354ca9`及本次状态write-back为范围。`rg`与逐路径source audit确认：raw
`LOCK_*`只由`anemone-abi::fs::linux::flock`定义，双architecture table只登记各自`SYS_FLOCK = 32`；syscall与
userspace facade消费这些定义但不复制grant truth。每个inode的private
`FlockDomain { SpinLock<Vec<FlockGrant>>, Event }`是holder/mode/conflict唯一真相，grant只保存opaque
opened-description capability与mode；`ProcFile`、`FileDesc`、`FilesState`、filesystem backend均无mode、waiter、
candidate或`has_flock` mirror。

allocation/lock audit确认Stage 0采用自然`Vec` allocation，不增加fixed capacity、Kconfig或补偿状态；这是0A已
记录的实现选择。所有grant mutation与final liveness/conflict recheck都在同一domain guard内；capability drop与
`Event::publish`在guard外，active-wait predicate只短暂取得non-sleepable domain guard并把notification视为recheck
hint，不跨park持锁。duplicate grant与retirement重复命中的impossibility使用常开`assert!`；cleanup先从domain
撤销grant并释放guard，再drop/publish。

全部`release_description_ref` caller audit确认fd-table路径先在private guard内unpublish并收集`Arc<ProcFile>`，再在
guard外exactly once执行`Live(1) -> Retired` handoff；fixed flock cleanup先于既有static
`FileDescOps::final_release`。focused concurrent-final-close只接受success/`EBADF`并由independent owner再次取得
exclusive grant证明最终无retired grant，不固定close/signal winner。

owner/lifecycle/concurrency/resource/ABI full-diff review没有finding，不需要代码修复或重新验证，也未发现新的
Apollyon、Keter、Euclid或停止条件。review确认没有target、owner、public API、shared contract、ABI、visible
semantics、acceptance或resolved manifest变化；production code write set仍为None。成功的真实纵切保留在
transaction branch，不能作为partial flock feature单独合入。

### 2026-07-29 - Checkpoint 0C validation and Stage 0 closure

- complete Stage 0 `git diff --check c69e2143 --`与0C tracked `git diff --check`：通过；`git status --short`没有
  untracked新文件，因此0C没有新增`git diff --no-index --check`对象。0S/0A/0B各自新增文件的no-index检查已在
  owning checkpoint通过。
- `mdbook build docs`：通过。
- RV64 wrapper、enabled KUnit、focused oracle、当前`sys` profile、LA64 app/kernel/rootfs composition、完整diff
  review与write-back全部关闭；LA64 runtime保持Not Run。

Checkpoint 0C与Stage 0成功关闭，实际representation、allocation/lock、runtime、observability、review与代码保留
决定均已记录。Stage 0 contract cutover为None，current contracts与register未修改；
`OPENED-DESC-RETIRE-001`、`FLOCK-DOMAIN-001`、`FLOCK-WAIT-001`、`FLOCK-LIFECYCLE-001`继续Not Effective。
Stage 0代码仍不能作为partial capability单独合入。`0 -> 1 Implementation Resolution Gate`、Stage 1与
`FLOCK-CUTOVER`均未授权、未进入；后续唯一合法动作是等待开发者独立授权该resolution gate。

## Stage 0 -> Stage 1 Implementation Resolution Gate - 2026-07-29

开发者在Stage 0独立关闭后单独授权解析Stage 1，并明确最终测试由开发者本人运行，避免agent代跑两条耗时的
QEMU wrapper。本授权只覆盖只读preflight与canonical docs/status write-back；Stage 1 test edit、runtime、
current-contract write-back与`FLOCK-CUTOVER`均未获执行授权。

preflight以clean `dev/drc/omega@759f9f14`为基线，读取Stage 0实际diff、0S-0C closure evidence、live
flock/opened-description/wait owner、focused oracle、user-test LTP group/profile owner、双pretest manifest与
wrapper、固定LTP `flock01/02/03/04/06` source、register、task opened-description与scheduler wake current
contracts。Stage 0没有production repair、module split或新probe前置；当前八组focused case已覆盖核心
owner/lifecycle/wait/race路径，剩余acceptance gap是hard link、exec/`FD_CLOEXEC`、representative non-regular
local files、advisory I/O、独立flock LTP group与LA64真实runtime。

Stage 1据此解析为两个checkpoint：1A只补`flock-test`矩阵、既有`linkat` syscall的userspace wrapper与user-test `flock`
group，并完成RV64/LA64 app build与source review；1B由agent临时把profile设为`flock`，开发者顺序运行
RV64/LA64 wrapper，agent读取共享log、恢复profile、完成full-diff review与task/VFS current-contract原子交接。
POSIX/OFD record-lock仍为NYI，namespace independence只由source audit证明，不伪造behavioral PASS。

开发者在1B handoff中只需运行canonical Stage 1 Ready定义中的两条wrapper并通知agent；PASS/FAIL/TCONF/BROK、
focused/KUnit summary、PowerOff与外部register归因仍由agent判读。任一wrapper失败或中断时先恢复profile，全部
新ID保持Not Effective，另一架构/build不得替代Not Run。完整交付、acceptance matrix、停止/退出条件、原子
cutover与逐文件manifest见
[Stage 1 Ready](../../rfcs/flock/implementation.md#7-stage-1-readyacceptance-closure-与-flock-cutover)。

本resolution未修改code、current contract或register，未运行build/QEMU/LTP，也未执行`FLOCK-CUTOVER`；R0
target、owner、ABI、visible semantics、Contract Impact与acceptance boundary均不变，没有新增
Apollyon/Keter/Euclid。Stage 1现在是`Ready / Not Active`，必须等待开发者独立授权从Checkpoint 1A进入。

## Checkpoint 1A acceptance assets - 2026-07-29

开发者独立授权1A后，write set保持在resolved manifest的五个test/userspace文件。`flock-test`在Stage 0八组
case基础上增加hard-link inode聚合、exec/`FD_CLOEXEC` terminal cleanup与representative local-file/advisory
I/O三组case，summary扩为11项；child exec mode只服务fixture。`anemone-rs`只增加既有`linkat(2)`的raw/typed
userspace wrapper，没有修改kernel ABI。user-test新`flock` group精确登记`flock01/02/03/04/06`各一次，未修改
`full.txt`、LTP source、disabled-case、timeout或judge；`profile.txt`不进入1A提交。

source/matrix review确认hard link经不同path进入同一inode domain，exec普通fd保留同一opened-description holder，
`O_CLOEXEC`最后alias触发terminal cleanup，directory/pipe按generic local facade admission，ordinary I/O不读取flock
state；既有conversion、no-grant waiter、signal/restart与concurrent-final-close assertions未弱化。RV64/LA64的
`flock-test`与`user-test`四次repository app build、1A文件format/whitespace与group-fixture audit通过。1A没有运行
QEMU/LTP、没有修改current contract/register，closure时四个新ID仍Not Effective；commit为`7396a90f`。

## Checkpoint 1B developer runtime and final review - 2026-07-29

1B临时选择唯一`flock` profile，由开发者在同一worktree顺序运行repository RV64与LA64 wrappers，日志分别为
`build/flock-stage1-rv64.log`与`build/flock-stage1-la64.log`；该证据明确分类为developer-run，不记作agent-run。
两边264项enabled KUnit全部通过，focused oracle均为`FLOCKTEST:SUMMARY:PASS:11`。glibc与musl各运行
`flock01/02/03/04/06`五项，两架构四个group summary均为
`attempted=5 passed=5 failed=0 infra_failed=0 skipped=0`，总计各架构10/10 case通过。日志中的
`FAIL LTP CASE <name> : 0`是runner为judge compatibility打印返回码的既有标签，case summary与group
classification才是判分truth。

RV64完成filesystem、network、device与machine action并正常退出。LA64同样完成KUnit、focused/LTP以及
filesystem/network/device orderly shutdown，进入PowerOff machine action后记录原始症状
`no power off handler succeeded, halting the system`，随后`QEMU: Terminated`。这与已登记
`ANE-20260726-SYSTEM-POWER-ARCH-COVERAGE`一致：LoongArch platform没有普通power-off driver/handler，只能末尾
halt。该症状发生在flock predicates与orderly shutdown前序全部通过之后，明确归因为外部system-power限制；
不宣称LA64 machine power-off支持，也不把它记为flock FAIL或Not Run。

完整`c69e2143..7396a90f` source/diff review再次确认raw `LOCK_*`与双架构syscall number只有ABI owner一份；
inode-associated `FlockDomain`单独拥有grant/mode/conflict truth；operation在同一domain serialization下重验
liveness，guard外drop/publish；notification只作progress hint；`ProcFile`首次`Live(1) -> Retired`在fd-table guard
外exactly-once执行flock cleanup，随后才运行static `final_release`。全部published-ref release caller、hard-link/
exec/generic facade、filesystem backend与`fcntl` record-lock path复核通过，没有mode/waiter mirror、dynamic
registry、backend hook或record-lock interaction。临时probe已删除；normal contention/wake没有production
log/counter。最终Apollyon、Keter、Euclid、Safe均为0，不需要production修复或target renegotiation。

validation-only `profile.txt`已恢复到原`sys`字节内容；master image、register与current limitations未修改。
tracked/new-file whitespace、状态/ID/link扫描与`mdbook build docs`在cutover write-back后完成，结果见下节。

## FLOCK-CUTOVER - 2026-07-29

Stage 1 acceptance matrix、双架构developer-run runtime、profile恢复、final review与docs交接全部闭合后，本checkpoint
原子Introduce / Effective以下四个ID：

- `OPENED-DESC-RETIRE-001`进入task opened-description current contract：`ProcFile`编排terminal episode，fd table
  先unpublish并释放private guard，首次`Live(1) -> Retired` exactly-once进入窄、不可失败的VFS flock handoff；
  handoff删除grant并总是提交recheck notification，随后才运行existing static `final_release`。
- `FLOCK-DOMAIN-001`、`FLOCK-WAIT-001`、`FLOCK-LIFECYCLE-001`一起进入VFS local flock current contract：
  inode domain单独拥有grant/mode/conflict，waiter把notification只作为predicate recheck hint，retired holder不能
  遗留或重获persistent grant。

`OPENED-DESC-001/002/003`、`OPENED-DESC-LIVENESS-001`与`SCHED-WAKE-001..004`保持Preserve；没有把fixed flock
handoff扩成registry，也没有把LA64 power-off缺口写入flock contract。RFC R0、Stage 1与本transaction同步
Closed / Completed，之后没有自动follow-up gate。最终whitespace、new-file与mdBook结果在本closure通过后，
四个ID作为同一原子current-contract unit生效。
