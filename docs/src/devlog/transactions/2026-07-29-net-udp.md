# 2026-07-29 - Network UDP

**Status:** Completed / R0 Stage 0-5 Closed
**Date:** 2026-07-29—2026-07-31
**Owner:** doruche, Codex
**Canonical Plan:** [RFC-20260729-net-udp R0](../../rfcs/net-udp/index.md),
[Stage 5 plan and closure](../../rfcs/net-udp/implementation.md#65-stage-5-closed--externaldual-architecture-closure)
**Canonical Revision:** R0
**Contract Impact:** Stage 0为None。Stage 1 `NET-UDP-DOMAIN-CUTOVER`已同时Refine
`NETDEV-LIFE-001`/`NET-ATTACH-001`并Introduce `NET-IFACE-DOMAIN-001`。`NET-BOUNDARY-001`、`NET-FRAME-OWN-001`、
`NET-FRAME-PROGRESS-001`、`NET-STACK-PUMP-001`、
`OPENED-DESC-001..003`、`IOMUX-POLL-001..003`、`EPOLL-WATCH-001`、`EPOLL-READY-001`、
`EPOLL-FILE-001`、`STM-OWNER-001`与`STM-RESOLVE-001`保持Effective/Preserve。Stage 2
`NET-UDP-CONTROL-CUTOVER`已Refine `STM-TARGET-001`并Introduce Active `NET-CONTROL-PLANE-001`。Stage 5
`NET-UDP-FINAL-CUTOVER`已原子Introduce Active `NET-PROTOCOL-BOUNDARY-001`、
`NET-SOCKET-ENDPOINT-001`、`NET-UDP-TRANSACTION-001`与`NET-SOCKET-WAIT-001`。

## Scope and authorization

本transaction随每次独立授权推进一个checkpoint或resolution gate，并执行对应前置条件、冻结write set、review、
validation、退出条件与write-back；关闭当前gate后不得自动进入下一gate。Stage 0-4、前两项contract cutover、
docs-only `4 -> 5`resolution及Checkpoint 5A均由各自独立授权完成；最终授权明确覆盖Stage 5 source、双架构runtime、
review、validation、`NET-UDP-FINAL-CUTOVER`、write-back与一个`udp: ...`checkpoint commit，随Stage 5 closure耗尽。
本事务不授权任何后续gate。
任何授权都不允许未批准地改变owner、public API、shared contract、ABI/visible semantics、acceptance boundary或
resolved manifest。

## 2026-07-29 - R0 public review and Stage 0 activation preflight

**Entry state:** `dev/drc/alpha@885420c3`，tracked与untracked worktree均clean；没有用户dirty change与0A
source/docs write set重叠。AGENTS/LOCAL、RFC正文/invariants/implementation、register、current contracts、
transaction workflow与live owners已重新读取；RFC没有`tracking-issues.md`，因为当前没有confirmed design blocker；
backgrounds未用于本次判断。

**R0 public review:** target、Contract Impact、correctness/owner/lifecycle/cleanup boundary、Evidence Matrix、
Stage 0 Ready定义与Stage 1-5 Outline按文档成熟度独立review。结论为Apollyon 0、Keter 0、Euclid 0；没有以
future Outline缺少具体type/path/command形成finding。R0接受只建立accepted target，不更新current contract或
授予后续stage权限。

**Live baseline and seam evidence:** production `net::worker`仍对每个external attach执行`Stack::new()`；
`Stack`仍保存per-entry `SocketSet`，`FrameDevice`仍固定`Medium::Ethernet`。vendored UDP `dispatch`从engine TX
queue取得datagram后在当前`Interface` context选择/使用source，`Interface::socket_egress`没有operation-local
selected-interface gate。若naive candidate把同一engine UDP resource交给多个interface poll，错误interface可先
进入source selection/egress；这不是current per-netdev production baseline缺陷。

**Frozen execution boundary:** 0A只允许修改`anemone-smoltcp-stack/Cargo.toml`以启用`socket-udp`并声明
`udp_topology` host target，以及新建`tests/udp_topology.rs`；现有support可读但无需修改。kernel、
`anemone-net-api`、vendored smoltcp、ordinary Stack/pump/adapter source、root build orchestration、SystemTarget/
Platform/rootfs configuration、current contracts、register与其它RFC保持只读。transaction/navigation/RFC状态写回
由public review批准，不构成contract cutover。

**Activation:** R0已接受，transaction与必要导航已经建立；Stage 0只在Checkpoint 0A进入Active。0B/0C、
rootfs、QEMU、LTP、双架构runtime和contract cutover尚未授权或执行。

## 2026-07-29 - Checkpoint 0A implementation, source audit and closure

**Implementation result:** `udp_topology`以两个不同IPv4 prefix的deterministic Ethernet interface、一份明确选择
第二interface/source的`CandidateSelection`和一个真实smoltcp UDP socket建立naive shared-engine fixture。错误的
第一interface先poll后，socket TX queue由2降为0，第一provider观察到唯一Ethernet/IPv4/UDP frame，第二provider
保持无TX；随后再poll选中的第二interface也无法恢复datagram。frame保留被选中的第二source address，但Ethernet
source与next-hop属于第一interface，直接证明engine得到source、却没有得到selected-interface admission capability。

ARP reply injection只准备第一interface的普通gateway neighbor；UDP datagram由`udp::Socket::send_slice`入队并由
smoltcp正常socket egress生成，没有raw socket、UDP packet injection、手工UDP encoder或trace文本断言。该结果
因此满足0A真实Endpoint路径要求，归类为candidate engine egress-admission seam ordering风险；current production
仍在每次netdev attach创建独立Stack，未暴露这份shared UDP resource，所以不是production regression。

**Final source and owner audit:** 最终source write set只有`anemone-smoltcp-stack/Cargo.toml`与新
`tests/udp_topology.rs`。manifest启用vendored smoltcp既有`socket-udp`feature，并以
`required-features = ["host-test"]`隔离新target；`src/lib.rs`与public surface未改变，kernel的
`default-features = false`依赖不会编译或导出test control。fixture直接持有全部SocketSet/socket/queue/provider
observation，只服务host characterization；没有跨crate handle、并列production owner、diagnostic field驱动行为、
ordinary queue/pump/local-link/retire路径或第二binding truth。vendored smoltcp、`anemone-net-api`、kernel、ordinary
Stack/pump/adapter source、current contracts、register与其它RFC均未修改。

0A不实现selected-interface check、local link、Endpoint retire或production admission；这些属于0B及以后，当前不以
characterization PASS外推。预期错误行为测试按0A退出条件保留，并在注释中固定退出条件：0B形成正确路径时必须
删除或改写。review结论为Apollyon 0、Keter 0、Euclid 0；没有Route Correction、Target Renegotiation、write-set
expansion、tracking issue或contract cutover。

**Validation evidence:**

- `cargo test -p anemone-smoltcp-stack --test udp_topology`：PASS，1/1；
- `cargo test -p anemone-net-api -p anemone-smoltcp-stack`：PASS；stack unit 2、`bounded_progress` 7、
  `frame_path` 9、`multi_instance` 2、`udp_topology` 1与net-api compile-fail doctest 2均实际执行；
- `cargo test -p anemone-smoltcp-stack --no-default-features --no-run`与
  `cargo check -p anemone-smoltcp-stack --no-default-features`：PASS；
- `just fmt kernel --check`：最终只报告未触及的vendored smoltcp baseline formatter差异
  `wire/sixlowpan/iphc.rs`两处与`wire/tcp.rs`一处；新增/修改文件没有formatter drift。按Stage 0规则原样记录，
  不把整个命令写成PASS；
- `just build --preset qemu-virt-rv64-release --bind smp=8 --bind memory=1G`：同一命令在sandbox内首先因lwext4 C
  compile触发`Bad system call`/`SIGSYS`；按环境边界在sandbox外重跑后PASS。此项只证明RV64 kernel compile
  integration，不证明runtime或UDP行为；
- `mdbook build docs`：PASS，只有既有规模的search-index warning；
- `git diff --check`以及两个新文件的`git diff --no-index --check -- /dev/null <file>`：无whitespace诊断；后者
  exit 1只表示存在new-file diff。

**Not Run:** rootfs、QEMU、LTP、RV64 runtime、LA64 build/runtime、syscall、VirtIO/external traffic与final harness。
host PASS和RV64 compile PASS不外推这些轨道。

**Checkpoint closure:** 0A交付、停止条件、source audit、review、validation floor与write-back均满足；R0、current
contracts和Stage 0路线保持不变。Checkpoint 0A Closed，但Stage 0未Closed；transaction保持Active并在此停止，
等待0B独立启动授权。

## 2026-07-29 - Checkpoint 0B activation preflight

**Authorization and entry state:** 用户把完成Checkpoint 0B设为本轮唯一GOAL，明确不得自动进入下一gate，并要求
单checkpoint使用`frame-udp: ...`commit。进入时为`dev/drc/alpha@5fdde098`，tracked/untracked worktree clean；
AGENTS/LOCAL、R0正文/invariants/implementation、register、current contracts、transaction与live source重新读取。
RFC仍无`tracking-issues.md`，current register无替代本probe或允许绕过frame contracts的active network issue。

**Baseline and manifest audit:** production `net::worker`仍per-external-attach创建`Stack`；Stack仍保存per-entry
`SocketSet`，adapter仍固定Ethernet，vendored UDP仍在socket dequeue后按当前Interface context执行egress。0A
expected-wrong test稳定存在，Stage 0 hypothesis、stop conditions和Resolved Write Set Manifest未漂移。0B冻结写集
为stack crate的`Cargo.toml`、`src/{lib,stack,pump,adapter,udp,local_link}.rs`与`tests/{support/mod,udp_topology}.rs`；
实际无需修改`adapter.rs`或`tests/support/mod.rs`。执行文档只写回本RFC、transaction、RFC索引与当前biweekly
devlog。kernel、`anemone-net-api`、vendored smoltcp、current contracts、register、build/config与其它RFC保持只读。

**Activation:** 独立授权只激活0B；0C、Stage 0 closure、`0 -> 1`resolution、rootfs/QEMU/LTP/runtime与contract
cutover均未授权。

## 2026-07-29 - Checkpoint 0B implementation, review, validation and closure

**Implementation result:** Stack-private `UdpEndpoints`是provisional Endpoint identity/binding/admission/receive-order/
retire唯一owner。一个Endpoint为两个external interface与一个local interface分别持有private smoltcp UDP engine
resource，但engine binding只投影aggregate port，不参与conflict；Endpoint TX phase在owner queue与selected engine
之间显式移交datagram。operation-local `InterfaceId + source IPv4`在commit前验证mapping、configured source、
destination、MTU与bounded Endpoint credit；wrong-interface-first没有可消费resource，selected interface才经真实
smoltcp UDP egress形成Ethernet frame。stale/unknown interface、missing selection、unsupported source、oversize与
TX full均返回typed stack-local outcome；accepted path没有进入vendored no-source drop。

Stack-private `LocalPort`报告`Medium::Ip`，不实现或修改shared `FrameProvider`。UDP egress先进入固定packet capacity
的local-link storage，只有后续finite pump round才从同一storage进入同一Stack的normal IPv4/UDP ingress；没有
Endpoint injection、Socket-to-Socket copy、fake encoder/decoder或unbounded queue。external provider blocked时
其它Endpoint/local port仍可推进；local capacity恢复后pending engine datagram由有限后续round重试。local packet
携带的Endpoint owner只用于aggregate retire cleanup，不参与destination lookup或delivery；retire先withdraw
aggregate identity，再删除全部engine handle与pending local association，stale identity不能继续send/retire。

**Source and surface audit:** ordinary Endpoint、selection admission、local device/link与local pump均保持
`no_std + alloc`；`host-test` facade只传递opaque validation Endpoint ID、protocol-domain selection/outcome、payload
与test-only observation。`SocketHandle`、`SocketSet`、smoltcp `Interface`、buffer/queue identity和raw packet bytes
没有从public surface导出；kernel依赖继续`default-features = false`，不编译或导出host control。所有TX phase、
engine enqueue/dequeue、local transfer、receive drain与retire mutation仍在独占`&mut Stack`pump boundary；finite
budgets、frame tests、multi-instance isolation与deadline路径未退化。0A expected-wrong PASS已改写为positive
capability matrix。最终source没有vendored/shared/kernel写、per-interface logical binding/lifecycle truth、direct
delivery、busy loop或write-set expansion。

**Change review:** change review结论为Apollyon 0、Keter 0、Euclid 0。owner-local binding与TX phase没有可写派生
mirror；`blocked_receive`是防止aggregate queue full时跨interface overtaking的显式协议状态，不是diagnostic；
local packet owner tag只服务withdraw-before-cleanup。conditional dead-code allowance明确以0C保留/删除决定为退出
条件。剩余不确定性是kernel global Stack consumer、production control plane与runtime wiring，属于Stage 1+ target
范围，不以0B host evidence外推。

**Validation evidence:**

- `cargo test -p anemone-smoltcp-stack --test udp_topology`：PASS，2/2；覆盖wrong-interface-first、双prefix同
  Endpoint、typed pre-commit failures、bounded local normal ingress、full/recovery、external blocked isolation与
  aggregate retire/stale identity；
- `cargo test -p anemone-net-api -p anemone-smoltcp-stack`：PASS；stack unit 2、`bounded_progress` 7、
  `frame_path` 9、`multi_instance` 2、`udp_topology` 2与net-api compile-fail doctest 2均实际执行；
- `cargo test -p anemone-smoltcp-stack --no-default-features --no-run`与
  `cargo check -p anemone-smoltcp-stack --no-default-features`：PASS；ordinary candidate保持`no_std + alloc`，host
  target/control由feature metadata隔离；
- `just fmt kernel --check`：新增/修改source和test无formatter diff；命令仍只报告未触及vendored smoltcp的
  `wire/sixlowpan/iphc.rs`两处与`wire/tcp.rs`一处既有baseline，未把整条命令记为PASS；
- `just build --preset qemu-virt-rv64-release --bind smp=8 --bind memory=1G`：sandbox内首先在lwext4 C compile触发
  `Bad system call`/`SIGSYS`，同一repository command在sandbox外重跑后PASS；只证明RV64 kernel compile
  integration；
- `mdbook build docs`、`git diff --check`与新文件逐个`git diff --no-index --check -- /dev/null <file>`：PASS；
  new-file whitespace命令的exit 1只表示存在diff，无whitespace诊断。

**Not Run:** rootfs、QEMU、LTP、RV64 runtime、LA64 build/runtime、syscall、VirtIO/external traffic与final harness。
host/compile evidence不外推这些轨道。

**Checkpoint closure and write-back:** 0B交付、stop-condition audit、change review、checkpoint validation与write-back
满足positive result；没有Route Correction、Target Renegotiation、tracking issue、register更新或contract cutover。
Checkpoint 0B Closed，但Stage 0未Closed；0C仍须独立选择candidate/facade/test的保留或cleanup，并在final source上
执行7.13完整branch/common gate。transaction保持Active并在此停止，不进入0C。

## 2026-07-29 - Checkpoint 0B post-closure engineering feedback and correction activation

**Authorization and boundary:** 开发者在0B关闭后的软件工程审查基础上，明确要求形成反馈、修复问题并由subagent
做独立review。该授权激活[0B Feedback Correction](../../rfcs/net-udp/implementation.md#741-checkpoint-0b-feedback-correction--closed)，
不授权0C、Stage 0 closure、`0 -> 1`resolution、runtime或contract cutover。进入时为
`dev/drc/alpha@3381adb0`，tracked/untracked worktree clean；R0与current contracts保持不变。

**Confirmed feedback:** review确认两个Keter和一个Euclid。第一，`UdpEndpoints::blocked_receive`只保存
`InterfaceId`，一个Endpoint aggregate queue饱和会阻止无关Endpoint ingress；最小host probe在Endpoint A保持一项
received datagram时连续pump 100轮，Endpoint B仍为0。第二，TX admission只检查interface MTU；以local MTU 128、
engine payload capacity 32发送64-byte payload时send先成功，pump随后在`owner admission must reserve enough private
engine capacity` assertion panic。第三，`stack.rs`同时承载ordinary owner orchestration与全部conditional host
DTO/facade/conversion，形成可局部拆分的test/production责任混合。既有`udp_topology` 2/2仍PASS，说明原matrix没有
覆盖这两个Keter；它不能反证feedback。

**Correction route and write set:** 这些问题保持R0 target和owner，按Route Correction处理：receive阻塞收回
Endpoint-local语义，TX capacity进入同一pre-commit predicate，host validation拆到同crate同owner的独立module。
冻结source/test写集为`src/{lib,pump,udp}.rs`、`src/stack/{mod,host_validation}.rs`与`tests/udp_topology.rs`；执行文档写回本RFC、
transaction、RFC/transaction索引与biweekly devlog。`local_link.rs`、Cargo、kernel、shared/vendored crate、current
contracts、register与build/config保持只读。最终source必须由独立subagent review，Apollyon/Keter未清零时不得重新
关闭0B或进入0C。

## 2026-07-29 - Checkpoint 0B feedback correction implementation, review, validation and closure

**Implementation result:** `UdpEndpoints::blocked_receive`、interface-wide admission gate及其retire cleanup已删除。
`drain_ingress`在aggregate queue饱和时把该Endpoint最早的engine datagram留在原engine；它不建立第二receive truth，
也不阻止同interface其它Endpoint继续normal ingress。quiescent full Endpoint不会报告software work或Immediate
recheck。TX admission在commit前以`min(interface MTU payload ceiling, Endpoint engine payload capacity)`形成唯一
maximum，capacity failure继续返回既有`Oversize { maximum }`，不会先报告send success再在pump中panic。

**Module and surface result:** ordinary Stack mapping/orchestration从`src/stack.rs`行为保持地迁入
`src/stack/mod.rs`；conditional DTO、fixture control、observation与conversion集中到同owner child module
`src/stack/host_validation.rs`。ordinary create/send/retire恢复module-private；`interface_mut`只在`cfg(test)`或
`host-test`下编译，因此no-default production不携带test bridge，unit test仍可使用。conditional facade没有导出
smoltcp handle、SocketSet、buffer/queue identity或production control authority；kernel dependency继续
`default-features = false`。实际source/test diff保持frozen write set，没有修改Cargo、kernel、`anemone-net-api`、
vendored smoltcp、register或current contracts。

**Regression result:** `udp_topology`新增两个focused case：小engine/大MTU在pre-commit拒绝且不留下pending TX，
capacity边界仍可delivery；Endpoint A aggregate receive queue饱和并保留engine datagram时，Endpoint B仍可推进，
quiescent pump为`work_remaining = false`与`Recheck::Idle`，credit恢复后A的旧datagram按序进入aggregate queue。
原有selection、bounded local ingress/recovery与aggregate retire/stale identity case继续通过。

**Independent review:** 最终source由独立subagent按软件工程审查等级复审，结论为Apollyon 0、Keter 0、Euclid 0。
review确认receive容量、pending datagram与overflow均保持Endpoint-local；TX capacity进入同一pre-commit owner
predicate；`stack/mod.rs`与`host_validation.rs`没有扩大ordinary/public/shared surface。未发现blocking finding。

**Validation evidence:** `cargo test -p anemone-smoltcp-stack --test udp_topology`为4/4 PASS；
`cargo test -p anemone-net-api -p anemone-smoltcp-stack`全部PASS；no-default `test --no-run`与`cargo check` PASS。
`just fmt kernel --check`只报告三处未触及的vendored smoltcp baseline，没有本次formatter diff。
`just build --preset qemu-virt-rv64-release --bind smp=8 --bind memory=1G`在沙箱内先由lwext4 C compile触发
`Bad system call`/`SIGSYS`，同一命令在沙箱外以最终源码PASS。`mdbook build docs`、`git diff --check`与两个新文件
逐个new-file whitespace check通过；后者exit 1只表示存在diff，没有whitespace诊断。

**Not Run and closure:** rootfs、QEMU、LTP、RV64 runtime、LA64 build/runtime、syscall、VirtIO/external traffic与
final harness仍为Not Run。correction保持R0 target、owner、ABI、shared contract和acceptance boundary；没有Target
Renegotiation、tracking issue、register更新或contract cutover。0B重新Closed，transaction因Stage 0未Closed继续
Active；0C仍须独立授权，本轮在此停止。

## 2026-07-29 - Checkpoint 0C positive decision closure, review and validation

**Authorization and entry state:** 用户把完成Checkpoint 0C设为本轮唯一GOAL，要求一个`frame-udp: ...`commit、
不得自动进入下一gate，并要求最终subagent review。进入时为`dev/drc/alpha@ddc7dda0`，tracked/untracked worktree
clean；AGENTS/LOCAL、R0正文/invariants/implementation、register与其open-issues/current-limitations、current network
contract及本transaction已重新读取。RFC仍无`tracking-issues.md`，current register没有替代本probe target或允许
绕过frame contracts的active network issue。

**Activation preflight:** production `net::worker`仍按external attach创建`Stack`；Stack仍以per-entry `SocketSet`
承接current frame path，kernel dependency仍`default-features = false`。vendored UDP `dispatch`仍在dequeue后选择
source，但0B candidate为accepted datagram显式设置operation-local source，并只把它交给selected private engine。
0B的4项positive matrix稳定通过，Stage 0 hypothesis、stop conditions与Resolved Write Set Manifest未漂移；用户
授权因此只激活0C decision closure/cleanup。`0 -> 1`resolution、Stage 1 activation、runtime与contract cutover
均未授权。

**Positive decision and code disposition:** 0C保留private ordinary aggregate Endpoint、显式selection admission、
bounded IP-medium local link、finite pump路径和5项长期deterministic topology tests。它们共同证明一个Stack owner
可聚合per-interface private engine resources而不复制binding/admission/receive/retire truth；wrong-interface-first
无法consume，selected engine取得显式source，local datagram经protocol egress、bounded handoff与下一finite round
normal ingress交付。该路线不需要修改vendored smoltcp、`anemone-net-api`、kernel或current contracts。

integration target需要conditional facade，因此本stage保留的UDP-specific surface只交换opaque validation
Endpoint ID、protocol-domain selection/outcome、payload和窄owner observation。它没有导出`SocketHandle`、
`SocketSet`或private buffer/queue identity；kernel dependency不编译该surface。existing `queue_ipv4` fixture
仍由frame-path长期integration tests真实使用，是既有test-only packet injection，不是UDP packet dump或production
入口。0A expected-wrong test已经在0B改写；没有遗留unrestricted inspection或不再使用的UDP host method。
ordinary candidate在Stage 1前没有kernel
consumer，故保留temporary dead-code allowance，并把退出条件明确改为Stage 1 production owner接管，或Stage 1
选择替代路线后删除candidate；不再以0C作为悬空退出点。

**Final source audit:** `UdpEndpoints`唯一保存Endpoint ID、binding、TX phase、aggregate receive queue与retire
publication；per-interface smoltcp handles只在crate-private engine mapping中使用。`queue_send`在commit前验证
selection、source、destination、MTU、engine payload capacity与single pending credit；`prepare_egress`只向selected
engine enqueue并显式提供local source，因此accepted path不会进入vendored no-source drop。external/local
enqueue、dequeue、provider/link transfer及normal ingress每步只有一个owner；local storage有限且每round受
`PumpBudget`约束。retire先withdraw aggregate identity，再移除engine handles与owner-tagged local packet；stale ID
fail closed。没有direct Endpoint injection、Socket-to-Socket copy、unbounded queue、parallel pump、shared/kernel
handle leak或diagnostic field驱动behavior。frame-path三个长期integration target继续实际执行并通过。

**Review finding and correction:** 首轮独立review确认一个Keter：当interface IP MTU小于IPv4+UDP固定header的
28 bytes时，`checked_sub(...).unwrap_or(0)`会把zero-length payload误判为`len <= maximum(0)`并commit；local
device随后为实际28-byte packet触发MTU assertion，形成send success后pump panic。修复保持owner、typed outcome与
R0 target不变：header本身放不下时，admission在commit前直接返回`Oversize { maximum: 0 }`。新增MTU 27 +
zero-length focused regression同时证明typed rejection与Endpoint没有pending TX；修复位于既有`udp.rs`/test
manifest内，不构成write-set expansion或Target Renegotiation。

**Stage 1 route input:** 后续只读`0 -> 1`gate应优先评估如何把已证明的aggregate private-engine candidate接入
initial-domain唯一global Stack access window及异构provider，而不是预设vendored/shared seam变化。local-link
candidate只作为Stage 2 production loopback resolution的证据输入。0C没有解析Stack access、worker/attach、
logical-interface owner或Stage 1 manifest，也没有使Stage 1达到Ready/Active。

**Validation evidence:** `cargo test -p anemone-smoltcp-stack --test udp_topology`为5/5 PASS；
`cargo test -p anemone-net-api -p anemone-smoltcp-stack`全部PASS，stack unit 2、`bounded_progress` 7、
`frame_path` 9、`multi_instance` 2、`udp_topology` 5与net-api compile-fail doctest 2均实际执行。no-default
`cargo test ... --no-run`与`cargo check`均PASS。`just fmt kernel --check`只报告三处未触及的vendored smoltcp
baseline formatter diff，没有本checkpoint formatter drift。repository-owned RV64 release build在sandbox内由lwext4
C compile触发`Bad system call`/`SIGSYS`，同一命令在sandbox外PASS；它只证明compile integration。

`mdbook build docs`、最终whitespace gate与独立review在本条后续closure段记录。

**Contract and unrun boundary:** Stage 0 Contract Impact为None；current contracts保持Effective，全部R0 candidate
继续Pending / Not Effective。没有Route Correction、Target Renegotiation、tracking issue、register更新或write-set
expansion。rootfs、QEMU、LTP、RV64 runtime、LA64 build/runtime、syscall、VirtIO/external traffic与final harness均
Not Run；host与RV64 compile evidence不外推这些轨道。

**Closure evidence:** Keter修复后的final source重新完成5/5 topology、完整stack/net-api host、no-default test/check、
formatter与沙箱外RV64 release compile gate；`mdbook build docs`与`git diff --check`通过。独立review明确批准
`docs/src/rfcs.md`只同步Stage 0状态摘要，不改变链接、导航结构、target或contract，满足manifest中的docs-review
例外；其它public navigation保持不变。最终review结论与checkpoint closure在下段补记。

**Final independent review and checkpoint closure:** Keter修复后的final source与write-back由同一subagent重新
复审，结论为Apollyon 0、Keter 0、Euclid 0、Safe 0。原MTU/header finding已由pre-commit typed rejection与
focused regression neutralize；review确认Endpoint/admission/retire仍只有一个owner，private engine projection、
selected-source egress、bounded local handoff、conditional facade/object fence和退出条件均未退化。review还确认
最终source/test/docs保持7.11 write set，current contracts未修改，Stage 1仍为Outline且`0 -> 1`resolution未执行。

Checkpoint 0C的decision closure、probe cleanup/source disposition、Stage 1 route input、source audit、review、
positive/common validation floor、Not Run边界与write-back均满足；Stage 0独立Closed。transaction因R0后续Stage仍
未完成而保持Active，并在此停止。不得由本closure自动解析`0 -> 1`gate、把Stage 1提升为Ready或进入Stage 1。

## 2026-07-29 - Stage 0 -> Stage 1 Implementation Resolution Gate

**Authorization and entry:** 开发者在Stage 0独立关闭后明确要求解析Stage 1；该授权只覆盖read-only preflight、
canonical Ready定义、resolved manifest与状态/transaction/navigation写回，不授权Stage 1 source、current-contract
cutover或Stage 2 resolution。入口为`dev/drc/alpha@1a37c6cb`，tracked/untracked worktree clean。AGENTS/LOCAL、RFC
R0正文/invariants/implementation、Stage 0 commits与transaction证据、register、current Network contracts、live
build/test入口和相关source owner均重新读取；RFC仍无active `tracking-issues.md`，register没有替代本target或允许
绕过frame contracts的network issue/limitation。

**Stage 0 evidence and live-source preflight:** Stage 0 final source继续以一个Stack-private aggregate Endpoint owner
聚合per-interface engine resource，explicit selection在commit前阻止wrong-interface dequeue，IP-medium local-link
candidate保持bounded/normal-ingress；5项`udp_topology`、full stack/net-api host、no-default、RV64 compile与最终
review证据均有exact closure记录。live production仍由`net::worker::prepare()`为每个published external netdev执行
`Stack::new()`并把`Stack + provider + InterfaceId`放入worker-local `PumpCore`；`Stack`本身已经用`Vec<InterfaceEntry>`
和generic `pump<P: FrameProvider>(InterfaceId, &mut P, ...)`支持一个instance内多个heterogeneous provider mapping。
`device/net::Registry`当前仍分配`ifindex/name`并把它们放入`NetdevSnapshot`；kernel attach authority拥有active
publication/shutdown gate，per-provider wake/deadline/retention已经闭合。`Stack::{add_interface,remove_interface,pump}`、
current generic worker/provider边界与Stage 0 candidate足以完成Stage 1，不需要修改shared/vendored seam。

**Resolved route and alternatives:** Stage 1保留每个external provider一个generic worker和owner-local provider
truth，但worker core不再持raw `Stack`；它只持`ExternalPumpPort`，每个finite round经initial-domain唯一
`DomainStack` access window串行调用同一`Stack`，repoll round间释放lock。kernel `InitialDomain`同时组合唯一
`LogicalInterfaces` owner与唯一`DomainStack`，但不合并两者私有truth；`device/net`退回NetdevId/origin/facts/pending
capability owner。一个global worker拥有全部provider会要求新的heterogeneous erasure/registry并移动provider
lifecycle，直接共享raw `Arc<SpinLock<Stack>>`会给worker/future consumer过宽authority；两者均无真实consumer收益。
Stage 0 positive result也排除了为vendored smoltcp增加selection seam的必要性。production local link、control plane和
Endpoint/socket operation分别保持Stage 2/3边界。

**Attach/identity/cutover result:** authoritative
[Stage 1 Ready](../../rfcs/net-udp/implementation.md#61-stage-1-closed--initial-domain--global-stack-walking-skeleton)
已经冻结单一Checkpoint 1A：boot无条件建立`lo` logical membership；external logical reservation在Stack mapping、
worker/wake/time准备后与active path一次发布；ordinary spawn failure撤销mapping/reservation并交回同一provider；
publish前terminal shutdown撤销未发布domain state后只retain provider。device/logical/protocol identity保持三域隔离，
old per-netdev Stack path在cutover时删除。`NET-UDP-DOMAIN-CUTOVER`未来必须原子Refine `NETDEV-LIFE-001`/
`NET-ATTACH-001`并Introduce `NET-IFACE-DOMAIN-001`；本resolution没有改写current contract，三个delta现在仍分别为
Effective baseline / Pending target。

**Manifest, review and validation boundary:** canonical plan已逐文件冻结kernel net/domain/worker、device registry、
VirtIO publication diagnostic、stack candidate comment/new multi-interface host target、current-contract closure docs与
validation-only inputs；明确禁止`anemone-net-api`、vendored smoltcp、pump/UDP/local-link semantics、VFS/syscall/
control-plane/SystemTarget/build orchestration写入。未来Stage 1 floor包括existing + new one-Stack/two-provider host
matrix、no-default、repository-owned format/RV64 build、fresh-disk RV64 KUnit/active attach/shutdown、identity/lock/
rollback/source audit、final change review和atomic docs cutover。Stage 1不恢复已删除的production ICMP probe；host
real-stack ICMP不外推为RV64 configured traffic。LA64、SMP>1、functional loopback、UDP syscall、LTP、hardware与
final harness继续Not Run。

**Document-gate result:** 本resolution保持R0 target、owner、ABI、visible semantics、Contract Impact与acceptance
boundary，因此不增加RFC修订，不创建tracking issue，不更新register/current contracts。只修改canonical RFC/
transaction/navigation/devlog文档；关闭前执行whitespace、mdBook、heading/link、manifest-file-existence与final diff
检查，不运行kernel build、host test、KUnit、QEMU、LTP或architecture runtime。文档/source audit不能冒充未来
Stage 1 code/runtime proof。

**Result:** Stage 1为**Ready / Not Active**，Stage 2-5保持Outline。下一步只能在新的明确授权下进入Stage 1，先在
本transaction记录actual activation point并重新核对frozen manifest；本轮不得修改source、执行
`NET-UDP-DOMAIN-CUTOVER`、解析Stage 2或把任何Pending contract写成Effective。

## 2026-07-29 - Stage 1 Checkpoint 1A activation preflight

**Authorization and entry:** 开发者把完成Stage 1设为本轮唯一GOAL，要求逐checkpoint提交`udp: ...`commit、
不得自动进入下一gate，并要求最终subagent review。本授权激活单一Checkpoint 1A及其
`NET-UDP-DOMAIN-CUTOVER`，不授权`1 -> 2`resolution或Stage 2 implementation。入口为
`dev/drc/alpha@bce0102c`，tracked/untracked worktree clean。AGENTS/LOCAL、R0正文与invariants、canonical Stage 1
Ready、register/open issues/current limitations、current Network contracts及本transaction均已重读；RFC仍无
`tracking-issues.md`。

**Preflight result:** HEAD相对Stage 0 closure `1a37c6cb`只包含docs-only Stage 1 resolution；Stage 0 source与五项
topology matrix未漂移。live production仍由每个external worker分别构造并拥有`Stack`，`device/net`仍拥有
ifindex/name；现有`Stack::{add_interface, remove_interface, pump}`、generic provider与worker finite-round边界足以
按Ready路线完成迁移。未发现必须修改`anemone-net-api`、vendored smoltcp、provider public API、SystemTarget、
control plane、local link、socket/Endpoint UAPI或其它manifest外owner的证据；R0 target、Contract Impact、ABI、
visible semantics、acceptance与Stage 1 frozen manifest均未漂移。Checkpoint 1A因此进入**Active**。

## 2026-07-29 - Stage 1 Checkpoint 1A implementation, validation and domain cutover

**Implementation route:** `device/net` registry删除ifindex/name与`NameTooLong` failure，只继续拥有boot `NetdevId`、
origin、normalized facts、record与pending provider capability；VirtIO publication diagnostic也只表达netdev identity和
facts。新`net/domain.rs`让`LogicalInterfaces`唯一拥有logical membership、identity、ifindex/name/kind，boot无条件
提交logical ID 0 / ifindex 1 / `lo` / `Loopback`；external reservation从ifindex 2和`eth0`开始单调消费identity，
abort不发布也不复用。opaque `NetdevId` association只服务诊断，不参与logical/protocol identity决策。

`InitialDomain`同时组合但不混淆logical owner与唯一`DomainStack`。production raw `Stack`只存在于后者的private
lock内，`Stack::new()`只在其构造发生一次。每个external worker继续拥有concrete provider/wake/deadline/lifetime，
但只持`ExternalPumpPort { Arc<DomainStack>, InterfaceId }`；每个finite `Stack::pump`调用取得一次Stack window，
repoll round之间释放。Stage 0 aggregate Endpoint candidate随global Stack取得production owner但operation仍dormant到
Stage 3；IP-medium local-link仍仅为Stage 2 resolution input。没有修改`anemone-net-api`、vendored smoltcp、pump/
UDP/local-link semantics或kernel host-facade dependency。

**Attach, rollback and lifetime audit:** missing MAC在reservation/mapping前返回同一published capability。普通attach
顺序是authority下reserve logical identity、锁外建立Stack mapping/wake/inactive worker、authority下commit logical
member与active record后activate。spawn failure由worker prepare先撤销mapping，attach owner再abort reservation并把
同一provider交回pending owner；publish前terminal race先锁外撤销mapping，再在authority下abort reservation，最后
stop inactive worker并retain provider。unfinished `ExternalMapping::drop`先fail-close撤销未发布mapping，再暴露
protocol bug；reservation本身没有published resource且identity按no-reuse contract保持已消费。

active worker不取得attach authority/device registry lock，provider callback只在finite Stack window内运行且不得
sleep/re-enter；shutdown只在authority lock内关闭admission和snapshot controls，锁外request stop，不取得Stack lock
或join。`ActivePath`的device/logical snapshots都明确仅为diagnostic/shutdown log projection，不能驱动provider或Stack
admission。source audit未发现raw Stack泄露、device/logical/protocol identity转换、old per-netdev Stack、parallel
mapping truth、lock inversion、unbounded repoll或terminal provider lifetime退化。

**Host and production-feature validation:** `cargo test -p anemone-net-api -p anemone-smoltcp-stack` PASS：stack unit 2、
`bounded_progress` 7、`frame_path` 9、`multi_instance` 2、新`multi_interface` 2、`udp_topology` 5与net-api compile-fail
doctest 2均实际执行。新matrix以一个Stack/两个provider证明双interface ICMP/frame progression、一个provider blocked
不阻塞另一个、wrong-provider不消费另一provider resource，以及rollback后剩余interface继续、removed ID fail closed、
replacement `InterfaceId`单调不复用。`cargo test -p anemone-smoltcp-stack --no-default-features --no-run`与
`cargo check -p anemone-smoltcp-stack --no-default-features`均PASS，证明host target未进入kernel dependency。

**Format, build and runtime evidence:** `just fmt kernel --check`对全部Stage 1 authored file clean；整条命令仍只报告
三处未触及的vendored smoltcp baseline（`wire/sixlowpan/iphc.rs`两处、`wire/tcp.rs`一处），故不把整条命令记为
PASS，也不越界修改vendored source。`just build --preset qemu-virt-rv64-release --bind smp=1 --bind memory=1G`在
sandbox内先命中已知lwext4 `Bad system call`/SIGSYS；同一命令在sandbox外对修正后的source PASS。

fresh-disk `./scripts/run-user-test-rv64.sh etc/preliminary/images/sdcard-rv.img
build/net-udp-stage1-rv64.log`从4G caller-selected master重建worktree-local runtime disk，未修改`profile.txt`或master，
最终QEMU exit 0。真实VirtIO publication和external attach之后实际运行263/263 KUnit，其中domain reservation/no-reuse/
failure-isolation与registry identity/facts KUnit明确PASS；现有`sys` profile额外完成epoll 11/11、glibc/musl attempted 4 /
passed 4。shutdown保持strict `filesystem -> network -> device -> PowerOff`。

该runtime run曾把VirtIO publication、initial-domain/global-Stack init与active attach三项一次性记录临时直接输出到
console，以核对一次性init和identity关联；开发者随后要求最终源码恢复普通`kinfoln!`，并明确本次不修改defconfig
print level。因此runtime run不作为最终INFO console可见性的exact-code proof；该日志等级恢复不改变owner、attach、
KUnit、worker或shutdown行为，最终源码由host/no-default/format/build重新验证。transaction不把INFO可见性记为PASS。

**Contract cutover — `NET-UDP-DOMAIN-CUTOVER`:** 同一Checkpoint 1A原子Refine current `NETDEV-LIFE-001`为
device-only publication identity/facts/capability owner，Refine `NET-ATTACH-001`为logical reservation + global-Stack
mapping + narrow pump-port attach transaction，并Introduce Active `NET-IFACE-DOMAIN-001`作为initial-domain logical
membership/identity与global-Stack composition contract。`NET-STACK-PUMP-001`只更新live enforcement，语义保持
Preserve。old device-owned ifindex/name和per-worker Stack source已同时删除；code、tests、contracts、RFC、transaction
与navigation在本checkpoint一起切换，不存在transitional双truth。

`NET-CONTROL-PLANE-001`、`NET-PROTOCOL-BOUNDARY-001`、`NET-SOCKET-ENDPOINT-001`、
`NET-UDP-TRANSACTION-001`、`NET-SOCKET-WAIT-001`与`STM-TARGET-001` Refine继续Pending。logical `lo`不等于
functional loopback；Stage 1没有使IP address/route、production software link、Endpoint operation、socket UAPI或
Linux readiness生效。

**Claim and stage boundary:** LA64 build/runtime、SMP > 1、functional production loopback、UDP syscall/UAPI、
configured external IP traffic、virtio-pci、hardware、full network LTP与final harness均Not Run。host ICMP不外推为
production configured traffic；current `sys` profile结果不外推为network LTP/UDP proof。没有Target Renegotiation、
tracking issue、register/limitation更新或write-set expansion。Stage 1在完成最终change review、docs/whitespace
gate和单一checkpoint commit后Closed；Stage 2保持Outline，`1 -> 2`resolution未执行。

**Final independent review and closure:** 最终source/test/current-contract/RFC/transaction aggregate diff由唯一一位
subagent按Anemone工程审查等级只读复审，结论为Apollyon 0、Keter 0、Euclid 0、Safe 0。review确认logical/netdev/
protocol identity各有单一真相源，attach publication-last与mapping -> reservation rollback成立，raw Stack只位于
`DomainStack`且worker只持fixed provider/port，finite pump与round间解锁没有authority/Stack反序，ordinary/terminal
provider retention没有提前Drop或join。conditional host target仍由`host-test`隔离；三项contract在aggregate diff中
原子切换，frozen manifest无越界，Stage 2仍Outline且`1 -> 2`gate未解析或激活。

review同时确认最终三处lifecycle记录均为`kinfoln!`，transaction没有把临时direct-console run宣称为最终INFO
print-level exact-code proof。最终`git diff --check`、三个新文件逐项new-file whitespace check与`mdbook build docs`
通过；mdBook只有既有large search-index warning。`just fmt kernel --check`仅报告三处既有vendored baseline，全部
authored file clean。Checkpoint 1A、`NET-UDP-DOMAIN-CUTOVER`与Stage 1在单一`udp: ...`checkpoint中Closed；
transaction因R0后续Stage仍未完成而保持Active，并在`1 -> 2 Implementation Resolution Gate`前停止。

## 2026-07-29 - Stage 1 -> Stage 2 Implementation Resolution Gate

**Authorization and entry:** 开发者在Stage 1独立关闭后明确要求解析Stage 2，并特别要求检查module/file切分。该授权
只覆盖read-only preflight、canonical Ready定义、resolved manifest与状态/transaction/navigation写回，不授权
Checkpoint 2A/2B source、SystemTarget/KConfig、generated defs、current-contract cutover或Stage 3 resolution。入口为
`dev/drc/alpha@5cc0669c`，tracked/untracked worktree clean。AGENTS/LOCAL、RFC R0正文/invariants/implementation、
Stage 0-1 commits和transaction证据、register/open issues/current limitations、current Network/SystemTarget contracts、
live build/test入口、selected RV64/LA64 config与相关source owner均重新读取；RFC仍无active `tracking-issues.md`。

**Stage 1 evidence and live-source preflight:** Stage 1 final source继续只有一个`DomainStack`/raw Stack owner，
per-provider worker只持fixed `ExternalPumpPort`，logical/netdev/protocol identity、attach rollback、shutdown admission和
provider lifetime边界与current contracts一致。Stage 0 retained candidate仍以IP-medium `LocalLink`、finite
`pump_local`、aggregate Endpoint和selected-source admission通过5项topology matrix，但production global Stack只保存
dormant local slot，logical `lo`尚无protocol mapping/address/route/worker。现有SystemTarget/resolver没有network
section；kernel build统一生成ignored typed defs，kernel不解析manifest。ordinary QEMU RV64/LA64 target都使用user
network backend，pretest preset分别选择对应target。

**Module-boundary result:** periodic engineering preflight确认无需改变semantic owner，但在增加Stage 2职责前应先做
same-owner split。`net/domain.rs`同时承载logical membership、Stack mapping transaction与KUnit；`net/worker.rs`
同时承载wake/deadline/stop与provider progression；stack root和`pump.rs`同时承载external、dormant local与UDP
orchestration；`tasks/build/mod.rs`同时承载build orchestration和全部generated-def rendering。authoritative
[Stage 2 Ready](../../rfcs/net-udp/implementation.md#62-stage-2-closed--static-control-plane与production-loopback)因此
冻结Checkpoint 2A为behavior-preserving split-only，再由2B增加新职责。该切分不建立新facade/public surface/owner，
2A关闭也不自动授权2B。

**Resolved implementation route:** Stage 2不需要新probe或vendored smoltcp change。SystemTarget使用optional
single-external `[network.ipv4]` typed input，ordinary QEMU RV64/LA64 target显式声明`eth0 10.0.2.15/24`与
`10.0.2.2` gateway；loopback保持implicit，physical/final target不被自动配置。xtask从resolved SystemTarget生成
独立private `network_defs.rs`，Platform/KConfig/Preset/rootfs不复制deployment truth。kernel `Ipv4ControlPlane`唯一
拥有local/connected/default route与source/interface selection；Stack只保存IPv4/default-route/AnyIP projection。
Stage 0 local-link candidate由DomainStack创建一次，local worker复用existing bounded pump/control；新增KConfig只拥有
独立packet capacity与IP MTU。conditional validation surface增加no-std KUnit route，ordinary kernel build不携带它。

**Cutover and evidence boundary:** Checkpoint 2A Contract Impact为None。2B final closure才允许
`NET-UDP-CONTROL-CUTOVER`原子Refine `STM-TARGET-001`并Introduce `NET-CONTROL-PLANE-001`；其它SystemTarget/frame/
domain/attach IDs Preserve，Socket/Endpoint/UDP transaction/wait IDs继续Pending。未来2B floor包括config/xtask、host/
no-default、owner KUnit、双架构release build、fresh-disk RV64 production local-worker KUnit、source bypass audit、final
review和atomic docs cutover。LA64 runtime、remote external traffic、UDP syscall/VFS/iomux、SMP>1、hardware、network
LTP和final harness继续Not Run，不能由build或conditional endpoint proof外推。

final resolution review修正了candidate invariant原先把真实remote external path列为Stage 2 cutover前置的证据分层
冲突：2B只以deterministic multi-interface selection、self-external local routing和RV64 production
control-plane/local-worker/normal-ingress证明`NET-CONTROL-PLANE-001`；真实remote external UDP仍是R0 Evidence
Matrix与Stage 5 final acceptance的强制项，由后续`NET-UDP-TRANSACTION-001`/external-path closure证明。该调整不
降低R0 target、owner、ABI或acceptance boundary，也不把Not Run写成PASS。

**Document-gate result:** resolution保持R0 target、owner、ABI/visible semantics、Contract Impact分类与acceptance
boundary，因此不增加RFC修订，不创建tracking issue，不更新register/current contracts。只修改canonical RFC/
transaction/navigation/devlog文档。final exact docs上`git diff --check`无诊断，`mdbook build docs`通过且只报告既有
large search-index warning；新增相对链接及两个generated HTML anchor可解析，2A/2B所有live manifest input存在且
planned new path无collision。未运行xtask tests、host tests、kernel build、KUnit、QEMU、LTP或architecture
runtime；文档/source audit不作为Stage 2 implementation proof。proof partition、owner-internal visibility与manifest
修正后的final engineering review为Apollyon 0、Keter 0、Euclid 0、Safe 0。

**Result:** Stage 2为**Ready / Not Active**，Stage 3-5保持Outline。下一步只能在新的明确授权下激活Checkpoint 2A，
先在本transaction记录actual activation point并重新核对frozen manifest；本轮不得修改source/config、执行2B或
`NET-UDP-CONTROL-CUTOVER`、把logical `lo`写成当前functional loopback、解析Stage 3或把Pending contract写成
Effective。

## 2026-07-29 - Stage 2 Checkpoint 2A activation and same-owner split

**Authorization and activation:** 开发者明确授权完成Stage 2 Checkpoint 2A，并要求一个checkpoint一个`udp: ...`
commit、最终由subagent review且不得自动进入2B。activation point为`dev/drc/alpha@3804ec0e`，tracked/untracked
worktree clean。AGENTS/LOCAL、net-udp R0正文/invariants/implementation、register、current transaction、current
Network/SystemTarget contracts与live source均已重读；RFC没有`tracking-issues.md`，register没有改变2A路线的
active issue。

**Frozen-manifest preflight:** live `domain.rs`、`worker.rs`、stack root/`pump.rs`与xtask build root仍分别混合
Stage 2 Ready中识别的same-owner proof surface；planned child path均不存在且2A manifest未漂移。现有caller只经
`net::domain`、`net::worker`、stack/pump既有facade与build owner访问；raw Stack、logical identity、provider
progression和generated-def rendering owner均未变化。Checkpoint 2A因此Active，只执行behavior-preserving move、
module wiring、owner-internal visibility/import/re-export与机械test relocation。Contract Impact为None；Cargo/config、
generated text、current contract、`local_link.rs`、`udp.rs`、kernel `net/mod.rs`和2B surface保持只读。

**Implementation and focused evidence before stop:** frozen source manifest内的五组same-owner split已经形成working
diff：domain按composition/interfaces/stack，worker按bounded root/control/external，Stack按root/interfaces/
udp_ops，pump按root/common/external/local，xtask build按orchestration/generated-def rendering分离。consumer
callsite、production exported type/method集合、generated text、test数量和owner逻辑保持不变；external pump跨child
module改用既有`PumpBudget` accessor，是字段直读的机械等价调整。没有触碰2B surface或current contract。

`cargo test -p anemone-net-api -p anemone-smoltcp-stack` PASS：stack unit 2、`bounded_progress` 7、`frame_path` 9、
`multi_instance` 2、`multi_interface` 2、`udp_topology` 5与net-api compile-fail doctest 2均通过。
`cargo test -p anemone-smoltcp-stack --no-default-features --no-run`与
`cargo check -p anemone-smoltcp-stack --no-default-features`均PASS。`just fmt kernel --check`确认全部2A authored
file clean，但整条命令仍只报告Stage 1已记录的三处vendored smoltcp baseline，故不把整条命令写成PASS。
tracked `git diff --check`与13个untracked new file逐项`/dev/null` whitespace check无诊断。

**Independent review and mandatory validation stop:** 唯一一位subagent对final working source做只读change review，
source结论为Apollyon 0、Keter 0、Euclid 0、Safe 0；确认全部source/docs位于2A manifest，domain/worker child
visibility只机械恢复原`net` owner scope，Stack/pump public surface、rollback/Drop、lock/worker/stop/deadline/provider
retention与generated-def bytes不变，kernel `net/mod.rs` caller未改。checkpoint closure另有一个Keter validation
blocker：`just xtask-test`执行55项，仅54 PASS；
`config::resolve::tests::resolved_selection_owns_all_snapshot_inputs`仍要求把`max_logical_cpus = 16`替换为`99`并断言
resolved值为16，但HEAD与working tree的tracked `conf/.defconfig`均为`max_logical_cpus = 1`。`resolve.rs`、
`.defconfig`均无本checkpoint diff，因此这是HEAD stale expectation，不是`generated_defs` split regression。

修复该mandatory gate的最小tracked write是`scripts/xtask/src/config/resolve.rs`，不在2A frozen manifest。按stop/
expansion contract，当前不得越界修改、不得把baseline failure降格为PASS、不得运行后续RV64/LA64 build gate来绕过
它，也不得声明2A Closed或提交checkpoint。proposed expansion的Contract Impact为None，不改变owner/public API/
shared contract/ABI/visible semantics/acceptance；若获得批准，应先把该文件加入2A manifest，只修正test fixture的
canonical expected value，再运行`just xtask-test`、全部原2A validation、final review和docs/status closure。
RV64/LA64 compile在本次stop后保持Not Run；QEMU/LTP按2A定义本就不运行。2B继续Not Active，
`NET-UDP-CONTROL-CUTOVER`仍未执行。

**Approved manifest expansion and resume:** 开发者随后明确批准上述最小write-set expansion。canonical
`implementation.md`已先把`scripts/xtask/src/config/resolve.rs`加入2A tracked manifest，并将允许改动冻结为
`resolved_selection_owns_all_snapshot_inputs`中两处`max_logical_cpus` fixture/expected snapshot对齐；不允许修改
resolver behavior、配置owner、schema、public API或其它test。Checkpoint 2A据此恢复Active。该approval保持
Contract Impact=None，不改变R0 target、owner、ABI/visible semantics或acceptance；修正后必须重新执行全部2A
validation和final independent review，失败时再次停止。

**Validation after approved expansion:** `resolve.rs`只把该test中的两个`max_logical_cpus`期待值从16对齐为
canonical值1；resolver behavior、配置owner/schema、public API与其它test未变。`just xtask-test`随后55/55 PASS。
完整host gate再次PASS：stack unit 2、`bounded_progress` 7、`frame_path` 9、`multi_instance` 2、
`multi_interface` 2、`udp_topology` 5与net-api compile-fail doctest 2；两条no-default compile/check也再次PASS。

双架构compile integration使用repository-owned命令与同一`--bind smp=1 --bind memory=1G`输入：RV64首次在
sandbox内命中已知lwext4 `Bad system call`/`SIGSYS`，相同命令在sandbox外PASS；LA64命令在sandbox内PASS。最终
generated kconfig/platform/boot defs保持既有typed shape，没有出现2B network projection或生成文本改动。两项build
只证明split后的compile integration，不外推runtime。

`just fmt kernel --check`确认全部2A authored file formatter-clean；整条命令仍只报告三个未触及的vendored smoltcp
baseline diff，因此精确记录为known baseline而非PASS。`git diff --check`、13个new file逐项`/dev/null` whitespace
check、diff summary、callsite/visibility/source/manifest audit均通过。production `Stack::new()`仍只有`DomainStack`
owner，raw Stack、logical identity、provider progression、generated-def bytes与原consumer surface没有第二入口。
`mdbook build docs` PASS，只有既有large search-index warning。

**Review closure and owner waiver:** manifest扩展前的独立source review结论为Apollyon 0、Keter 0、Euclid 0、
Safe 0；随后只增加获批的两处test fixture修正、domain facade机械re-export和closure write-back。开发者在查看
最终收口状态后明确指示“不需要再review了，现在可以直接收口”，因此取消原计划的额外final exact-diff
subagent review，并以该owner waiver替代它；不把先前source review误写成对后续exact diff的复审。

**Checkpoint closure:** Checkpoint 2A以单一`udp: ...`commit独立Closed，Contract Impact为None；全部Stage 2
candidate contract保持Pending，current contracts没有修改。QEMU runtime、Stage 2 KUnit/runtime、LTP、functional
loopback、control plane与UDP syscall均`Not Run`，未以host或compile结果外推。Stage 2本身尚未关闭；Checkpoint 2B
保持Ready / Not Active，下一步只能由新的明确授权激活。本checkpoint没有执行`NET-UDP-CONTROL-CUTOVER`、解析
Stage 3或自动进入任何后续gate。

## 2026-07-29 - Stage 2 Checkpoint 2B activation

**Authorization and activation:** 开发者明确授权完成Stage 2 Checkpoint 2B，并要求一个checkpoint一个`udp: ...`
commit、最终由subagent review且不得自动进入下一gate。activation point为`dev/drc/alpha@94b2377f`，tracked/
untracked worktree clean。AGENTS/LOCAL、net-udp R0正文/invariants/implementation、register、current transaction、
current Network/SystemTarget contracts与live source均已重读；RFC仍无`tracking-issues.md`，register没有改变2B路线的
active issue。

**Frozen-manifest preflight:** Checkpoint 2B的全部existing tracked input仍位于6.2.7冻结manifest，计划新增的
`net/domain/control_plane.rs`、`net/worker/local.rs`、`net/kunit.rs`、`anemone-net-api/src/ipv4.rs`与
`contracts/net/control-plane.md`均尚不存在。production仍只有`DomainStack`构造raw Stack；external worker只持
`ExternalPumpPort`，logical `lo`仍没有protocol mapping、address、route或local worker；SystemTarget仍无network
section，build只生成kconfig/platform/boot defs。Stage 0 local-link/UDP candidate保持private dormant，vendored
smoltcp与manifest外owner无需修改。Checkpoint 2B据此Active；current contracts在final source/config/runtime/
review evidence全部满足前保持原Effective/Pending边界。

**Generated-output manifest stop:** implementation开始后的exact ignore audit发现6.2.7把
`anemone-kernel/src/network_defs.rs`冻结为repository build可覆盖的ignored generated output，但live
`anemone-kernel/.gitignore`只列出`platform_defs.rs`、`kconfig_defs.rs`与`boot_defs.rs`，`git check-ignore -v
--no-index anemone-kernel/src/network_defs.rs`没有匹配。`anemone-kernel/.gitignore`不在2B tracked manifest；若不
增加该规则，normal build会留下untracked generated source，无法满足generated provenance/clean gate。实现期间曾
把preset/tuple network snapshot test加入2A历史上获批过、但本2B未授权的`config/resolve.rs`；该working change已
完整撤回，2A批准没有被继承。

Checkpoint 2B现按write-set停止合同暂停。请求的最小扩展只有`anemone-kernel/.gitignore`，且只增加
`src/network_defs.rs`一行；Contract Impact为None，不改变owner、public API、shared contract、ABI、visible
semantics或acceptance。批准后应先把该文件加入6.2.7 authoritative manifest并记录approval，再写ignore rule；随后
用`git check-ignore -v`证明四份generated kernel input都命中明确规则，并继续执行完整2B xtask/host/no-default/
双架构build/RV64 runtime/review/docs floor。批准前不得生成或提交`network_defs.rs`，不得继续2B source实现或
执行`NET-UDP-CONTROL-CUTOVER`。

**Approved manifest expansion and resume:** 开发者明确批准上述最小扩展。canonical `implementation.md`已先把
`anemone-kernel/.gitignore`加入2B tracked manifest，并将允许改动冻结为新增`src/network_defs.rs`一行；不允许
改写其它ignore scope、generated owner或source policy。Checkpoint 2B据此恢复Active。该approval保持
Contract Impact不变，不改变R0 target、owner、public API、ABI/visible semantics或acceptance；扩展后仍须执行
全部2B validation与final independent review。

**Implementation route:** SystemTarget serde/schema现在拥有optional single-external `[network.ipv4]`，ordinary
QEMU RV64/LA64 target显式声明`eth0 10.0.2.15/24`与`10.0.2.2` gateway；section缺失保持loopback-only且没有
fallback。resolver snapshot由build materializer生成private ignored `network_defs.rs`，Platform、rootfs、Preset和
KernelConfig不复制deployment truth；clean/ignore与exact-generation tests覆盖该第四份generated kernel input。
`anemone-net-api`只增加checked IPv4 address/CIDR value，vendored smoltcp保持无diff。

InitialDomain在唯一DomainStack内一次建立`127.0.0.1/8` IP-medium local mapping并prepare sleeping local worker。
boot attach drain完成后，`Ipv4ControlPlane`按configured-local -> `127/8` -> connected -> default顺序一次性选择
source/interface，验证SystemTarget interface恰好匹配committed logical member，安装external CIDR/default route与
self-external local `/32` projection，发布control plane后才activate local worker。Stack只执行明确projection和
selection；external/local worker仍只持固定pump port与wake/control capability。shutdown先withdraw publication，再
在authority锁外请求local/external stop；boot-lifetime Stack/link/provider继续retain。

stack crate增加独立no-std `kunit` feature，kernel现有`kunit`显式转发；ordinary dependency仍
`default-features = false`。conditional bridge只允许kernel KUnit创建/发送/接收/retire opaque Endpoint，并带Stage 3
真实consumer出现后的删除条件。三项KUnit通过production control-plane selection、DomainStack、sleeping local worker、
protocol egress、bounded software link和normal ingress证明`127.0.0.1`、另一`127/8`与self-external address；没有
direct injection或socket-to-socket fast path。

**Runtime failure and authorized Route Correction:** 第一次final RV64 wrapper实际启动后，首项
`udp_loopback_worker_127_0_0_1` panic为“bounded local worker did not deliver KUnit datagram”。host topology原先以
`PumpBudget(1, 1)`运行，未复现production `32/32`预算。read-only failure indexing定位到`pump_local`：protocol
egress round结束后`LocalLink::transfer()`才把packet从egress发布为later normal ingress；因为本轮egress只处理一项、
未耗尽32项预算，旧outcome返回Idle，sleeping worker可能在没有其它wake owner时遗留该packet。

该failure触发6.2.9 local finite-progression stop；partial code按开发者要求保留。开发者随后明确批准应用已定位的
local pump progression修正并恢复2B。修正只在既有2B manifest内让non-empty transfer计入
`egress_may_remain`，因此请求一个后续有限round；owner、normal-ingress语义、budget和acceptance不变。host regression
改用production `PumpBudget(32, 32)`，断言第一round为Immediate、第二round完成normal ingress并转为Idle，直接证明
不会以busy-poll补偿。该Route Correction没有扩大public/shared API或改变R0 target/contract classification。

**Validation on final source/config:** `just xtask-test`最终60/60 PASS，覆盖network absent/present、invalid/unknown
field、address/prefix/gateway/interface、preset/tuple同一resolved target、exact generated shape与clean清单。
`cargo test -p anemone-net-api -p anemone-smoltcp-stack` PASS：net-api unit 2、stack unit 2、
`bounded_progress` 7、`frame_path` 9、`multi_instance` 2、`multi_interface` 2、`udp_topology` 6和net-api doctest 2；
production-budget focused regression单独PASS。`cargo test -p anemone-smoltcp-stack --no-default-features --no-run`、
`cargo check -p anemone-smoltcp-stack --no-default-features`与`--features kunit`均PASS，ordinary/conditional Cargo
feature audit无host facade泄露。

最终generated output分别由selected QEMU SystemTarget重新生成；LA64
`just build --preset qemu-virt-la64-release --bind smp=1 --bind memory=1G` PASS，RV64由最终fresh-disk wrapper内同源
build PASS。两份resolution摘要均精确显示`network=eth0=10.0.2.15/24 gateway=10.0.2.2`，生成文件命中独立ignore
rule且未提交为canonical source。

最终`./scripts/run-user-test-rv64.sh etc/preliminary/images/sdcard-rv.img build/net-udp-stage2-rv64.log`重新从
caller-selected master生成worktree-local disk，QEMU exit 0。270/270 KUnit全部PASS；三项production local-path、
control-plane publication/table/missing-interface与late-wake KUnit均明确执行。现有whitelist同时完成glibc/musl
`confstr01`/`sysconf01` attempted 4 / passed 4 / failed 0；这只是regression observation，不外推为network LTP。
shutdown日志保持strict `filesystem -> network(local then eth0 stop) -> device -> PowerOff`。

source audit确认production `Stack::new()`仍只有`DomainStack`一个调用点，local mapping只创建一次，route/source
decision只在`Ipv4ControlPlane`，kernel没有smoltcp handle/type，Platform/rootfs/Preset没有network copy；logical、
protocol、provider和control state各有唯一owner。四份generated kernel input均由`.gitignore`明确覆盖，manifest
tracked path无越界；获批`.gitignore`扩展只有一行。`just fmt kernel --check`只保留三处未触及的vendored smoltcp
baseline，全部2B authored/generated file clean。最终whitespace、mdBook与independent review结果记录在下方closure。

**Contract cutover and claim boundary:** 同一2B checkpoint原子Refine current `STM-TARGET-001`，使SystemTarget拥有
optional first-version static IPv4 deployment，并Introduce Active `NET-CONTROL-PLANE-001`，使initial-domain
control plane唯一拥有local/connected/default route、source/interface selection、stable association/wake projection与
bounded local-handoff protocol。`STM-OWNER-001`、`STM-RESOLVE-001`和existing frame/netdev/domain/attach IDs
Preserve。`NET-PROTOCOL-BOUNDARY-001`、`NET-SOCKET-ENDPOINT-001`、`NET-UDP-TRANSACTION-001`与
`NET-SOCKET-WAIT-001`继续Pending；conditional KUnit没有使Socket/UAPI/readiness提前生效。2026-07-30
post-close correction保持`NET-BOUNDARY-001` Preserve，只恢复既有artifact-neutral validation boundary，不改变
Stage 2 runtime closure或其它contract状态。

LA64 runtime、SMP > 1、remote external UDP/frame traffic、UDP syscall/UAPI、VFS/iomux、virtio-pci functional
traffic、hardware、full network LTP与final harness均Not Run。LA64 build不外推runtime，self-external local proof不
替代remote external provider proof。Stage 2只有在下方final review/docs gate与单一checkpoint commit关闭后才Closed；
本transaction继续Active，`2 -> 3`resolution与Stage 3均未授权或执行。

**Final independent review and closure:** 唯一一位subagent对final aggregate source/config/current-contract/RFC/
transaction diff做只读exact-diff review。初次结论为Apollyon 0、Keter 0、Euclid 1、Safe 1：Euclid指出
interface-domain current contract仍把external logical facts写成只服务attach/诊断，但final control plane已经用
logical name匹配SystemTarget并保留boot-lifetime association；Safe指出host validation facade仍带已经过期的
Stage 1删除注释。两项分别更正为boot-time match/association但不拥有address/route，以及长期host-only facade不得
成为production capability；没有改变代码行为、owner、public/shared API或acceptance。第一次增量复核确认两项已
Neutralized，但又发现页首`Contract Impact`仍把已经完成的Stage 2 cutover写成计划状态；该Euclid已更正为
`STM-TARGET-001` Refine与`NET-CONTROL-PLANE-001` Active，并同步更新transaction当前授权边界。最终增量复核
确认全部finding已Neutralized且没有新问题，结论为Apollyon 0、Keter 0、Euclid 0、Safe 0。

review同时确认frozen manifest无越界，`.gitignore`只有获批单行；SystemTarget -> resolved generated input ->
private kernel control plane provenance单一；logical/protocol/provider/control truth、publication/rollback、shutdown
锁外stop与late wake成立；non-empty transfer请求later bounded round且第二round回到Idle；conditional KUnit feature/
object fence与Stage 3删除条件正确。validation和Not Run边界没有外推，Stage 3仍是未激活Outline。

最终`git diff --check`与五个新文件逐项`/dev/null` whitespace check无诊断，`mdbook build docs` PASS且只有既有
large search-index warning。`just fmt kernel --check`仅报告三处未触及vendored smoltcp baseline，全部2B authored
与generated file clean。Checkpoint 2B、`NET-UDP-CONTROL-CUTOVER`与Stage 2在单一`udp: ...`checkpoint中Closed；
transaction因R0后续Stage仍未完成而保持Active，并在`2 -> 3 Implementation Resolution Gate`前停止。

## 2026-07-30 - Stage 2 post-close validation-boundary correction

**Authorization / feedback:** 开发者把Stage 2关闭后的software-engineering review视为正式反馈并授权修正、验证和
单一commit收口，但未授权`2 -> 3`resolution或Stage 3。review确认此前kernel把`kunit`传播为stack crate同名
feature、并让lower crate的type/method理解KUnit harness，重复了net-frame-path `NFP-005`已经Neutralized的
boundary问题；独立`net/kunit.rs`也没有表达被测semantic owner。该问题不改变R0 target、runtime behavior、ABI、
acceptance或Stage 2既有proof，因此不增加RFC revision、不重新打开Stage 2，作为独立post-close correction处理。

**Implementation / lifecycle:** kernel `kunit`现只转发stack的artifact-neutral `udp-validation-probe`；
`anemone-smoltcp-stack`把conditional DTO、错误映射与create/send/receive/retire集中在`stack::udp_probe`，ordinary
`udp_ops.rs`不再含validation `cfg`或conditional method，`anemone-net-api`保持无validation feature。kernel删除
`net/kunit.rs`并把三项production local-path测试共置到最低共同composition owner `net/mod.rs::kunits`；
`LogicalInterfaceSnapshot`与`DomainStack`的conditional support集中在各自owner文件底部。长期
`host_validation.rs`仍只服务真实host-test consumer且不进入kernel dependency；临时probe明确在Stage 3 real
Socket-to-Endpoint capability出现时删除或由真实consumer替代。repo `AGENTS.md`同步记录该module-layout规范。

**Validation:** `cargo test -p anemone-net-api -p anemone-smoltcp-stack`通过net-api unit 2、stack unit 2、
`bounded_progress` 7、`frame_path` 9、`multi_instance` 2、`multi_interface` 2、`udp_topology` 6与net-api doctest 2；
stack `--no-default-features --no-run`、no-default check与`udp-validation-probe` check均PASS，`just xtask-test`
60/60 PASS。LA64与RV64 release build均PASS；RV64 build首次在sandbox内由既有`lwext4` `SIGSYS`失败，相同命令
在sandbox外通过。

最终fresh-disk `run-user-test-rv64.sh`从caller-selected master建立worktree-local disk并正常退出：270/270 KUnit
全部PASS，迁移后的`net::kunits`三项local-path测试明确执行，现有LTP whitelist attempted 4 / passed 4 / failed 0，
shutdown保持strict `filesystem -> network -> device -> PowerOff`。source/dependency audit确认两个外部crate没有
KUnit vocabulary，ordinary stack owner source没有validation `cfg`，host facade未进入kernel dependency，probe
保留Stage 3删除条件。authored formatter、whitespace与mdBook gate通过；formatter整命令只保留三处未触及
vendored smoltcp baseline。

**Review / contract / stop:** final exact-diff review确认KUnit semantics只由kernel harness owner承担，stack probe
以能力命名且没有并列production truth，测试与最低共同semantic owner共置，长期host facade和临时probe lifecycle
边界明确；Apollyon/Keter/Euclid/Safe均为0。此前Stage 2 closure中“stack crate独立`kunit` feature”和“conditional
KUnit object fence正确”的记录仅是当时历史事实，已由本条correction取代。`NET-BOUNDARY-001`保持Preserve；
R0 revision、Stage 2 runtime closure和其它contract状态不变。LA64 runtime、SMP>1、remote external traffic、UDP
syscall/LTP、hardware与final harness继续Not Run。本correction **Closed**；transaction保持Active，并继续停在
Stage 3 Outline，未运行`2 -> 3`resolution。

## 2026-07-30 - Stage 2 -> Stage 3 Implementation Resolution Gate

**Authorization and entry:** 开发者在Stage 2与post-close boundary correction独立关闭后明确要求解析Stage 3，
重点要求在这个较大stage开始前完成module/file boundary设计，并允许`anemone-abi`、`anemone-rs`进入write set；
随后接受3A/3B/3C总体路线并授权canonical文档写回。该授权只覆盖read-only preflight、Stage 3 Ready定义、
resolved manifest与transaction/navigation同步，不激活Checkpoint 3A或任何source/build/runtime/current-contract gate。
入口为`dev/drc/alpha@2e083891771c`，tracked/untracked worktree clean；AGENTS/LOCAL、R0正文/invariants/
implementation、transaction、register/current limitations、current contracts、live protocol/net/File/syscall/config/
ABI/user-test owner与Linux 6.6.32 public xref均已复核。

**Module and capability resolution:** existing `udp.rs`混合Endpoint、namespace、engine projection、TX/RX ownership，
`net/domain/stack.rs`则即将同时承载pump与Endpoint access window。Stage 3因此先以3A完成same-owner split：protocol
UDP拆为`endpoint/namespace/datagram`，Stack operation与DomainStack UDP window分别进入owner child module，保持
behavior/public surface/conditional probe不变。3B再建立unbound Endpoint、domain-wide bind/ephemeral namespace、
kernel-private `UdpEndpointPort`、`File::prv`中的`UdpSocketFile`与`socket/bind/getsockname`真实lifecycle/address
纵切；3C增加implicit bind、selection/admission、owned receive transaction与`sendto/recvfrom` nonblocking纵切。
三个checkpoint必须独立activation/closure，3A/3B不得自动进入下一项。

真实fence选择“private raw Stack instance + kernel-private role facade”，不建立`ProtocolStack`/`EndpointOps`或future
TCP trait hierarchy。`anemone-net-api`只承载真实stack/kernel consumer共享的protocol-domain value/outcome；Socket
只取得`Arc<DomainStack> + opaque non-reused Endpoint ID`，不取得smoltcp handle、queue或route truth。namespace直接
扫描bounded Endpoint committed binding，不新增并列`BindingTable`。Stage 3 real consumer在3B出现后删除
`udp-validation-probe` feature/module/DomainStack support及kernel Cargo的probe forwarding edge；kernel `kunit`
feature本身继续服务owner-local KUnit。长期host facade只保留仍有真实host-test consumer的部分。

**Lifecycle, ABI and resource resolution:** `UdpSocketFile`使用association lock与operation mutex；creation guard在
fd publication前拥有rollback，commit后只有`ProcFile` semantic final release撤销association并发起non-blocking
retire。dup/fork共享opened description，`SOCK_CLOEXEC`保持fd-local，`SOCK_NONBLOCK`保持description-local。
receive在Stack lock内detach后释放所有Stack/source private lock，再按payload -> peer sockaddr -> actual addrlen
顺序copyout，任一fault消费datagram且不requeue。sockaddr固定16-byte IPv4 layout、unaligned byte copy、input
`16..=128`与Linux output truncation/writeback order；五项asm-generic syscall和精确socket errno进入
`anemone-abi`/kernel/`anemone-rs`，新`udp-test`同时作为typed/raw API consumer。

Stage 3只执行immediate operation：effective nonblocking would-block返回`EAGAIN`；default-blocking若必须sleep则
通过带notice与Stage 4删除条件的temporary `EOPNOTSUPP`bridge停止，不busy-poll或伪装成nonblocking。KernelConfig
新增Endpoint/TX/RX/payload capacity与ephemeral first/last参数，由`conf/.defconfig`、xtask materialization/
validation和repository-generated `kconfig_defs.rs`共同拥有；不把重要policy写成source literal。

**Contract, validation and result:** Stage 3 Contract Impact解析为None。existing effective IDs Preserve；
`NET-PROTOCOL-BOUNDARY-001`、`NET-SOCKET-ENDPOINT-001`、`NET-UDP-TRANSACTION-001`与
`NET-SOCKET-WAIT-001`继续Pending，因为本stage尚不证明delayed-retire/race、wait/readiness、fragment、remote
external与双架构runtime floor。Resolved manifest覆盖config、net-api/stack、kernel net/socket/syscall、
`anemone-abi`、`anemone-rs`、`udp-test`与pretest rootfs/user-test，但明确保持current contracts、register、vendored
smoltcp、VFS/opened-description core、iomux/epoll和net provider/worker只读。resolution review同时原地修正
`invariants.md`中一组仍把已完成Stage 2 `STM-TARGET-001` Refine写成future cutover的stale current-fact；该修正只
对齐既有effective contract，不改变R0 target、revision、Contract Impact或Stage 3 source manifest。

本resolution的`git diff --check` PASS；`mdbook build docs` PASS，只有既有large search-index warning，生成页与两处
Stage 3 Ready链接使用同一有效anchor。Stage 3代码、Kconfig generation、host/KUnit/app/kernel build、rootfs、QEMU、
LTP、LA64 runtime、hardware与final harness均Not Run。最终结果为Stage 3 **Ready / Not Active**；下一步只能由新的
明确授权激活Checkpoint 3A，不得自动进入3B/3C、`3 -> 4`resolution或contract cutover。

## 2026-07-30 - Stage 3 Checkpoint 3A activation preflight

**Authorization and entry:** 开发者明确授权只完成Stage 3 Checkpoint 3A，要求一个`udp: ...`commit、最终
subagent review并在closure后停止，不得自动进入3B/3C。activation point为`dev/drc/alpha@cfa5ec37`，tracked/
untracked worktree clean；AGENTS/LOCAL、R0正文/invariants/implementation、register/open issues/current
limitations、current Network/Opened-description/IOMUX/Epoll/SystemTarget contracts、本transaction与live source
均已重读。RFC没有`tracking-issues.md`，register没有改变3A路线的active issue。

**Frozen route and boundary:** live `udp.rs`、`stack/udp_ops.rs`与kernel `net/domain/stack.rs`仍与Ready baseline
一致，现有caller只经crate-private protocol surface、private Stack composition或KUnit-only probe window访问它们。
3A因此激活，只执行behavior-preserving same-owner item movement：protocol UDP按endpoint/namespace/datagram角色
目录化，Stack UDP operation改名进入owner child module，DomainStack的既有probe access window进入owner child
module。ordinary visibility、conditional probe/host facade、public symbol、capacity和call path保持不变；不增加真实
Endpoint consumer、shared API、ABI/Kconfig/syscall，不删除probe，不修改current contract/register/invariants。
若拆分要求新增behavior API、越出3A最小source/write-back集合或命中6.3.10/全局停止条件，立即停止并上报。

## 2026-07-30 - Stage 3 Checkpoint 3A implementation and validation candidate

**Implementation and owner boundary:** protocol `udp.rs`按既有item职责目录化为
`udp/{mod.rs,endpoint.rs,namespace.rs,datagram.rs}`；Endpoint resource/engine projection、bounded namespace与
datagram ownership phase分别进入同一owner的child module。原flat-module private access只在parent `udp` scope
恢复为`pub(super)`，旧`crate::udp::*` crate-private路径由窄re-export保持；没有新增crate-public symbol、type、
method、capacity或behavior。`stack/udp_ops.rs`逐字改名为`stack/udp.rs`。kernel `net/domain/stack.rs`目录化为
`stack/{mod.rs,udp.rs}`，只有既有KUnit-only probe access window移入`udp.rs`；DomainStack composition、pump、
private raw Stack lock和所有caller保持原路径。temporary `udp-validation-probe`、host facade与kernel forwarding
edge原样保留到3B real consumer删除gate。

**Source and write-set audit:** item/body对照只显示Rust sibling-module所需的parent-scope visibility与module wiring；
Stack UDP operation的old/new file `cmp`相同。kernel仍只由`net/mod.rs` KUnit调用四个probe method，没有real Endpoint
consumer、Socket/File/syscall、shared API、ABI或Kconfig变化。source write set只有Ready 3A子集：stack crate
`src/{udp.rs,udp/**,stack/{mod.rs,udp_ops.rs,udp.rs}}`与kernel
`src/net/domain/{stack.rs,stack/{mod.rs,udp.rs}}`；execution write-back只使用本transaction与后续获准的RFC/navigation/
biweekly surfaces。current contracts、`invariants.md`、register、vendored smoltcp、provider/worker、VFS/iomux/epoll
和tests全部只读；未发生write-set expansion、Route Correction或Target Renegotiation。

**Validation evidence:** final source上`cargo test -p anemone-net-api -p anemone-smoltcp-stack` PASS：net-api 2，
stack unit 2、bounded-progress 7、frame-path 9、multi-instance 2、multi-interface 2、UDP topology 6，以及net-api
compile-fail doctest 2全部通过。`cargo test -p anemone-smoltcp-stack --no-default-features --no-run`、
`cargo check -p anemone-smoltcp-stack --no-default-features`和精确
`cargo check -p anemone-smoltcp-stack --no-default-features --features udp-validation-probe`均PASS；
`just xtask-test`为60/60 PASS，`just fmt kernel --check`与`git diff --check` PASS。RV64 release build首先在sandbox
内的unchanged `lwext4` C compile命中`Bad system call`/SIGSYS；获得开发者明确授权后，同一
`just build --preset qemu-virt-rv64-release --bind smp=1 --bind memory=1G`在sandbox外PASS，故只把首个失败归类为
环境限制。随后`just build --preset qemu-virt-la64-release --bind smp=1 --bind memory=1G`在sandbox外PASS。两项只
证明architecture integration；未启动guest runtime。

**Claim boundary and closure candidate:** Contract Impact为None；existing Effective IDs Preserve，全部Socket/
Endpoint/UDP/wait candidate继续Pending。rootfs、QEMU guest runtime、KUnit runtime、UDP syscall/LTP、LA64 runtime、
remote external、SMP>1、hardware与final harness均Not Run。3A source与validation已满足closure candidate；最终
subagent exact-diff review、docs/status gate和单一`udp: ...`commit仍待完成，完成前不宣称3A Closed，3B/3C保持
Ready / Not Active。

## 2026-07-30 - Stage 3 Checkpoint 3A final review and closure

**Independent review:** 唯一一位subagent显式读取全部untracked split child和transaction closure candidate，按
Anemone change-review等级逐item/body、visibility、caller、owner和write set复核，结论为Apollyon 0、Keter 0、
Euclid 0、Safe 0。review确认`stack/udp_ops.rs -> stack/udp.rs`逐字相同；protocol旧flat item body保持，变化只有
child module framing、合并两个`impl Endpoint` block与恢复原owner访问域的`pub(super)`；`udp/mod.rs`的窄
crate-private re-export没有新增crate-public path。DomainStack四个probe method及body保持，raw Stack/lock仍private，
caller仍只有`net/mod.rs` KUnit；没有3B consumer、Socket/File/syscall、capacity、Kconfig或ABI变化。

**Final write-back and closure:** RFC/implementation/navigation/biweekly同步Checkpoint 3A Closed、Checkpoint 3B-3C
Ready / Not Active；current contracts、`invariants.md`、tracking/register/current limitations保持只读，Contract Impact
为None，全部Socket/Endpoint/UDP/wait candidate继续Pending。最终docs增量由同一reviewer复核，formatter、whitespace
与mdBook gate在最终文件重跑。3A source、validation、review、stop-condition audit与write-back由单一
`udp: split stage 3 endpoint owners` checkpoint commit关闭；transaction保持Active并立即停止。3B/3C、`3 -> 4`
resolution、candidate contract cutover、rootfs/QEMU guest runtime与其它Not Run范围均未执行或激活。

## 2026-07-30 - Stage 3 Checkpoint 3B activation preflight

**Authorization and entry:** 开发者明确授权只完成Stage 3 Checkpoint 3B，要求持续推进到closure、一个
`udp: ...`commit、最终subagent exact-diff review并在关闭后停止；3C、Stage 4 resolution与contract cutover均未
授权。activation point为`dev/drc/alpha@f30b050d`，tracked/untracked worktree clean；AGENTS/LOCAL、R0正文、
implementation、register/current limitations、current Network/Opened-description/IOMUX/Epoll/SystemTarget
contracts、本transaction与live protocol/net/File/fd/syscall/config/ABI/app owners均已重读。RFC没有
`tracking-issues.md`，register没有改变3B路线的active issue。

**Frozen route and boundary:** 3A split与6.3 Ready定义未漂移。3B只建立unbound Endpoint、binding/port0 namespace、
kernel-private `UdpEndpointPort`、`File::prv`中的anonymous `UdpSocketFile`和`socket/bind/getsockname`；不注册
`sendto/recvfrom`，不接入datagram、blocking或iomux。`task/files.rs`、`fs/file.rs`与anonymous VFS lifecycle只读，
复用既有fd reservation、description refs与创建时固定的final-release hook。current contracts、`invariants.md`、
register、vendored smoltcp、net worker/provider和其它apps保持只读；全部candidate contract继续Pending。

## 2026-07-30 - Stage 3 Checkpoint 3B implementation and validation candidate

**Protocol and capability implementation:** `anemone-net-api::udp`只承载stack/kernel真实consumer共享的opaque
boot-local non-reused Endpoint ID、resource limits、bind request、committed local binding与typed create/bind/query/
retire outcomes。concrete Stack唯一拥有Endpoint collection、binding conflict、ephemeral scan、per-interface engine
projection与retire：64-capacity boundary、wildcard与任一同port冲突、相同specific冲突、不同specific共存、port0
回绕/exhaustion、failed bind保持unbound、retire后port reuse与stale ID fail-closed均由同一namespace完成，没有第二
binding table或Socket mirror。host compatibility create-and-bind在绑定失败时rollback fresh Endpoint，避免遗留
unbound resource。

kernel `net::udp::UdpEndpointPort`只持有`Arc<DomainStack> + UdpEndpointId`，control plane只判定explicit local
address是否属于`127/8`或configured external address，raw Stack/private lock不逃逸。`UdpSocketFile`只包含
`association: SpinLock<Option<UdpEndpointPort>>`与operation mutex；创建顺序为fd reservation -> Endpoint ->
anonymous File -> FileDesc -> fd commit。publication前由creation guard先retire再断言，publication后semantic final
release先take association、释放spin lock，再non-blocking retire；它不取operation mutex、不等待worker，也不以
`Drop`或capability clone决定liveness。dup/fork共享opened description，CLOEXEC保持fd-local，NONBLOCK保持
description-local；getsockname operation guard保持到copyout结束。

**ABI, config and consumer:** `anemone-abi::net`固定双架构16-byte/alignment/offset sockaddr layout与asm-generic
198/200/204/206/207号码；kernel只注册198/200/204。input以byte copy接受unaligned `16..=128`，output先读signed
32-bit length、复制prefix、再写actual length；unbound name为`0.0.0.0:0`，rebind为`EINVAL`，non-socket为
`ENOTSOCK`，explicit nonlocal为`EADDRNOTAVAIL`，conflict为`EADDRINUSE`，port0 exhaustion为`EAGAIN`。Kconfig
owner新增Endpoint/TX/RX/payload与ephemeral range参数、checked storage arithmetic和generated constants；双架构
`anemone-rs` raw/typed surface由新`udp-test`真实消费并接入两份pretest rootfs与`user-test`。

real capability出现后已删除stack `udp-validation-probe` feature、kernel forwarding edge、`stack/udp_probe.rs`与
DomainStack probe support；原三项local-path probe KUnit不再保留为production-like旁路。owner-local KUnit改为证明
File association与creation guard；长期`host_validation.rs`只保留真实host integration consumer，ordinary operation
调用production Stack path。source audit确认没有`sendto/recvfrom` handler、probe residue、raw Stack escape、
smoltcp handle/queue truth、binding mirror或manifest外tracked source修改。

**Validation evidence:** final source上`just xtask-test`为62/62；完整net-api/stack host gate通过net-api unit 2、
stack unit 2、bounded-progress 7、frame-path 9、multi-instance 2、multi-interface 2、UDP topology 7与net-api doctest 2。
两项stack no-default test/check、`just fmt kernel --check`、`just fmt udp-test --check`、RV64/LA64 `udp-test` build均
PASS。RV64 kernel build在sandbox内由unchanged `lwext4` C compile命中`Bad system call`/SIGSYS，相同repository
command在sandbox外PASS；LA64 release kernel build也PASS。两项build只证明architecture integration。

修正后exact code由canonical RV64 wrapper从caller-selected master重建rootfs/runtime disk并正常退出：269/269
KUnit全部PASS，两项`fs::socket::udp` KUnit明确执行；`UDPTEST:SUMMARY:PASS:6`覆盖create errno/flags、unbound/name、
binding/port0/address、unaligned/truncated/negative/zero-length copy、dup/fork/final release与CLOEXEC exec；LTP whitelist
attempted 4 / passed 4 / failed 0，shutdown严格保持`filesystem -> network -> device -> PowerOff`。日志中的
`FAIL LTP CASE ... : 0`是harness标签，case与group summary均为failed 0。

**Claim boundary:** Contract Impact为None；existing effective IDs Preserve，`NET-PROTOCOL-BOUNDARY-001`、
`NET-SOCKET-ENDPOINT-001`、`NET-UDP-TRANSACTION-001`与`NET-SOCKET-WAIT-001`继续Pending。LA64 runtime、
send/receive、remote external、SMP>1、blocking/signal、poll/select/epoll、fragment、hardware、network LTP与final
harness均Not Run。source、validation与stop-condition audit已满足closure candidate；独立exact-diff review、最终
docs gate与单一commit仍待完成，完成前不自动激活3C。

## 2026-07-30 - Stage 3 Checkpoint 3B final review and stop

**Independent exact-diff review:** 唯一一位subagent读取`f30b050d..worktree`全部tracked/untracked source、配置、
consumer与write-back，并复核RV64 runtime证据。最终分类为Apollyon 2、Keter 1、Euclid 0、Safe 0，因此3B不满足
closure floor：

1. Apollyon：`scripts/xtask/src/config/kconfig.rs`只检查datagram count乘payload的`usize`溢出，没有检查
   `VecDeque`/`PacketMetadata`等实际allocation layout或受支持上界；`capacity=usize::MAX, payload=1`可通过生成期
   validation，随后在Endpoint构造中确定性capacity/layout panic。需要验证每种实际allocation layout并增加
   max-count/payload-one regression。
2. Apollyon：anonymous `UdpSocketFile`以`InodeType::Regular`创建，`fstat`/`statx`因此返回`S_IFREG`而非Linux
   socket要求的`S_IFSOCK`。修正需要为VFS `InodeType`增加Socket投影，并处置多个exhaustive consumer；
   `anemone-kernel/src/fs/inode.rs`及相邻VFS owner不在6.3.11 frozen manifest，且anonymous VFS被明确设为只读，
   已触发write-set expansion停止条件。
3. Keter：namespace-wide endpoint capacity来自每次create request，ephemeral range来自每次bind request；不同caller
   可在同一Stack逐次改变domain policy，形成并列真相源。需要由Stack/UdpEndpoints一次拥有namespace policy，
   per-endpoint TX/RX/payload limits才保留为Endpoint-local；host小容量/小端口区间必须走显式test-only配置路径。

**Evidence disposition:** 现有final source的269/269 KUnit、两项UDP File KUnit、
`UDPTEST:SUMMARY:PASS:6`、LTP whitelist 4/4、严格`filesystem -> network -> device -> PowerOff`与无panic日志均为
有效证据，但没有覆盖上述allocation-layout、`S_IFSOCK`或mixed-policy defect。LA64 runtime及其它Not Run边界不变。

**Stop / write-back:** 开发者指示先记录问题并提交当前实现候选，但不关闭3B。RFC/implementation/navigation/
biweekly同步为Checkpoint 3B Review Hold；3C仍Ready / Not Active。current contracts、`invariants.md`、register/
current limitations保持只读，Contract Impact仍为None，全部candidate继续Pending。该commit只保存implementation
candidate、验证证据与review findings，不构成3B closure；修正前必须先明确批准并冻结VFS write-set expansion，
随后处置全部Apollyon/Keter、重跑受影响validation与独立exact-diff复审。不得进入3C、Stage 4 resolution或
candidate contract cutover。

## 2026-07-30 - Stage 3 Checkpoint 3B correction approval and reactivation

**Approval and retained boundary:** 开发者在收到扩展理由、最小VFS范围、public/shared-surface影响、
Contract Impact与验证计划后明确回复`approved`。该批准只重新激活3B Review Hold correction；3C继续Ready /
Not Active，Stage 4 resolution、current-contract cutover、target/ABI/acceptance变化均未授权。进入时为
`dev/drc/alpha@8d774608`，tracked/untracked worktree clean；该commit继续是已记录但未关闭3B的candidate baseline。

**Read-only preflight and resolved expansion:** live exhaustive-consumer audit确认新增`InodeType::Socket`只要求
`anemone-kernel/src/fs/inode.rs`、`fs/api/getdents64.rs`与`fs/ext4/{mod.rs,inode.rs,superblock.rs}`五个manifest外
文件：generic VFS投影`S_IFSOCK/DT_SOCK`，ext4显式拒绝anonymous-only Socket。`fs/anonymous/mod.rs`已经接受全部
非Dir类型；ramfs/devfs/procfs没有接收任意Socket type的可达创建路径且现有分派有wildcard，因此保持只读。
`fs/socket/udp.rs`在原manifest内改用Socket inode；`anemone-rs`原manifest内增加窄`statx` wrapper供真实consumer。

**Backend admission follow-up:** 开发者随后指出ramfs等backend是否纳入应按实际需求判断。更窄的入口审计确认
ramfs/proc创建helper只由hard-coded类型调用，继续只读；但devfs public `DevfsNodeAttr.ty`的leaf准入只排除Dir，
新增枚举会实际扩大其publish能力并允许Socket。authoritative manifest因此再最小加入
`anemone-kernel/src/fs/devfs/mod.rs`，只在publish owner入口返回`NotSupported`并增加owner-local KUnit。该修正保持
anonymous-only Socket边界，Contract Impact仍为None，不改变lifecycle、public owner、ABI/acceptance或3C状态。

namespace correction继续使用原Stage 3 source union：新增shared `UdpNamespacePolicy`，`UdpEndpoints`在Stack构造时
一次拥有Endpoint capacity与ephemeral first/last；create request只保留per-Endpoint TX/RX/payload limits，bind
request不再携带range。production `DomainStack`从generated KernelConfig一次构造policy；host UDP tests使用显式
host-only constructor/config path。参数acceptance唯一位于kernel compilation：xtask只做typed parse/default
materialization/constant generation；stack owner以type-free public const predicate封装private `Endpoint`、
`ReceivedDatagram`与smoltcp `PacketMetadata`的actual allocation layout，kernel static assertions同时覆盖nonzero、
payload protocol range、ephemeral cross-field、namespace Vec、receive VecDeque、metadata与payload storage。

**Contract and validation:** Contract Impact为None；anonymous/VFS与opened-description lifecycle owner不变，
existing Effective IDs Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending。temporary workspace-relative
KernelConfig必须通过xtask parse/generation后在kernel static assertion处compile-fail，覆盖zero、payload range、
ephemeral cross-field和Endpoint/TX/RX max-count payload-one layout；随后重跑`just xtask-test`、完整host/no-default、
kernel/app formatter、RV64/LA64 app/kernel build、fresh-disk RV64 wrapper、mdBook、whitespace/source/write-set audit，
并由同一时间唯一一位subagent在final exact diff上复审Apollyon/Keter/Euclid。全部清零前3B保持Review Hold；
不得进入3C。

## 2026-07-30 - Stage 3 Checkpoint 3B capacity-model correction

**Reassessment:** final exact-diff review在既有修正上继续要求把per-interface private `SocketStorage`纳入
KernelConfig layout predicate，并让external/local `SocketSet`按Endpoint capacity eager reserve。live type/layout
evidence显示64-bit host的`Endpoint`为136 bytes、`SocketStorage`为200 bytes；两者layout representability只在约
`4.6e16`个Endpoint以上出现差异。Stack构造已经先按完整capacity执行Endpoint backing allocation，因此真实机器会
在任何用户`socket()`或SocketSet growth前先命中R0明确排除的allocator OOM；该finding没有识别独立、较早或正常
配置可达的failure。

**Route Correction:** 开发者确认持续穷尽private allocator layout已使实现形状扭曲。3B不扩张
`stack/interfaces.rs`或`local_link.rs`，不把lazy SocketSet变为eager allocation。cross-crate private-layout predicate
删除；kernel继续唯一验收nonzero、payload protocol ceiling、ephemeral cross-field及TX/RX direct payload-byte
arithmetic。xtask仍只parse/materialize/generate，不做semantic acceptance。invalid-config matrix相应只要求
maximum TOML count与payload-two在kernel byte-storage assertion失败；payload-one的物理OOM不伪装成layout contract。
normal Endpoint/queue/port exhaustion继续返回typed outcome；ordinary allocator OOM仍按R0 non-goal处理。

**Boundary:** 该修正减少public/shared proof surface，不改变R0 target、namespace/Endpoint owner、ABI visible
semantics、normal capacity、acceptance或Contract Impact；全部candidate保持Pending，3B继续Review Hold，3C仍Ready /
Not Active。原SocketStorage Keter按无独立reachable failure与有害eager-allocation repair证据Neutralized；最终source
仍须重跑本checkpoint validation floor并完成新的独立exact-diff review。

## 2026-07-30 - Stage 3 Checkpoint 3B natural-shape follow-up

**Correction to the preceding allocation-order rationale:** `UdpEndpoints`不再按namespace capacity在Stack构造时
eager reserve完整Endpoint backing；该reserve不是capacity owner或normal exhaustion语义所要求，只是为了证明极端
配置下某个private layout会先失败，反而把逻辑上限扭曲成启动期内存承诺。Endpoint collection恢复`Vec::new()`并按
真实publication lazy growth，external/local `SocketSet`继续lazy。此前“Endpoint backing一定先失败”的顺序论证不再
作为finding neutralization依据。

**Retained boundary:** kernel仍唯一验收nonzero、payload protocol ceiling、ephemeral cross-field与TX/RX direct
payload-byte arithmetic。private metadata/container layout和物理allocation success不成为KernelConfig contract；
物理上不可满足的trusted configuration属于R0 allocator/OOM非目标，不等同于ordinary用户触发的bounded capacity
exhaustion。该follow-up不改变namespace policy owner、normal typed outcome、ABI、Contract Impact、3C状态或write set。

## 2026-07-30 - Stage 3 Checkpoint 3B final review and closure

**Independent final review:** 唯一一位subagent读取`8d774608..worktree`完整source/config/consumer/docs exact diff与
final RV64日志，按Anemone change-review等级复核capacity model、namespace owner、VFS/backend admission、public
surface、lifecycle、ABI、write set与3C边界。结论为Apollyon 0、Keter 0、Euclid 0、Safe 0。review确认private
metadata/layout不再反向塑造shared surface；物理不可满足的trusted config属于R0 allocator/OOM非目标，不是ordinary
用户capacity failure；normal endpoint/port exhaustion仍返回typed outcome。`UdpEndpoints`一次拥有capacity/range，
ext4/devfs在真实admission owner拒绝Socket，ramfs/proc固定类型入口无需机械扩集。

**Final evidence:** correction source复用已通过的62/62 xtask、完整host/no-default、invalid-config kernel
compile-fail matrix、双架构app/kernel build证据；correction-source RV64 wrapper执行271/271 KUnit，明确覆盖devfs Socket
拒绝、UDP inode `S_IFSOCK`、creation guard与association，并得到`UDPTEST:SUMMARY:PASS:7`、
`EPOLLTEST:SUMMARY:PASS:11`及`filesystem -> network -> device -> PowerOff`。最后的Endpoint collection
`Vec::with_capacity -> Vec::new`只恢复lazy allocation，不改变publication、capacity判断、binding或ABI；按开发者指示
不重复QEMU。formatter、whitespace与mdBook在final authored source通过。glibc LTP group为2/2；musl输出未形成完整
aggregate，因此不宣称本轮LTP 4/4，且它不是3B UDPTEST退出条件。

**Closure and stop:** source/write-set audit只命中3B manifest及获批VFS/devfs expansion；`interfaces.rs`、
`local_link.rs`、current contracts、register/current limitations、`invariants.md`与3C source保持只读，临时invalid config
已删除。Contract Impact为None，existing Effective IDs Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending。
Checkpoint 3B由本单一`udp: ...`checkpoint独立Closed，transaction保持Active并立即停止；3C仍Ready / Not Active，
Stage 4 resolution、current-contract cutover与其它Not Run范围均未授权。

## 2026-07-30 - Post-3B module-boundary correction authorization and activation

**Finding and authorization:** 3B关闭后的周期工程审计发现kernel Socket API把`bind/getsockname`合入同一
`address.rs`，3C manifest又计划把`sendto/recvfrom`合入`datagram.rs`；`anemone-abi::net`根部则直接平铺纯
Linux UAPI。开发者接受一syscall一文件、`anemone_abi::net::linux` strict source-path cutover与
`anemone-rs`父级目录化方向，并明确要求同时拆分`sys/os`两层`linux/anemone`下全部现有公开inline module，完成
验证后交一个commit。

**Resolved boundary:** 该授权只激活独立post-3B behavior-preserving structural checkpoint。kernel改为
`socket.rs`、`bind.rs`、`getsockname.rs`，3C未来使用独立`sendto.rs`/`recvfrom.rs`；shared sockaddr/error helper
继续由`abi.rs`拥有。`anemone-abi`只新增`net::linux`module identity并原子更新workspace direct consumers，不保留
flat compatibility re-export，也不预建无consumer的native network ABI。`anemone-rs::sys::linux::net`继续拥有raw
wrapper，`os::linux::net`继续拥有typed wrapper；其它公开inline module只做同owner物理拆分，所有既有public Rust
path和function/type body保持不变。resolved write set已回写canonical implementation manifest；Contract Impact为
None，现有Effective IDs Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending，3B保持Closed，3C保持
Ready / Not Active。

**Validation and stop:** 必须运行repository formatter、结构与consumer audit、`udp-test`双架构app build、
RV64/LA64 kernel build、whitespace和mdBook，并对final exact diff执行change review。出现function/type body变化、
owner/API超出已批准`net::linux`source-path cutover、shared contract/ABI visible semantics变化或3C行为时立即停止；
全部通过后仅关闭本纠偏checkpoint并停止。

## 2026-07-30 - Post-3B ABI file-shape correction

开发者在source修改前修正`anemone-abi`物理布局：`src/net.rs`保持单文件，直接inline空`native`与承载现有Linux
UAPI的`linux`，不采用`net/{mod.rs,linux.rs}`目录。`anemone_abi::net::linux` strict source-path cutover、无flat
compatibility re-export、workspace direct-consumer原子更新及其它checkpoint边界不变；canonical implementation
manifest已同步。获批public Rust API delta现在明确为strict source-path cutover与空`native`module identity；除此
之外，本修正不改变owner、wire ABI/visible semantics、Contract Impact、验证或停止条件。

## 2026-07-30 - Post-3B module-boundary correction closure

**Implementation:** kernel `fs/api/socket`删除聚合`address.rs`并把`create.rs`改名为`socket.rs`，现由
`socket.rs`/`bind.rs`/`getsockname.rs`各自承载唯一syscall，shared sockaddr/error mapping继续由`abi.rs`拥有；
3C的`sendto.rs`/`recvfrom.rs`没有创建。`anemone-abi/src/net.rs`按修正后的单文件形状inline空`native`与承载全部
现有Linux network UAPI/layout assertion的`linux`，kernel、`anemone-rs`与`udp-test` direct consumers原子切换到
`anemone_abi::net::linux`，未保留flat re-export。`anemone-rs::{sys,os}::{linux,anemone}`四个parent已目录化，
全部现有公开inline domain module成为独立child file；raw/typed net layers和所有既有public path保持不变。

**Validation:** 13项拆分前后module body逐字对照PASS；source/API audit确认旧flat ABI path、inline domain module、
旧kernel聚合文件与未授权3C文件均为0。`just fmt kernel --check`、`just fmt udp-test --check`、RV64/LA64
`just app build --arch ... udp-test`、两个冻结的release `just build --preset ... --bind smp=1 --bind memory=1G`、
`git diff --check`与`mdbook build docs`均PASS。RV64首次sandbox build在`lwext4` C compile命中`Bad system call`/
SIGSYS；完全相同repository命令在sandbox外PASS，归类为环境失败。QEMU、KUnit runtime、UDPTEST/LTP、LA64
runtime、hardware与final harness均Not Run，本checkpoint不外推runtime能力。

**Review and stop:** exact diff/change review确认function/type body、syscall number/layout/errno、Socket/Endpoint owner、
opened-description lifecycle、wire ABI、shared contract与3C behavior均未改变；Apollyon/Keter/Euclid/Safe全0。
Contract Impact为None，existing Effective IDs Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending。
post-3B纠偏checkpoint独立Closed，transaction保持Active并立即停止；3C仍Ready / Not Active，Stage 4 resolution与
current-contract cutover均未授权。

## 2026-07-30 - Stage 3 Checkpoint 3C implementation, review, repair and closure

**Entry state and scope:** `dev/drc/alpha@1bf50b2e9e6c`；post-3B module-boundary correction已Closed，tracked/
untracked worktree只含本次3C source。live RFC/implementation、transaction、register/current limitations、current
contracts、AGENTS/LOCAL与frozen manifest已重读。授权只覆盖implicit bind、control-plane/Stack send admission、owned
receive transaction、`sendto/recvfrom` ABI/library/user-test、validation/review/write-back与一个`udp: ...`commit；不
覆盖Stage 4、`3 -> 4`resolution、current-contract cutover或manifest外owner。

**Implementation:** `anemone-net-api::udp`增加operation-local egress selection、peer、owned received datagram与typed
send/receive outcome；Stack在commit前重验interface/source/MTU/TX capacity，在Stack锁内detach whole receive
datagram。kernel-private `UdpEndpointPort`先提交implicit wildcard binding，再调用唯一control plane selection和Stack
admission；send success后请求选中pump。`UdpSocketFile` operation mutex串行bind/name/send/receive，receive guard跨
payload/peer/addrlen copyout，final release仍不取该mutex。kernel新增一syscall一文件的`sendto.rs`/`recvfrom.rs`，
只支持`MSG_DONTWAIT`；nonblocking would-block返回`EAGAIN`，default-blocking would-block使用带Stage 4删除条件的
`EOPNOTSUPP` bridge。zero length不访问payload pointer，null peer完全忽略addrlen，detach后的三类copy fault均消费
datagram。`anemone-rs`增加typed IPv4 wrapper，`udp-test`形成13项real consumer matrix。

**Initial independent review and repair:** 唯一subagent对`1bf50b2e..worktree`完整source diff（含两个new syscall
file）进行change review，结论Apollyon 3、Keter 0、Euclid 0、Safe 0：aggregate RX credit恢复后engine datagram
缺少durable progression；fresh unbound oversize在ABI allocation guard处早于implicit bind失败；bind与selection对
specific noncanonical `127/8` source predicate不一致。三项都在frozen owner/write set内修复：Stack detach后同步
refill各engine；`UdpSendOperation`在同一File operation guard内先`ensure_bound`再做bounded copyin与Stack admission；
control plane统一用`Ipv4Address::is_loopback()`。host recovery删除手工pump，user runtime增加fresh oversize
`getsockname` retention与`127.0.0.2` specific-source roundtrip。

**Validation on repaired source:** `just xtask-test`为62/62；net-api/stack完整host matrix通过，其中
`udp_topology`为9/9；no-default test/check、kernel与`udp-test` formatter、RV64/LA64 `udp-test` app build、RV64/LA64
release kernel build、`git diff --check`均PASS。RV64首次sandbox build在unchanged `lwext4`命中`Bad system call`/
SIGSYS；完全相同repository command在sandbox外PASS，归类为环境限制。canonical fresh-disk RV64 wrapper执行
271/271 KUnit并输出`All tests passed!`、`UDPTEST:SUMMARY:PASS:13`、`EPOLLTEST:SUMMARY:PASS:11`、LTP whitelist
`attempted=4 passed=4 failed=0 infra_failed=0 skipped=0`与strict
`filesystem -> network -> device -> PowerOff`。日志为`build/net-udp-stage3-rv64.log`。

**Invalid-config evidence boundary:** 3C没有修改KernelConfig schema、generated values、kernel static assertion或
配置consumer；按开发者明确决定，invalid-KernelConfig matrix为**Not Re-run / unchanged owner**。本checkpoint只
沿用3B已记录的compile-fail evidence，不宣称3C重跑，也不因此降低其它final-source gate。

**Final review and closure:** 同一subagent复审完整staged source diff，确认三项Apollyon均Neutralized，最终
Apollyon 0、Keter 0、Euclid 0、Safe 0；review绑定source diff SHA-256
`31dcaca208a392a787ced1e5ece81d47ce5290e17014f15e120911ad06667c7e`，cached whitespace无诊断。residual
non-blocking test gaps为recvfrom unsupported flags、null peer配invalid addrlen和nonempty datagram配zero receive
length的direct case；现有实现路径已审查且这些不改变3C acceptance，不扩大为Stage 4授权。source/write-set audit
只命中resolved manifest；current contracts、register/current limitations、`invariants.md`、task/files、fs/file、
iomux/epoll与Stage 4 source保持只读。Contract Impact为None，existing Effective IDs Preserve，全部Socket/Endpoint/
UDP/wait candidate继续Pending。Checkpoint 3C关闭Stage 3，transaction因R0后续Stage仍未完成而保持Active并立即
停止；没有运行`3 -> 4`resolution、激活Stage 4或执行current-contract cutover。

## 2026-07-30 - Stage 3 -> Stage 4 Implementation Resolution Gate

**Authorization and entry:** Stage 3已在`dev/drc/alpha@0204ac74`完整Closed且进入时worktree clean。开发者先要求
只读说明Stage 4路线，随后确认multi-waiter不能作为4C后加能力，并明确授权更新implementation文档，同时要求真实
实现出现摩擦时保留调整空间。本授权只覆盖read-only preflight、Stage 4 Ready定义、transaction/RFC/navigation/
biweekly状态同步与docs gate；不覆盖4A source、后续checkpoint、current-contract cutover或任何source修改。

**Live owner evidence:** concrete Stack/Endpoint继续拥有RX aggregate、TX admission phase、capacity与private engine
facts；kernel `UdpSocketFile`拥有association/operation但`FileOps::poll`仍是empty snapshot-only stub。existing
`PollRoute`/`IomuxWaitRound`已经支持source-neutral复数consumer与snapshot/register/final-scan，shared
`wait_for_iomux_ready()`当前只对`ppoll/pselect6`可见。vendored smoltcp在IPv4 parse、UDP demux前已有明确first/later
fragment拒绝，Stage 4需要真实frame injection proof而不是预先修改vendor。现有ABI/library wrapper、opened-description
hook与epoll consumer足够，因此`anemone-abi`、`anemone-rs`、`task/files.rs`、`fs/file.rs`和epoll implementation不进入
resolved source manifest。

**Resolved route:** Stage 4分为三个独立activation checkpoint。4A建立Endpoint facts/invalidation handoff、kernel-private
reverse event route与完整复数`PollRoute` Socket source，并完成基础poll/select/epoll evidence；4B下移或窄化暴露现有
source-neutral wait loop，让blocking `sendto/recvfrom`在释放operation/source/Stack锁后等待并重验，同时完成最小
deterministic multi-waiter coverage；4C只做capacity/provider、signal/cancel、dup/fork/close、copy/concurrent receive、
fragment injection与stress evidence closure，不把single-waiter扩成multi-waiter。Stage 4每个checkpoint仍须独立授权并
在关闭后停止。

**Route Correction boundary:** exact facts/event类型名、pending edge batch表示、reverse/route container、shared helper
签名/落点、Socket owner内部拆分与checkpoint内部施工顺序仍是implementation preference。保持R0且仍在manifest内时，
先更新canonical implementation并在本transaction记录；新增path/owner先走write-set expansion。single-waiter、复制
第二wait loop、duplicate readiness truth、Stack持waiter/route、owner/ABI/contract变化或降低Stage 4/5 evidence floor
不是可批准的实现弹性，必须停止并进入普通review或Target Renegotiation Gate。

**Contract, manifest and result:** Stage 4 Contract Impact解析为None。effective `OPENED-DESC-001..003`、
`IOMUX-POLL-001..003`与`EPOLL-*` Preserve；全部Socket/Endpoint/UDP/wait candidate继续Pending，等Stage 5达到
LA64同源runtime与remote-external evidence floor后再决定final cutover。canonical
[Stage 4 definition](../../rfcs/net-udp/implementation.md#64-stage-4--closed--blockingiomux与datagram-hardening)
冻结完整manifest并保持ABI/library、opened-description、epoll、vendor、config/rootfs/current-contract owner只读。
本gate结果为Stage 4 **Ready / Not Active**；下一步只能由新的明确授权激活4A，不得自动进入4B/4C、`4 -> 5`
resolution或contract cutover。

**Document gate and stop:** current-status/anchor/write-set扫描与`git diff --check`通过；`mdbook build docs`通过，
只有既有large search-index warning。diff只包含canonical RFC/implementation、transaction、RFC/transaction导航与
当前biweekly devlog六个文档文件；source/current contracts/register均未修改。source build、host test、KUnit、QEMU、
LTP与architecture runtime均Not Run，本docs-only resolution不从文档验证外推Stage 4实现能力。

## 2026-07-30 - Stage 4 Checkpoint 4A implementation, review and closure

**Authorization and entry:** developer创建并要求持续推进的唯一goal是Stage 4 Checkpoint 4A；进入时branch为
`dev/drc/alpha`、HEAD `c2b1fd4bc014`。AGENTS/LOCAL、canonical R0/implementation、current transaction、register/
current limitations、effective OPENED-DESC/IOMUX/EPOLL/Network contracts与live Endpoint/Socket owners已经重读。
授权覆盖4A source、focused review/validation、六个execution write-back文档与一个`udp: ...`commit；明确不覆盖
4B/4C、`4 -> 5`resolution、Stage 5或current-contract cutover。

**Implementation:** shared `UdpEndpointFacts`只承载Stack-owned live/readable/writable point-in-time snapshot；opaque
`UdpEndpointInvalidation`只携带boot-unique Endpoint lookup identity。Stack对create/bind/send/receive/pump/capacity
recovery/retire predicate transition形成bounded coalesced invalidations，DomainStack释放Stack锁后通过weak observer
反向路由。Socket同一owner拆为`udp/{mod.rs,source.rs}`；source唯一拥有association publication、event registration与
复数interest-bearing `PollRoute` snapshots。`FileOps::poll`先fallibly准备route replacement，在source临界区发布后
读取fresh facts；ready-at-register也保留route。stale pruning、旧snapshot drop、notify、creation rollback与semantic
final release均保持锁外handoff，final release不取得operation mutex或等待worker。

**Runtime test correction:** 初次新增UDP case返回`EIO`。逐段检查证明sender在异步`sendto`后立即close，使semantic
final release按既有owner规则retire其Endpoint-owned queued datagram，早于local pump transfer；这不是production
send-close语义缺陷。test sender改用ack pipe，在ppoll/pselect/epoll readiness被父进程观察后才允许child close。
review随后指出wait syscall若提前返回errno，`?`会让child阻塞在ack并污染后续case；修复为始终先ack/reap，再传播
保存的wait result。production source/send/close语义未改变。

**Independent review and repairs:** 唯一final review最初确认两个Keter：host-only `HostEndpointFacts` re-export越出
frozen manifest；ready-at-register要求缺少精确route-retention测试。前者通过直接复用existing `UdpEndpointFacts`并
消除`anemone-smoltcp-stack/src/lib.rs`实质diff收敛；后者增加real prepared source KUnit，先得到
`Subscribed(WRITABLE)`再证明后续hint命中保留route。review还确认上述delayed-sender cleanup Euclid并已修复。
最终verdict为Apollyon 0、Keter 0、acceptance-affecting Euclid 0、Safe 0；剩余一个可选Euclid是ppoll/pselect/epoll
仍合并为单个case且失败只报告`EIO`，不影响4A acceptance，未扩大本checkpoint做诊断拆分。

**Validation on exact source:** `just xtask-test` 62/62；`cargo test -p anemone-net-api -p
anemone-smoltcp-stack`通过，`udp_topology` 10/10；no-default test build/check、kernel/udp-test formatter、RV64/LA64
`udp-test` app build与RV64/LA64 release kernel build均PASS。sandbox RV64 build在unchanged `lwext4`命中
`Bad system call`/SIGSYS；相同repository build在sandbox外PASS，归类为环境限制。canonical fresh-disk RV64 wrapper
执行273/273 KUnit（包括ready-at-register KUnit）、`EPOLLTEST:SUMMARY:PASS:11`、
`UDPTEST:SUMMARY:PASS:14`、LTP whitelist `attempted=4 passed=4 failed=0 infra_failed=0 skipped=0`与正常
`filesystem -> network -> device -> PowerOff`；日志为`build/net-udp-stage4a-rv64.log`。

**Boundary and stop:** source/write-set audit只命中6.4.10允许的4A subset；最终`anemone-smoltcp-stack/src/lib.rs`、
ABI/library、task/files、fs/file、iomux/epoll implementation、vendor、config/rootfs、current contracts、`invariants.md`、
register/current limitations、其它RFC与scripts均无source write。Contract Impact为None；effective OPENED-DESC/IOMUX/
EPOLL与Network/control-plane IDs保持Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending。LA64 runtime、
remote external、hardware、`smp>1`、full network LTP与final harness均Not Run。Checkpoint 4A独立Closed，transaction
因R0后续checkpoint仍未完成而保持Active并立即停止；4B/4C保持Ready / Not Active，未解析Stage 5或执行cutover。

## 2026-07-30 - Stage 4 Checkpoint 4B implementation, review and closure

**Authorization and entry:** developer创建并持续推进的唯一goal是Stage 4 Checkpoint 4B；进入时branch为
`dev/drc/alpha`、HEAD `9e3aa472`，worktree无其它dirty change。AGENTS/LOCAL、canonical R0/implementation、current
transaction、register/current limitations、effective OPENED-DESC/IOMUX/EPOLL/Network contracts与live
Socket/iomux/syscall owner已经重读。授权只覆盖4B source、focused review/validation、六个execution write-back文档与
一个`udp: ...`commit；不覆盖4C、`4 -> 5`resolution、Stage 5或current-contract cutover。

**Implementation:** 唯一source-neutral `wait_for_iomux_ready()`从`fs/api/iomux/wait.rs`下移到最低共同owner
`fs/iomux/wait.rs`；`ppoll/pselect6`经原API层re-export继续调用同一实现，UDP adapter只提供一个File source的
snapshot/register/final scan。`sendto/recvfrom`每轮只取得一次File operation guard；`WouldBlock`后guard已经释放，
才进入shared wait，因此schedule期间不持operation/source/Stack锁。blocking与nonblocking复用同一真实operation
predicate；effective `O_NONBLOCK`、creation-time `SOCK_NONBLOCK`和`MSG_DONTWAIT`立即返回`EAGAIN`，per-call flag
不修改opened-description status。

send继续先提交implicit bind，再只copyin payload一次并以kernel transaction跨wait保存；每轮重试重新取得operation
guard并执行current route/source/interface selection与Endpoint admission。receive只在成功detach后持有datagram，
等待重试不会提前detach或requeue，既有payload/peer/addrlen copyout fault consume语义不变。signal/force的final
predicate miss经shared wait映射`EINTR`，未使用`RestartSyscall::Idempotent`；Stage 3 default-blocking
`EOPNOTSUPP` bridge、notice与删除条件注释全部移除。

**Deterministic evidence correction:** 初版real-consumer case使用ready pipe加固定`sched_yield`。运行虽PASS，独立
review仍判定它不能证明两个receiver已经同时发布route并park，也不能证明取消一条已发布route不会撤销另一条，形成
一个阻塞4B closure的Euclid。修复后的child在ready write之后唯一可能阻塞的调用是目标`recvfrom`；父进程局部挂载
procfs并读取`/proc/<pid>/status`，必须观察两个child同时为`S (sleeping)`才注入datagram。一个datagram只允许一个
detach，loser继续等待下一datagram；signal一个waiter并收到`EINTR`结果后，父进程再次确认survivor仍为`S`，再发送
下一datagram。result pipe以1秒`ppoll`有界等待，使lost wake稳定形成FAIL而非整套测试挂死。

第一次procfs barrier runtime在udp-test阶段以`ENOENT`失败，因为competition environment的procfs要到后续
user-test初始化才挂载；该空等QEMU被人工终止。最终用例自行在case入口mount procfs、退出时unmount，保持该观察
能力局部于real consumer，不修改rootfs/user-test owner。canonical wrapper的第一次非TTY调用还因sudo无法读取密码
失败；依据LOCAL授权改用TTY后，相同repository wrapper完成。以上均为执行环境/测试证据摩擦，没有要求production
owner、public API、shared contract、ABI或acceptance扩张。

**Independent review:** 同一subagent的初审除上述deterministic-evidence Euclid外，Apollyon/Keter/Safe均为0，并
确认production source没有owner/correctness blocker。局部procfs barrier与bounded-result修复后，follow-up对最终
source、execution write-back和frozen manifest复审为Apollyon 0、Keter 0、acceptance-affecting Euclid 0、Safe 0；
没有4C finding前移、Target Renegotiation或write-set expansion。

**Validation on exact source:** `just xtask-test`为62/62；`cargo test -p anemone-net-api -p
anemone-smoltcp-stack`全部通过，其中`udp_topology` 10/10；no-default test build/check、kernel/udp-test formatter、
RV64/LA64 `udp-test` app build与RV64/LA64 release kernel build均PASS。sandbox RV64 build在unchanged `lwext4`
命中`Bad system call`/SIGSYS；相同repository build在sandbox外PASS，归类为环境限制。canonical fresh-disk RV64
wrapper执行273/273 KUnit、`EPOLLTEST:SUMMARY:PASS:11`、`UDPTEST:SUMMARY:PASS:15`（包括
`blocking-multi-waiter-signal`）、LTP whitelist `attempted=4 passed=4 failed=0 infra_failed=0 skipped=0`与正常
`filesystem -> network -> device -> PowerOff`；日志为`build/net-udp-stage4b-rv64.log`。

**Boundary and stop:** 最终source只修改4B manifest中的`fs/iomux/{mod.rs,wait.rs}`、
`fs/api/iomux/wait.rs`、`fs/api/socket/{mod.rs,abi.rs,sendto.rs,recvfrom.rs}`和`udp-test/src/main.rs`。
`ppoll/pselect6`、PollRoute/IomuxWaitRound contract、Stack/Socket source owner、epoll、ABI/library、task/files、fs/file、
vendor、config/rootfs、current contracts、`invariants.md`、register/current limitations、其它RFC与scripts均无写入。
Contract Impact为None；effective OPENED-DESC/IOMUX/EPOLL与Network/control-plane IDs保持Preserve，全部Socket/
Endpoint/UDP/wait candidate继续Pending。LA64 runtime、remote external、hardware、`smp>1`、full network LTP与final
harness均Not Run。Checkpoint 4B独立Closed，transaction因4C/Stage 5仍未完成而保持Active并立即停止；4C保持
Ready / Not Active，未解析Stage 5或执行cutover。

## 2026-07-30 - Stage 4 Checkpoint 4C implementation, review, repair and closure

**Authorization and entry:** developer创建并持续推进的唯一goal是Stage 4 Checkpoint 4C；进入时branch为
`dev/drc/alpha`、HEAD `cce16b50`，worktree无其它dirty change。AGENTS/LOCAL、canonical R0/implementation、current
transaction、register/current limitations、effective OPENED-DESC/IOMUX/EPOLL/Network contracts、live
Endpoint/Socket/source owner与vendored IPv4 fragment gate已经重读。授权只覆盖4C source/evidence、独立review、
完整validation、六个execution write-back文档与一个`udp: ...`commit；不覆盖`4 -> 5`resolution、Stage 5、
current-contract cutover或target/owner/ABI/visible semantics/acceptance变化。

**Evidence implementation:** deterministic host proof增加initial-unbound writable、selection/oversize failure保持general
writable、TX saturation/recovery和provider credit exhaustion/recovery，证明Socket没有复制provider/private-engine
capacity truth。真实`BoundedProvider` frame injection依次输入first fragment、later fragment和完整fragment pair；三者
均不形成Endpoint delivery/readiness，随后同五元组完整UDP datagram正常收取，证据绑定vendored IPv4 parse前的
explicit gate且vendor无diff。Socket source KUnit覆盖retire publication withdrawal、late duplicate hint和retired poll。

RV64 real consumer扩展八轮dup/one-alias-close/final-close/port reuse、blocking receive与send多waiter及signal
cancellation、blocking receive与ppoll/pselect/epoll并存，以及payload/peer/addrlen三类并发copy fault各消费一个
datagram后control recovery。blocking-send压力使用未解析neighbor保持Endpoint admission占用；并发fault producer初版
以nonblocking back-to-back send命中正确`EAGAIN`，随后只把test producer改为blocking。这两项都是evidence shape
摩擦，没有要求production owner/API/ABI/contract或acceptance变化。

**Independent review and repair:** 唯一subagent的final exact-diff初审为Apollyon 1、Keter 0、Euclid 0、Safe 0：
semantic retire复用ordinary `READABLE|WRITABLE` interest过滤，合法empty-interest或HANG_UP-only route可能收不到
final-close hint并永久睡眠。该lost wake在冻结Socket source owner/write set内修复：source先撤publication与reverse
route，再在锁外通知全部detached routes；ordinary Endpoint fact invalidation继续按ordinary interests过滤。KUnit同时
注册readable、empty-interest与HANG_UP三条route，证明retire全部通知、late duplicate hint不重复通知且retired poll
返回`IdentifierRemoved`。同一reviewer复审最终source为Apollyon 0、Keter 0、Euclid 0、Safe 0；没有其它owner、锁、
lifecycle identity、concurrent detach/copy、fragment fidelity、write-set或contract finding。

**Validation on repaired exact source:** `just xtask-test`为62/62；`cargo test -p anemone-net-api -p
anemone-smoltcp-stack`全部通过，其中`udp_topology` 12/12；no-default test build/check、kernel/udp-test formatter、
RV64/LA64 `udp-test` app build与RV64/LA64 release kernel build均PASS。sandbox RV64 build在unchanged `lwext4`
命中`Bad system call`/SIGSYS；完全相同repository command在sandbox外PASS，归类为环境限制。canonical fresh-disk
RV64 wrapper执行274/274 KUnit（含empty-interest/HANG_UP retire KUnit）、`EPOLLTEST:SUMMARY:PASS:11`、
`UDPTEST:SUMMARY:PASS:16`、LTP whitelist `attempted=4 passed=4 failed=0 infra_failed=0 skipped=0`与正常
`filesystem -> network -> device -> PowerOff`；日志为`build/net-udp-stage4-rv64.log`。

**Boundary and stop:** 最终source只修改4C最小subset中的`udp_topology.rs`、Socket-owned `udp/source.rs`和
`udp-test/src/main.rs`；source fix没有新增文件、owner或public surface。vendored smoltcp、adapter/protocol production
owner、ABI/library、task/files、fs/file、iomux/epoll implementation、config/rootfs、current contracts、`invariants.md`、
register/current limitations、其它RFC与scripts均无写入。Contract Impact为None；effective OPENED-DESC/IOMUX/EPOLL
与Network/control-plane IDs保持Preserve，全部Socket/Endpoint/UDP/wait candidate继续Pending。LA64 runtime、remote
external、hardware、`smp>1`、full network LTP与final harness均Not Run。Checkpoint 4C关闭Stage 4；transaction因
Stage 5仍是Outline而保持Active并立即停止。未运行`4 -> 5`resolution、激活Stage 5或执行current-contract cutover。

## 2026-07-31 - Stage 4 -> Stage 5 Implementation Resolution Gate

**Authorization and entry:** 开发者先要求只解析Stage 5并查看计划；read-only preflight完成后，开发者明确同意该路线并
授权开始落resolution文档。进入时为`dev/drc/alpha@d12364d7`、worktree clean。AGENTS/LOCAL、RFC
`index/invariants/implementation`、本transaction、RFC/contract workflow、current Network/Opened-description/IOMUX/
EPOLL/System Target contracts、register/current limitations、Stage 4 closure diff/evidence、live production owner、
QEMU target/platform、pretest wrapper/rootfs与`udp-test`均已重读。本授权只覆盖六个current-status/write-back文档和
docs validation；不覆盖source、build、host test、KUnit、QEMU、LTP、Stage 5 activation或contract cutover。

**Live baseline and audit:** Stage 4 closure commit为`5e3045f2`；current `d12364d7`在其后只把`udp-test`异步local
readiness evidence改为bounded wait，并为LA64 pretest rootfs补齐既有procfs mount point。该post-close correction没有
修改production kernel/protocol、owner、ABI/visible semantics或current contract，Stage 5以current tree而不是旧测试
source为baseline。live source确认production只使用initial-domain唯一`DomainStack`，logical ifindex/name仍由domain
owner唯一拥有；`udp-validation-probe`、per-netdev Stack、legacy ifindex projection、temporary dual wiring与Stage 3
blocking bridge均不存在。conditional host-validation facade仍有长期test consumer且kernel dependency关闭default
features，因此不是Stage 5应删除的production seam。没有Apollyon/Keter或active network register item阻塞resolution。

**Resolved route:** canonical [Stage 5 plan](../../rfcs/net-udp/implementation.md#65-stage-5-ready--not-active--externaldual-architecture-closure)
现将Stage 5解析为单一Checkpoint 5A。唯一source delta是同源`udp-test` remote-external roundtrip、一个host-loopback
UDP token/ack peer与两个canonical wrapper的peer lifecycle；kernel/protocol/ABI/config/rootfs保持只读。RV64由agent-run，
LA64由developer在同一exact source上user-run；guest PASS、peer PASS、17项summary与normal PowerOff必须同时满足。
host/Stage 4 evidence只在production source无漂移时复用，不重复运行无关build/QEMU gate。

**Cutover and claim boundary:** `NET-PROTOCOL-BOUNDARY-001`、`NET-SOCKET-ENDPOINT-001`、
`NET-UDP-TRANSACTION-001`与`NET-SOCKET-WAIT-001`继续Pending / Not Effective。Checkpoint 5A只有在RV64/LA64共同
evidence floor、final exact-diff review与docs validation全部满足后，才能在一个`NET-UDP-FINAL-CUTOVER`原子建立
`docs/src/contracts/net/udp-socket.md`并关闭RFC/transaction；partial success不执行部分functional cutover。R0 target、
owner、ABI/visible semantics、Contract Impact分类与acceptance boundary均未改变，不增加RFC修订。

**Document-gate result and stop:** authoritative Ready定义、review/observability/validation、LA64 handoff、停止/退出条件、
final contract surface和Resolved Write Set Manifest只保存在implementation 6.5；本entry不复制第二份计划。
current-status/anchor/write-set扫描、`git diff --check`与`mdbook build docs`通过；未运行build、host test、KUnit、QEMU、
LTP或architecture runtime。Stage 5达到Ready / Not Active；本resolution关闭后授权耗尽。下一步只能由新的明确授权
激活Checkpoint 5A，不得自动修改source、运行QEMU或执行current-contract cutover。

## 2026-07-31 - Stage 5 Checkpoint 5A activation preflight

**Authorization and entry:** 开发者以“完成Stage 5”为本轮唯一授权，明确允许执行Checkpoint 5A的source、双架构
runtime、review、validation、`NET-UDP-FINAL-CUTOVER`、write-back与单一`udp: ...`checkpoint commit；不得自动进入
后续gate。进入时为`dev/drc/alpha@79b40277`、worktree clean，该HEAD只在`d12364d7`之上提交已批准的docs-only
Stage 5 resolution。AGENTS/LOCAL、R0 `index/invariants/implementation`、本transaction、register/current
limitations、current Network/Opened-description/IOMUX/EPOLL/System Target contracts、live `udp-test`、两套pretest
wrapper/rootfs/SystemTarget/Platform与build owner均已重读；RFC没有`tracking-issues.md`，没有active network register
item或Apollyon/Keter阻塞activation。

**Frozen boundary:** authoritative [Stage 5 plan](../../rfcs/net-udp/implementation.md#65-stage-5-ready--not-active--externaldual-architecture-closure)
仍只允许`udp-test`增加同源remote-external case、新建窄host UDP peer、两个canonical wrapper拥有peer lifecycle，以及
6.5.9列出的final contract/write-back文档。`d12364d7..79b40277`没有production source变化；kernel/protocol、
`anemone-net-api`、smoltcp stack/vendored source、ABI/library、SystemTarget/Platform、rootfs与QEMU provider保持只读。

**LA64 execution correction:** 原Ready计划把LA64标为developer user-run，只因当时需要人工Docker环境。开发者在本次
activation中明确说明LA64现在也可由agent直接运行，无需人工Docker。该调整只改变validation执行归属：LA64仍使用与
RV64相同exact source、同一canonical wrapper和同一guest PASS、17项summary、peer PASS、normal PowerOff floor；不
降低external-path/architecture evidence，不改变R0 capability、owner、ABI/visible semantics、contract delta或
acceptance结果。最终transaction按agent-run记录实际provenance。

## 2026-07-31 - Stage 5 Checkpoint 5A LA64 mandatory-evidence stop

**Exact-source evidence:** Stage 5 source完成syntax/AST/formatter check后，RV64 canonical wrapper在当前candidate上
退出0；主日志`build/net-udp-stage5-rv64.log`包含`UDPTEST:PASS:remote-external-roundtrip`、
`UDPTEST:SUMMARY:PASS:17`、`EPOLLTEST:SUMMARY:PASS:11`、LTP whitelist `attempted=4 passed=4 failed=0
infra_failed=0 skipped=0`与正常`filesystem -> network -> device -> PowerOff`，peer日志
`build/net-udp-stage5-rv64-peer.log`包含READY与remote-external PASS。LA64运行前后的tracked Stage 5 source diff
SHA-256均为`e55c13e4f4442fc57940dba39abb25dca3ffa80f71106e35fd5726b9bb3e80e6`，新peer文件SHA-256为
`f23778b31dcbd89ec37cce16e1833510bc3b6ff6706bd1b0d927a59992185e9f`；没有production source漂移。

**LA64 failure:** agent按开发者修正后的执行归属运行同一LA64 canonical wrapper。首次sandbox尝试在QEMU前被
`no_new_privileges`阻止sudo；按`LOCAL.md`授权在sandbox外重跑后完成rootfs、app/kernel build、274/274 KUnit与
epoll 11/11。host peer日志`build/net-udp-stage5-la64-peer.log`包含READY和
`UDPPEER:PASS:remote-external-roundtrip`，证明request到达host且ack send成功；guest主日志
`build/net-udp-stage5-la64.log`却记录`UDPTEST:FAIL:remote-external-roundtrip:5`和
`UDPTEST:SUMMARY:FAIL:passed=16:failed=1`。该`EIO`来自bounded `ppoll`结果没有满足唯一ready/POLLIN predicate；
随后user-test因child失败panic，系统没有进入normal PowerOff。QEMU在保留日志后由agent通过monitor终止，该退出不计
作wrapper或shutdown PASS。

**Mandatory stop:** 该结果直接命中6.5.7的LA64 guest marker、summary与normal shutdown停止条件。Checkpoint 5A立即
停止；未运行final subagent review、`NET-UDP-FINAL-CUTOVER`、final docs validation或commit。四项Socket/Endpoint/
UDP/wait candidate全部保持Pending / Not Effective，RFC未关闭Stage 5且transaction保持Active。peer PASS不能
替代guest ingress/readiness proof，也不把target内失败登记为accepted limitation。继续需要独立Route Correction先
定位LA64 VirtIO provider ingress、worker progression与Socket readiness之间的断点；若真实修复涉及6.5.9外的kernel/
provider owner，必须先批准精确write-set扩展、contract影响和对应双架构回归，不能在当前冻结manifest内猜测修复。

## 2026-07-31 - Stage 5 LA64 interrupt-delivery Route Correction approval

**Root cause:** generated QEMU DT把PCI INTx 16-19以level-high交给PCH-PIC，并以base vector 0继续路由到EIOINTC。
live `loongson_platic.rs`的mask位语义与硬件相反，没有初始化PCH route/HT vector或EIOINTC route/enable，且root-domain
`claim/ack/eoi`尚未实现。VirtIO因此不能以external IRQ推进RX completion；protocol deadline偶然补做ARP后的request
egress解释了peer PASS，而ack到达后没有及时Stack pump解释了guest timeout。该组合把故障定位在LA64 interrupt owner，
不是UDP/Socket、peer token、timeout或wrapper lifecycle。

**Approved expansion:** 开发者明确批准把
`anemone-kernel/src/driver/intc/loongson_platic.rs`加入authoritative Stage 5 manifest，并允许按同一target继续。
RFC implementation 6.5.9/6.5.10已记录边界：只在既有QEMU LA64 PCH-PIC/EIOINTC root owner内修正初始化、mask/
unmask、claim/eoi与level-high translation；generic IRQ、machine descriptor、VirtIO/provider、protocol/Socket、ABI和
其它architecture source保持只读。Contract Impact为None，R0 target、visible semantics、acceptance与四项candidate
contract状态不变；不以更长timeout或periodic polling代替真实IRQ delivery。

**Validation consequence:** production source已进入candidate后，必须先重跑LA64 canonical wrapper，再在同一exact
source上重跑RV64 canonical wrapper；两者仍须同时满足guest remote PASS、17项summary、peer PASS、附带回归与normal
PowerOff。通过前不运行final cutover或commit；若需要第二个production owner，重新停止上报。

## 2026-07-31 - Stage 5 synchronous VirtIO block IRQ Route Correction approval

**Second root cause:** PCH-PIC/EIOINTC修正恢复PCI INTx后，LA64 canonical wrapper在guest test前持续进入hwirq 19。
QEMU PCI topology将source 19映射到首个VirtIO block device，两次monitor PC采样均落在中断flow的unmask路径；live
`virtio_blk.rs`确认该driver以`VirtIOBlk::read_blocks/write_blocks`同步轮询used ring，却仍request IRQ并安装空handler。
空handler不调用`VirtIOBlk::ack_interrupt()`，device ISR保持asserted，LevelFlow在每次eoi/unmask后立即重触发。该缺口
以前被未工作的LA64 root interrupt delivery掩盖；它不是UDP/Socket、peer、timeout或PCH route修正本身的失败。

**Approved expansion:** worker按6.5.7和上一entry的停止边界再次上报后，开发者明确批准继续。authoritative
[Stage 5 manifest](../../rfcs/net-udp/implementation.md#659-resolved-write-set-manifest)现加入
`anemone-kernel/src/driver/block/virtio_blk.rs`，只允许删除同步block driver的IRQ注册与空handler，并留下关键退出条件：
未来异步I/O只有在安装会acknowledge device ISR、并拥有completion/wake lifecycle的真实handler后才可重新request
IRQ。不新增异步路径、waiter或callback，不修改generic IRQ、block/VFS API、VirtIO transport、其它driver、protocol/
Socket或ABI。Contract Impact为None，R0 target、visible semantics、acceptance与四项candidate状态不变。

**Validation consequence:** production source再次变化后，先执行kernel formatter/static gate，再跑LA64 canonical wrapper，
通过后在同一exact source上跑RV64 canonical wrapper。两边仍须同时具备guest remote PASS、17项summary、peer PASS、
KUnit/epoll/LTP回归与normal PowerOff；通过前不运行final review、`NET-UDP-FINAL-CUTOVER`或commit。若仍需manifest外
production owner，按6.5.7再次停止。

## 2026-07-31 - Stage 5 Checkpoint 5A final evidence and cutover

**Final source and write-set audit:** 最终source/harness diff只包含`udp-test`同源remote-external case、planned
`scripts/net-udp-echo-peer.py`、两套canonical wrapper的peer lifecycle，以及两项经开发者分别批准的
`loongson_platic.rs`和`virtio_blk.rs` Route Correction。production protocol、Socket、opened-description、iomux/
epoll、shared/vendored stack、SystemTarget/Platform、rootfs、QEMU provider、ABI/library与其它architecture source均无
diff；没有第二route、queue、wake、Endpoint、binding、completion或IRQ truth。host validation facade的入口继续有
长期test consumer且kernel dependency保持`default-features = false`，因此case与peer脚本均作为长期validation资产
保留，不删除。

最终source/harness diff SHA-256为
`6a1853fa4b6d62297377498442722e3327fcea826cc2cf160f23293dc3ebd393`；new peer文件SHA-256为
`f23778b31dcbd89ec37cce16e1833510bc3b6ff6706bd1b0d927a59992185e9f`。`bash -n`、Python AST parse、
`just fmt kernel --check`、`just fmt udp-test --check`与`git diff --check`通过。Stage 4已通过且Stage 5未漂移的host/
no-default/independent build证据按6.5.6复用；canonical wrappers已经重建rootfs、app和release kernel，未在其前后重复
执行独立build或QEMU。

**LA64 canonical evidence:** 在第二项Route Correction后的final source上先运行
`./scripts/run-user-test-la64.sh etc/preliminary/images/sdcard-la.img build/net-udp-stage5-la64.log`。wrapper退出0；主日志
包含274/274 KUnit、`UDPTEST:PASS:remote-external-roundtrip`、`UDPTEST:SUMMARY:PASS:17`、
`EPOLLTEST:SUMMARY:PASS:11`、LTP whitelist `attempted=4 passed=4 failed=0 infra_failed=0 skipped=0`与完整
`filesystem -> network -> device -> PowerOff`标记；`build/net-udp-stage5-la64-peer.log`包含
`UDPPEER:READY:49153`和`UDPPEER:PASS:remote-external-roundtrip`。LA64当前没有电源驱动，guest在完成orderly
shutdown markers后进入halt；agent按开发者明确规则通过QEMU monitor `quit`结束launcher。这是当前target的标准host
收尾，不替代guest markers，也不把缺少machine poweroff误判为Stage 5失败。

**RV64 canonical evidence:** LA64完成后在未改变的同一source上运行
`./scripts/run-user-test-rv64.sh etc/preliminary/images/sdcard-rv.img build/net-udp-stage5-rv64.log`，wrapper自然退出0。主日志
同样包含274/274 KUnit、remote-external PASS、UDP 17/17、epoll 11/11、LTP 4/4与完整orderly shutdown；
`build/net-udp-stage5-rv64-peer.log`包含READY与PASS。guest marker与peer marker共同证明request经external route/
VirtIO egress到达host、ack经provider ingress和normal UDP demux返回；loopback/self-external case没有替代该proof。

**Claim boundary:** physical hardware、`smp>1`、full network LTP、final harness、任意非QEMU deployment和第一版能力
包络外Socket功能均Not Run / R0非目标。双架构single-NIC QEMU结果不外推这些轨道，也不新增accepted limitation。
register/current limitations经审计没有net-udp条目，保持只读。

**Final review and docs-only correction:** 上述source、contract与全部write-back形成candidate exact diff后，
本checkpoint唯一subagent按Apollyon/Keter/acceptance-affecting Euclid门槛完成只读审查。唯一reported finding是
`interface-domain.md`、`frame-path.md`、`netdev-lifecycle.md`与`attach-lifecycle.md`仍保留Stage 1-4的
LA64/virtio-pci/remote-external Not Run边界，与Stage 5 final evidence冲突，形成一个阻塞cutover诚实性的Euclid；
review没有要求代码、owner、public API、ABI或acceptance变化。

worker按6.5.7停止并报告精确范围、contract影响与validation计划后，开发者明确批准把这四页加入Stage 5 docs write
set。修正只更新最后核验、validation/enforcement和当前接受边界，并同步RFC Evidence Matrix/implementation；全部
contract rule、state owner、handoff、source与R0 target保持不变。开发者随后明确取消修复后的再次subagent review；
本地final exact-diff/write-set/current-status/anchor审计、`mdbook build docs`与whitespace gate承担最终确认，不重跑
build或QEMU。修正后该Euclid Neutralized，最终Apollyon 0、Keter 0、acceptance-affecting Euclid 0；没有剩余
cutover blocker。

**Final docs and exact-diff audit:** `mdbook build docs` PASS，只保留既有large search-index warning；
`git diff --check` PASS。两个planned new files分别执行`git diff --no-index --check -- /dev/null <file>`均无whitespace
诊断，exit 1只表示new-file diff。current-status、contract-ID、navigation、generated anchor、resolved write set与
register只读扫描一致；最终source/harness diff仍为
`6a1853fa4b6d62297377498442722e3327fcea826cc2cf160f23293dc3ebd393`，peer hash仍为
`f23778b31dcbd89ec37cce16e1833510bc3b6ff6706bd1b0d927a59992185e9f`，说明docs correction没有改变已验证source。
本地final review确认四份contract只修正evidence scope，未改写rule/owner/handoff/acceptance；没有剩余旁路、状态或
claim漂移。依据开发者决定，没有再次启动subagent，也没有重复build或QEMU。

**`NET-UDP-FINAL-CUTOVER`:** 本原子checkpoint建立
[Network UDP Socket current contract](../../contracts/net/udp-socket.md)，共同Introduce Active
`NET-PROTOCOL-BOUNDARY-001`、`NET-SOCKET-ENDPOINT-001`、`NET-UDP-TRANSACTION-001`与
`NET-SOCKET-WAIT-001`。既有Network/control-plane、OPENED-DESC、IOMUX、EPOLL与System Target contract保持
Preserve。RFC R0与Stage 5改为Closed，本transaction改为Completed；没有partial cutover、target revision、owner、
public API、ABI/visible semantics或acceptance boundary变化。
