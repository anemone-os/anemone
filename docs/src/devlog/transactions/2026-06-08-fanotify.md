# 2026-06-08 - Fanotify

**Status:** Active
**Owners:** doruche, Codex
**Area:** fs / VFS / syscall ABI / task-fd / iomux / LTP
**RFC:** [RFC-20260604-fanotify](../../rfcs/fanotify/index.md)
**Current Phase:** Gate D / D5 corrective boundary closed; D6 FID/name negative gate not started

## Scope

本事务跟踪 fanotify 子系统按 RFC gate 的 staged 实现。Gate A 已合并 D1 task/fd
通用能力与 D2 `fs::fanotify` owner module / group fd，使 Stage 0 + Stage 1 成为第一个
用户可见 gate。D3 mark registry 与 `fanotify_mark()` 已完成 Gate B；D4 path-fd read
提交协议与 Gate C corrective repair 已完成。D5 VFS 基础 event injection 已源码级闭合；
D6 FID/name negative gate 继续按依赖顺序后置。

本事务按 [Agent 编排建议](../../rfcs/fanotify/backgrounds/agent-orchestration.md)
推进，不按文档阶段机械拆分 worker。实现顺序以依赖拓扑为准：

- D1：task/fd 通用能力；
- D2：fanotify owner module、UAPI parser、group fd、bounded queue；
- Gate A review：确认 Stage 0 + Stage 1 合并 gate 闭合；
- D3：mark registry、target identity、basic mark parser 和 add/remove/flush；
- D4：path-fd read 提交协议、event fd reservation/commit/rollback、metadata copyout 和
  no-notify；
- D5：VFS 基础 event injection；
- D6+ 后续阶段暂不在本轮启动。

非目标：

- 不在 D4 实现真实 VFS enqueue、FID/name 正向 ABI 或 syscall 层 fanotify downcast。
- 不在 Gate A 宣称 LTP fanotify 基础用例通过。
- 不运行 QEMU / LTP，除非用户后续明确要求；运行态证据默认由用户提供。
- 不通过 fanotify 私有 fd number 状态、private nonblock mirror 或 fd-close bridge 来替代
  task/fd 通用能力。

## Invariants

- `fs::fanotify` 是 group、queue、registry、mark record、matching 和 file ops private
  state 的 owner；模块外只能通过 typed facade 交互。
- `fanotify_init()` 的用户可见成功只能在 Stage 0 + Stage 1 合并 gate 记录。
- group fd nonblock read 必须读取当前 opened file description 的 `FileStatusFlags`，
  不能固化成 fanotify private state。
- fd reservation / commit / rollback、opened-description final-release callback 和
  notification suppression 属于 task/fd 通用能力，不能由 fanotify 维护并行真相源。
- fanotify group fd 必须提供完整 `FileOps` vtable：read / poll / optional ioctl 走
  group fd file ops，seek / positioned I/O / read_dir fail closed。
- queue cap 和 overflow sentinel 必须早于真实 VFS enqueue。
- 每个 worker 只能写入编排文档指定 write set；需要扩大 write set 时必须停止上报。

## Handoff

**Last Updated:** 2026-06-10

**Current Branch:** `dev/drc/fanotify`

**Canonical RFC:** [RFC-20260604-fanotify](../../rfcs/fanotify/index.md),
[Invariants](../../rfcs/fanotify/invariants.md),
[Implementation Plan](../../rfcs/fanotify/implementation.md),
[Tracking Issues](../../rfcs/fanotify/tracking-issues.md)

**Completed:** Gate A 已合流 D1 task/fd 通用能力与 D2 `fs::fanotify` owner module。当前
实现包含 fanotify ABI constants、`fanotify_init()` / `fanotify_mark()` handler、anonymous
group fd、bounded queue、overflow sentinel、blocking/nonblock read、poll、semantic close/dead
wakeup、opened-description final-release hook、generic read-user hook、fd reservation /
commit / rollback 和 generic notification suppression marker。`/proc/<tgid>/fdinfo` 仍按
RFC 属于 Stage 5/fdinfo 后续。D3 已实现 mark registry、inode/mount/filesystem target key、
group cleanup handles、basic `fanotify_mark(ADD/REMOVE/FLUSH)` 和 legacy ignored mask storage。
D4 已在 fanotify read-user path 内落下单 record path-fd 提交协议和 event-fd
opened-description no-notify 证据。Gate C corrective repair 已修复 `FANOTIFY-041`：fd-table /
task `Drop` 不再触发 group semantic teardown，exit 和 fd-table replacement 显式 drain
opened-description final-release。D5 已接入基础 event hook：open 在 fd publish 前提交
`FAN_OPEN`，read/write syscall helper、sendfile、ftruncate/truncate/fallocate grow/O_TRUNC 的成功
边界提交 `FAN_ACCESS` / `FAN_MODIFY`，普通 opened-description final-release 提交
`FAN_CLOSE_WRITE` / `FAN_CLOSE_NOWRITE`；matching 仍由 `fs::fanotify` owner registry 统一处理
inode / mount / filesystem、自身 / parent-child、`FAN_ONDIR` 和 legacy ignore mask。内核内部
直接 `File` helper 与 `FileDesc::*` wrapper bridge 已从 fanotify 可见面排除，`fs::File` 不再
import fanotify 或持有 notification suppression policy。

**In Progress:** 暂无。FID/name negative gate 尚未启动。

**Open Blockers:** 当前无活跃 Apollyon / Keter。Gate A review 的三个 Keter 已修复：dead
group read 不再被 nonblock 误判成 `EAGAIN`；dead group poll HUP 不再被 caller interests
过滤；Gate A `fanotify_mark()` fail-closed placeholder 返回稳定 `EINVAL`，不再暴露
`ENOSYS`。Gate B review 未发现新的 Apollyon/Keter blocker；Gate C corrective review
确认 `FANOTIFY-041` 已 neutralized。Gate D review 发现并修复两个 Keter：`FAN_OPEN`
不再在 fd 发布后排队，legacy ignored mask matching / modify clearing 不再被 ordinary mark
mask gate 误限制。Gate D runtime corrective repair 又收窄 direct `File` helper 事件边界，
修复 ext4 loop mount 中 preemption-disabled 路径进入 fanotify `Mutex` 的 panic。`FANOTIFY-042`
corrective repair 已把 D5 基础事件源迁回 syscall/API helper 与 task/fd lifecycle，删除
`NoNotifyGuard` 和 `*_opened()` 桥。完整 fanotify profile 仍有 D6/Stage 5 语义和测试分类缺口。

**Next Action:** 启动 D6 FID/name negative gate 和 LTP 分类；不得把 FID/name 正向 ABI、
permission events 或 procfs fdinfo 合入 D6 negative gate。

**Do Not Redo:** 不要新增 `anemone-kernel/src/fs/api/fanotify/`；不要让 syscall / VFS /
task-fd / procfs 层 downcast fanotify private state；不要把 Stage 0 单独记为用户可见 gate；
不要用 private nonblock mirror、private fd reservation 或 fd-number close bridge 通过 Gate A。

## Phase Log

### 2026-06-08 - 事务日志启动与前置检查记录

**Phase:** orchestration / pre-audit

**Change:** 建立本事务日志，并把 [RFC-20260604-fanotify](../../rfcs/fanotify/index.md)、
事务索引、mdBook Summary 和当前双周 devlog 连接到同一条实现记录。

**Review:** 总控只读刷新当前落点：分支为 `dev/drc/fanotify`，前置检查时工作区干净；fanotify
代码侧仍只有 `SYS_FANOTIFY_INIT` / `SYS_FANOTIFY_MARK` syscall number；`fanotify.txt`
列出 `fanotify01` 到 `fanotify23`。`FileDesc` 已有 shared `FileStatusFlags`，但普通
`FileOps::read` 只接收 `&File`，fd table 尚无 reserve/commit/rollback、final-release
callback 或通用 notification suppression。anonymous inode、typed poll latch 和 VFS
open/read/write/truncate/close 集中点仍适合作为后续落点。

**Validation:** 本阶段只做文档启动和只读前置检查；未运行 `just build`、QEMU 或 LTP。

**Next:** 启动 Agent 1 与 Agent 2；不启动 Agent 3+。

### 2026-06-08 - Agent 1 D1 task/fd 通用能力

**Phase:** Gate A / D1

**Change:** Agent 1 在 task/fd 层落下 fanotify 下游共享能力，不新增 fanotify policy：

- `read()` / `readv()` 在 ordinary kernel-buffer fallback 前尝试 opened-description
  generic read-user hook；hook 接收当前 opened file description 的 `FileStatusFlags`、
  当前 task userspace 和一个或多个用户缓冲段。`pread*` / positioned read 不进入该 hook。
- `FilesState` 增加 fd reservation / commit / rollback。reserved slot 占用 allocator
  bitmap，但在 commit 前 `fds[fd] == None`，因此普通 fd allocation 不会复用，`get_fd()`、
  close 和 fd snapshot 也不可观察；commit 只发布已准备好的 `FileDesc`，rollback 幂等清理。
- opened-description final-release 使用 fd-table published-slot refcount，而不是
  `Arc<FileDesc>` transient ref 或 fd-number close bridge；dup/fork 共享同一个 `ProcFile`，
  最后一个 published slot 关闭后在 fd-table 锁外运行 callback。
- notification suppression 是 generic opened-description marker，task/fd 核心只保存和暴露
  capability，不检查 fanotify concrete type。

**Review:** D1 未修改 `fs::fanotify`，也未新增 fanotify-specific downcast。普通 `open_fd()`
继续使用 empty description ops；需要特殊 opened-description 行为的下游必须显式调用
`open_fd_with_description_ops()` 或用 `FileDesc::new_opened()` 配合 fd reservation commit。
final-release callback 在锁外执行，避免后续 fanotify teardown 受 fd-table 锁序限制。

**Validation:** `git diff --check` 通过。`just fmt kernel --check` 未通过，但失败包含既有
非 D1 文件格式噪声（`lwext4-rust`、generated `kconfig_defs.rs` / `platform_defs.rs`、
`task/sig`）；Agent 1 仅按 formatter 建议手工收敛了本次 `task/files.rs` 格式。sandbox
内 `just build` 在 `lwext4_rust` C 编译触发已知 `Bad system call`。非 sandbox `just build`
越过该问题后，当前失败来自 Agent 2 write set 的 `fs/fanotify/api/{init,mark}.rs` 缺少
`TryFromSyscallArg` import；未暴露 D1 代码错误。

**Next:** Agent 2 修复 owner module 编译后，合流运行 Gate A review；重点检查 read-user hook
是否被 fanotify group fd 使用、`FAN_NONBLOCK` 是否读取 current `FileStatusFlags`、reservation
commit/rollback 是否覆盖 path-fd read 提交协议的需要，以及 final-release 是否支撑 group
semantic teardown。

### 2026-06-08 - Agent 2 D2 fanotify owner module 与 group fd

**Phase:** Gate A / D2

**Change:** Agent 2 在 `fs::fanotify` owner module 中落下 Stage 0+1 首批可见能力：

- `anemone_abi::fs::linux::fanotify` 增加 fanotify init flags、mark flags、event masks、
  metadata constants、`FanotifyEventMetadata` 和 `FanotifyResponse`。
- `anemone-kernel/src/fs/fanotify/` 新增 owner module 骨架，包含 `api/`、`types.rs`、
  `group.rs`、`file.rs`、`event.rs`、`queue.rs`、`registry.rs`、`mark.rs` 和 `hooks.rs`。
- `fanotify_init()` 解析支持 Gate A init flag matrix，拒绝 FID/name、pidfd、audit、
  unlimited queue/marks 等暂缓 init flags；privileged group creation 使用 anonymous inode
  group fd，并通过 D1 `open_fd_with_description_ops()` 安装 read-user 和 final-release hook。
- group fd 提供 read-user、poll、`FIONREAD`、fail-closed vtable 和 final-release teardown。
  `FAN_NONBLOCK` 读取当前 opened-description `FileStatusFlags`，不保存 fanotify private mirror。
- queue 固定 `DEFAULT_MAX_EVENTS = 16384`，队满时发布单个 overflow sentinel，后续真实 VFS
  enqueue 前已有资源上限和可观测 overflow 边界。
- `fanotify_mark()` 在 Gate A 只做 parser / feature gate；mark registry storage 留给 D3。

**Review:** D2 没有新增 `fs/api/fanotify/`，fanotify group fd private-state downcast 限定在
`fs/fanotify/file.rs`。`hooks.rs` 只提供 typed no-op facade，未接入真实 VFS enqueue。
`registry.rs` 和 `mark.rs` 是 owner placeholder，不保存 mark state。

**Validation:** Controller 当前环境中 `just build` 可正常通过；旧的 sandbox
`lwext4_rust` `Bad system call` 记录不作为当前编译缺口。Gate A 合流后重新运行
`git diff --check` 和 `just build`，均通过；build 仅剩既有 `sync/mono.rs` unused-import
warning。

### 2026-06-08 - Gate A review 修复

**Phase:** Gate A / review

**Review:** 只读 reviewer 针对 D1+D2 diff 和 RFC Gate A 条件发现三个 Keter：

- dead group read state 与 live-empty state 混在一起，导致 nonblock read 可能在 group dead
  后返回 `EAGAIN` 而不是 EOF。
- fanotify `poll()` 把 `HANG_UP` 按 caller interests 过滤，`ppoll(POLLIN)` 可能看不到
  close/dead readiness。
- `fanotify_mark()` 对 known/deferred mask 走 `NotYetImplemented` placeholder，映射成
  `ENOSYS`，不符合 RFC 稳定 fail-closed errno matrix。

**Change:** Controller 在 Gate A write set 内修复：

- `FanGroup::pop_read_state()` 返回 `FanReadState::{Event, Empty, Dead}`，`fanotify_read_user()`
  先把 `Dead` 映射为 EOF，再对 live-empty + current `NONBLOCK` 返回 `EAGAIN`。
- blocking read waiter 从 `yield_now()` busy loop 改成 waiter-owned `Latch`；read trigger 在
  group lock 下注册，并在注册后同锁重扫 queue/dead，enqueue/dead detach 后在锁外 trigger。
- `FanQueue::poll()` 只按 interests 控制 `READABLE`，dead `HANG_UP` 无条件进入 revents，
  因此 dead group 不会重新 arm 一个不可触发 waiter。
- registry / mark placeholder 返回 `SysError::InvalidArgument`，并用注释记录 Gate A 已有
  syscall entry 但 mark storage 语义仍延后到 D3。
- `fs/fanotify/mod.rs` 对 Gate A 暂未被外部消费的 typed facade re-export 添加局部
  `allow(unused_imports)`，保留 RFC 约定的 owner-module facade，同时消除 fanotify 自身
  build warning。

**Residual Risk:** Reviewer 记录一个 Euclid：vectored metadata copy 在 D4 path-fd submit
协议前仍可能在后续 segment fault 时留下已 copy 的字节。Gate A synthetic `FAN_NOFD`
路径不把它作为 blocker；D4 必须在 event fd 安装、metadata copyout 和 rollback 协议中
系统性处理。

**Recheck:** Reviewer 复核确认三个 Keter 已关闭，结论为：Gate A 无 Apollyon / Keter
blocker。Euclid 残留仍按 D4 path-fd read submit gate 处理，不阻塞本 Gate。

### 2026-06-08 - Gate A validation

**Phase:** Gate A / validation

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合本轮 Gate A 范围和用户要求。

**Result:** Gate A 代码层面闭合：Stage 0+1 首个用户可见 gate 具备 task/fd 通用能力、
`fs::fanotify` owner module、group fd read/poll/close wakeup、bounded queue 和 stable
fail-closed mark placeholder。D3+ 未启动。

### 2026-06-08 - Gate A follow-up review cleanup

**Phase:** Gate A / review cleanup

**Review:** 用户要求把能自然修复的 Gate A follow-up 直接收口，无法在当前阶段干净抽象的项用注释记录边界。复核结论仍为无 Apollyon / Keter blocker；本轮只处理 D1/D2 边界内的问题，不启动 D3+。

**Change:**

- `fanotify_mark()` 在返回 Gate A registry-deferred `EINVAL` 前先通过当前 task fd table 取得 `fanotify_fd` 并确认它是 fanotify group fd；坏 fd 现在保持 task/fd 层 `EBADF` 边界，非 fanotify fd 仍 fail closed。
- `fs/fanotify/mod.rs` 不再 re-export concrete `FanEvent` / `FanGroup`，只保留 syscall、hook 和 mask typed facade，避免 D3/D5 误用 group lifecycle / enqueue internals。
- fanotify group fd final-release 不再因为 generic notification suppression 跳过 teardown；先执行 `mark_dead()`，再用断言暴露 group fd 被错误 suppression-mark 的 owner-boundary bug。
- `FanPollTrigger::interests` 和 `FanQueue::dropped_events` 加注释标明当前只服务诊断 / telemetry，不驱动 readiness 或 queue 行为。
- `fanotify_read_user()` 加注释记录 Gate A consume-before-copy 只因 synthetic `FAN_NOFD` 安全；D4 path-fd read 必须用 RFC 的 fd reservation、metadata copyout、commit / rollback 协议替换。
- RFC index 的下一步同步为 Gate A closed / D3 not started。

**Residual Risk:** D4 path-fd read 提交协议仍未实现，本轮只记录边界，不提前补一个会被 D4 拆掉的临时 copyout abstraction。

### 2026-06-08 - Agent 3 D3 mark registry 与 fanotify_mark

**Phase:** Gate B / D3 implementation

**Write Set Expansion:** 用户批准 D3 write set 仅增加
`anemone-kernel/src/fs/fanotify/file.rs`。原因是 `fanotify_mark()` 需要从 group fd 的
owner-module private state 取得 `Arc<FanGroup>`，原 D3 write set 若在 syscall 层绕行
会迫使 `api/mark.rs` downcast private state，违反 `fs::fanotify` owner-module contract。
扩展范围限定为 `file.rs` 内部窄 helper `group_from_file(&File) -> Result<Arc<FanGroup>,
SysError>`；downcast 仍只存在于 `fs/fanotify/file.rs`，syscall / VFS / task-fd / procfs
层不访问 fanotify concrete private state。contract/gate 影响：保持 owner-module facade
边界，允许 D3 registry 绑定 group id / generation / weak handle；不触碰 D4 path-fd read、
VFS hook、task/fd reservation、read/write syscall 或 procfs。

**Change:**

- `FanTarget::{Inode, Mount, Filesystem}`、`FanTargetKey::{Inode, Mount, SuperBlock}` 和
  `FanPathKey { mount, dentry }` 落到 `types.rs`，集中封装 target/path identity。
- `FanMarkRecord` 成为 mark state 唯一 owner，保存 event mask、ignored mask、
  `ignored_survives_modify`、target strong ref、`target_dead`、group id/generation 和 weak
  group handle；target map 和 group cleanup list 只保存 `MarkHandle`。
- `registry.rs` 建立全局 registry lock、slot map 和 `marks_by_target`，实现
  ADD / REMOVE / FLUSH 的同锁线性化策略。group teardown 先按 registry -> group 顺序标记
  dead 并删除 mark records，释放 registry lock 后再清 group queue / 触发 waiters，避免
  registry lock 包住后续可能变重的 queue drop。
- `group.rs` 增加 group generation、mark cleanup handle list 和 registry-owned
  teardown helpers；group state 不复制 mask、target refs、target_dead 或 mark generation。
- `fanotify_mark()` 在进入 registry 前完成 command/target/mask validation，显式处理
  `FAN_MARK_INODE == 0`，支持 inode / mount / filesystem target、`ONLYDIR`、
  `DONT_FOLLOW`、`EVENT_ON_CHILD`、`ONDIR` 和 legacy `FAN_MARK_IGNORED_MASK` /
  `FAN_MARK_IGNORED_SURV_MODIFY`。`FAN_MARK_IGNORE`、`FAN_MARK_EVICTABLE`、permission、
  FID/name/dirent/self/metadata、FS error 和 exec-open masks 均返回稳定 `EINVAL`，不进入
  registry。mount / filesystem mark 需要 `CAP_SYS_ADMIN`。
- path resolution 遵守 `pathname == NULL` 时以 `dfd` 作为目标、相对路径以 directory `dfd`
  为 base、`DONT_FOLLOW` 不跟随最后一级 symlink、`ONLYDIR` 对最终 target 返回 `ENOTDIR`。

**Review:** 本地自审未发现 mark state 第二真相源、模块外 downcast、fd-number state、
tracking issues 进度表化或 D4 范围越界。`FAN_MARK_IGNORE` 新语义未被 legacy ignored-mask
路径接受。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合用户要求。

**Residual Risk:** D5 matching 尚未接入，`ignored_survives_modify == false` 的 modify 后清除
只能等 VFS `FAN_MODIFY` hook 调用 registry matching/ignore 逻辑时验证。首批 target
lifecycle 仍按 RFC 接受的 strong-ref + pre-unmount flush / mark-dead 口径；完整 umount /
filesystem kill 清理由后续 VFS lifecycle hook gate 负责。`fanotify04`/手工
add/remove/flush runtime smoke 未在本阶段运行。

### 2026-06-08 - Gate B review 修复

**Phase:** Gate B / review

**Review:** 总控源码 review 和只读 reviewer 均未发现 D3 的 Apollyon / Keter blocker。只读
reviewer确认 mark state 由 `FanMarkRecord` 单一拥有，target map 与 group cleanup list
只保存 `MarkHandle`；registry 使用 weak group handle；ADD / REMOVE / FLUSH 共用同一
registry lock；`FAN_MARK_IGNORE` 保持 fail-closed。

**Change:** Controller 在 D3 write set 内做两处 review cleanup：

- `fanotify_mark(pathname == NULL)` 不再把 `dfd == AT_FDCWD` 解释为 cwd。Linux 在该形式下
  直接把 `dfd` 当作被 watch 的 object fd；`AT_FDCWD` 不是有效 object fd，因此现在返回
  `EBADF`，而相对 pathname 解析仍保留 `AT_FDCWD == cwd` 语义。
- `FanMarkRecord` 不再另存一份 group id / generation；这两个诊断和匹配身份从
  `MarkHandle` 派生，并在 record 构造时用 `assert!` 校验 handle 与 group 一致。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合当前 Gate B 范围。

**Result:** Gate B 代码层面闭合。运行态 `fanotify04` / add-remove-flush smoke 仍待后续用户
或 dedicated runtime 验证；D4 可在干净基线后启动。

### 2026-06-08 - Agent 4 D4 path-fd read 提交协议

**Phase:** Gate C / D4 implementation

**Change:** Agent 4 在 D4 write set 内实现 fanotify read-user path 的 path-fd 提交协议，
未接入真实 VFS hook，未实现 FID/name 正向 ABI，也未在 syscall 层 downcast fanotify
private state：

- `FanEvent` 增加 path target；synthetic 与 overflow event 继续固定 `FAN_NOFD`，不会进入
  path-fd open / fd commit 协议。
- `fanotify_read_user()` 在 group lock 下完成 empty/nonblock/dead 判断并选择完整 event；
  事件出队后释放 group lock，再执行 object open、fd reservation 和用户 copy。
- path event read 使用 task/fd `reserve_fd()` 取得稳定但未发布 fd number；metadata record
  全部 copyout 成功后才 `commit()`，copyout 失败依赖 `FdReservation` drop rollback，不留下
  用户不可见的新 fd。
- object open 失败时记录 debug log 并输出 `fd = FAN_NOFD`，继续 copyout；fd reservation
  失败则作为 read 错误返回，因为没有稳定 fd number 就不能构造 path-fd metadata。
- event fd 按 group `event_f_flags` template 构造 access/status/fd flags，并安装 generic
  opened-description notification suppression marker；fanotify-local `NoNotifyGuard` 只包住
  内部 event-object open，后续 VFS hook 通过 generic marker suppress returned event fd。
- metadata copyout 先在 userspace lock 下验证完整 record 覆盖的所有 segments，再写入，避免
  第二个 iovec fault 时留下半个 metadata record 与已保留 fd 的不一致状态。

**Review:** 本地自审确认 D4 未触碰 D3 owner state（`api/mark.rs`、`registry.rs`、`mark.rs`），
未修改 VFS hook call sites、fdinfo、procfs 或 `fs/api/fanotify/`。新增注释集中记录 event
target 不是 fd identity、reserved fd number 只有 metadata copyout 成功后才发布、
construction-time no-notify 与 returned event fd generic marker 的边界，以及 copyout
先验验证的 rollback 依据。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合 D4 worker 指令。

**Residual Risk:** D4 当前只证明 read-user 提交协议和 synthetic/internal path event helper，
没有真实 VFS enqueue 触发路径；D5 matching/VFS hook 接入后仍需复核 no-notify facade 是否被
所有 event hook 检查。首批 read 只提交一个 metadata record；后续如果扩展 batching，必须保持
每个 record 的 fd reservation / copyout / commit 边界。

### 2026-06-08 - Gate C review cleanup

**Phase:** Gate C / review

**Review:** 总控源码 review 发现 path-fd event object reopen 复用了 group `event_f_flags`
template，但缺少 ordinary open 的对象类型相关 status validation。

**Change:** `open_event_target()` 现在在 read 边界按 queued target 重新校验 event fd status
flags；当前与普通 open 对齐，拒绝 block inode 上的 `O_DIRECT`。这是 read-time 校验，因为
`fanotify_init()` 只能验证 flag template，不能提前知道后续 event target 类型。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合当前 Gate C 范围。

**Result:** Gate C 代码层面闭合。只读 reviewer 确认 read/readv 仍为 generic dispatch、
private cast 仍在 `fs/fanotify/file.rs` 内、event fd reserve/copyout/commit 顺序正确、
object open failure 输出 `FAN_NOFD`、synthetic/overflow 仍不进入 path-fd open。D5 仍需在真实
hook 接入时复核 construction-time guard 和 returned-fd generic suppression marker。

### 2026-06-08 - Gate C follow-up Euclid cleanup

**Phase:** Gate C / review cleanup

**Review:** 多 agent 复核 `HEAD~2..HEAD` 未发现 D3/D4 的 Apollyon / Keter blocker。确认的
两个 Euclid 是：RFC index 的下一步仍停在 Gate A / D3；`FanEvent` metadata `pid` 默认使用
`tid()`，但当前 gate 拒绝 `FAN_REPORT_TID`，默认 ABI 应报告 tgid。

**Change:**

- RFC index 的下一步更新为 Gate C closed / 等待用户确认后启动 D5 VFS 基础 event injection，
  并明确不得把 FID/name 正向 ABI、procfs fdinfo 或 permission events 合入 Gate C。
- `FanEvent` metadata 默认 `pid` 改为当前 task `tgid()`。现有 task identity API 已足够表达
  默认 Linux fanotify ABI，因此本轮不引入新的 report-mode interface；注释记录若后续启用
  `FAN_REPORT_TID`，应把 pid/tid 选择提升为 group report-mode 决策。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未运行，符合当前 review cleanup 范围。

### 2026-06-09 - Gate C runtime regression: teardown from deferred drop

**Phase:** Gate C / runtime corrective blocker

**Evidence:** 用户在 D4 完成后的 fanotify runtime smoke 中运行 `fanotify07`。permission event
仍按 RFC 暂缓边界在 `FAN_ACCESS_PERM` mark 处返回 `EINVAL`，LTP 报
`TCONF: CONFIG_FANOTIFY_ACCESS_PERMISSIONS not configured in kernel?: EINVAL (22)`，这是当前
预期的 unsupported feature 分类。但 testcase 退出后内核 panic：
`fanotify/registry.rs:218: Mutex cannot be locked when interrupts are disabled`。

**Analysis:** panic 不是 permission feature gate 失败，而是 group fd semantic teardown 的
调用上下文错误。`fanotify_final_release()` 在 opened-description final-release callback 中
执行 `mark_dead()`，后者进入 fanotify registry 普通 `Mutex`。显式 `close(fd)` 路径通常在
可睡眠 syscall 上下文、fd-table lock 外运行 callback；但进程退出后 task / fd table 释放可能
由 deferred task dispose 或 scheduler/trap cleanup 执行，这些位置可能处于 interrupts-disabled
上下文。把 final-release 语义释放留给 `FilesState::drop()` / task memory drop 会让 fanotify
teardown 从不可睡眠上下文拿普通 `Mutex`，违反 task/fd lifetime contract。

**RFC Correction:** 已将 RFC contract 收紧为：opened-description final-release 可以继续作为
semantic teardown 的抽象事件，但它必须由 task/fd 层的显式生命周期路径触发；`Drop` /
memory last-drop / deferred task dispose 只能释放内存或暴露遗漏清理的 bug，不能执行需要锁、
唤醒或 registry mutation 的系统资源释放。

**Tracking:** 新增 `FANOTIFY-041`，作为 D5 前必须修复的 Gate C corrective blocker。修复应优先
落在 task/fd 层：在 exit、fd-table replacement / unshare 等显式路径 drain published fd
refs 并运行 final-release；`FilesState::drop()` 不再调用 `release_description_ref()`，只断言
table 已被显式清理干净。fanotify 侧可保留 final-release hook，并在入口断言当前为
interrupts-enabled / 可睡眠上下文。

**Validation Target:** 修复后 `fanotify07` 可以继续因 permission events 暂缓而 `TCONF`，但
不得 panic。`unknown syscall number 123` 单独归类为 `sched_getaffinity` 兼容缺口，不作为本
corrective blocker 的因果链。

### 2026-06-09 - FANOTIFY-041 corrective repair

**Phase:** Gate C / corrective repair

**Change:** 实现 worker 将 opened-description semantic teardown 从析构兜底移到 task/fd
显式生命周期路径：

- `FilesState::drain_all_published_fds()` 在 whole fd-table 边界摘除 published slots，清理
  allocator / reserved state，并把 `ProcFile` refs 返回给调用者在锁外执行 final-release。
- `FilesState::drop()` 不再调用 `release_description_ref()`，只断言 fd table 已通过显式路径清空。
- `Task::close_all_fds_for_exit()` 在 interrupts-enabled、sleepable context 中替换为空 fd table，
  并只在旧 `Arc<RwLock<FilesState>>` 是最后一个 strong ref 时 drain，避免误关仍由
  `CLONE_FILES` 共享的 fd table。
- `kernel_exit()` 在 deferred task dispose 和 zombie 尾部前显式调用 `close_all_fds_for_exit()`。
- fd-table replacement / unshare 和 clone 发布前失败路径不再把 cleanup 留给 task / fd table
  `Drop`。
- fanotify group fd final-release 入口断言非 notification-suppressed、interrupts enabled 且
  sleepable，再进入 `mark_dead()`。

**Review:** review agent 对当前 diff 做只读复核，结论为无 Apollyon / Keter blocker。
`Arc::strong_count == 1` 被记录为 Euclid 级残余风险：它是保守 ownership proxy，不是精确
task-owner 计数；当前未发现正常路径会让非 owner observer 稳定跨越最后 task-owner exit。
若未来出现该路径，`FilesState::drop()` 会断言暴露遗漏，而不会在 Drop 中静默执行 fanotify
teardown。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；build 输出只剩既有 `anemone-kernel/src/sync/mono.rs` unused-import
  warning。
- QEMU / LTP：未由 agent 运行。用户侧 `fanotify07` 期望可继续因 permission events 暂缓而
  `TCONF`，但不应再触发 deferred-drop teardown panic。

**Result:** `FANOTIFY-041` 已在 tracking issues 中 neutralized；Gate C corrective repair
源码级闭合。D5 仍需等待用户确认后启动。

### 2026-06-09 - D5 VFS 基础 event injection

**Phase:** Gate D / D5

**Decision:** 用户批准启动 D5，并批准将 `anemone-kernel/src/fs/api/fallocate.rs` 纳入 D5
write set。原因是当前 `fallocate` grow path 会通过 `inode.truncate(end)` 改变可见文件大小，
属于 RFC 中 “truncate 或等价内容修改路径” 的基础 `FAN_MODIFY` 边界；若跳过它，D5 会留下
已知内容修改漏报点。扩展影响限定为成功 grow 后派发基础 modify event，不改变 `fallocate`
errno、allocation 语义或 Stage 5 backlog。

**Design Boundary:** D5 采用非侵入式两层设计：`fs::fanotify` owner module 内实现 matching /
enqueue facade，VFS / task-fd 层只提交事实输入。外部 hook 不访问 registry、group、queue 或
mark internals，不打开 event fd、不 copy userspace buffer、不等待 permission response。

**Change:**

- `fs::fanotify::notify_path_event()` 负责 no-notify suppression、registry snapshot、
  inode / mount / filesystem matching、self / parent-child matching、`FAN_ONDIR` 和 legacy
  ignored-mask 处理。legacy ignored mask 与 ordinary mark applicability 分开计算；non-surviving
  ignored mask 在 `FAN_MODIFY` 上先清除，再决定当前 modify 是否被 ignore。
- `openat::finish_open()` 在成功打开普通对象后用 fd reservation 建立 unpublished
  `FileDesc`，先派发 `FAN_OPEN`，再用不可失败 commit 发布 fd；这避免 shared `CLONE_FILES`
  peer 在 fd 发布窗口内抢先 close 导致 `FAN_CLOSE_*` 先于 `FAN_OPEN`。
- read / positioned read 成功读取后派发 `FAN_ACCESS`；write / positioned write / append /
  truncate / fallocate grow 成功内容修改后派发 `FAN_MODIFY`。
- `FAN_CLOSE_WRITE` / `FAN_CLOSE_NOWRITE` 来源保持 opened-description final-release，不引入
  fd-number close bridge。

**Review:** Parfit 做只读 Gate D recheck，确认两个 Keter 已关闭：`FAN_OPEN` 现在发布 fd 前
排队；legacy ignored mask matching / modify clearing 不再依赖 ordinary mark mask 命中。复核未发现
新的 D5 blocker：VFS hook path 仍只提交事件事实，没有 event-fd open、userspace copy 或
permission wait；fanotify private downcast 仍限定在 `fs/fanotify/file.rs`，syscall / VFS /
task-fd 路径使用 typed facade。

**Accepted Deferred Semantics:** 按用户确认，高修复代价竞态或 ABI 强一致性缺口允许记录后置。
D5 保留以下 residual boundaries，不作为 Gate D blocker：

- 运行态 QEMU / LTP 未由 agent 执行；本 gate 只有 source-level + build + reviewer 证据。
- `fanotify_read_user()` 仍是单 record per read；multi-record batching 已作为后续 read-user
  行为缺口记录，不能用本 D5 closure 声明 `fanotify01` 全量运行通过。
- 内核内部 direct `File` helper 与用户可见 fd I/O 的 fanotify 可见性边界已在后续 runtime
  corrective repair 中收窄；direct helper 不再作为 fanotify event source。
- `O_TRUNC` 当前先提交 `FAN_MODIFY`，再提交 `FAN_OPEN`；更精确 Linux event ordering 需要
  更细的 open/truncate 事务建模，暂缓到运行态日志或 merge/order gate。
- legacy ignore mask 已覆盖 D5 所需的基础 matching / modify clearing；`FAN_MARK_IGNORE`、
  evictable marks、已有 writer 时 add ignore mask 的 no-op、完整 mark merge/order 和 fdinfo
  仍属 Stage 5 backlog。

**Validation:**

- `git diff --check`：通过。
- `just fmt kernel --check`：失败仅剩 generated `anemone-kernel/src/kconfig_defs.rs` 与
  `anemone-kernel/src/platform_defs.rs` 的既有格式噪声；D5 touched source 不再出现在 diff。
- `just build`：通过；build 仅剩既有 `anemone-kernel/src/sync/mono.rs` unused-import warning。

**Result:** Gate D / D5 源码级闭合。D6 FID/name negative gate 尚未启动。

### 2026-06-09 - D5 runtime lock-order corrective repair

**Phase:** Gate D / D5 corrective repair

**Evidence:** 用户侧 `build/user-test-rv64.log` 在 `fanotify01` 后续 ext4 loop mount 阶段触发
panic：`fs/fanotify/mod.rs:74: Mutex cannot be locked when preemption is disabled`。用
`build/anemone.elf` 符号化后，栈为 ext4 mount / lwext4 block read -> loop backing
`BackingFileHandle::read_exact_at()` -> `File::read_at()` -> `File::read_at_with_notify()` ->
`fs::fanotify::notify_path_event()` -> `current_task_notifications_suppressed()`，最后进入
fanotify no-notify guard 的普通 `Mutex`。

**Correction:** D5 原 residual boundary 曾记录 “`File` direct-call helper 也会触发
read/write hook，内部 I/O 与用户 fd I/O 的精确可见性差异后置”。运行态栈证明这不是可暂缓的
ABI 差异，而是会把 fanotify sleepable lock 带入 preemption-disabled backend I/O 的锁序错误。
本修复收窄事件面：用户可见 fd I/O 仍通过 `FileDesc::*` 调用 `File::*_opened()` wrapper
产生 `FAN_ACCESS` / `FAN_MODIFY`；直接 `File::{read,read_at,write,write_at,append,
append_at_current_end}` 作为内核内部 handle 路径，统一 suppress fanotify。`notify_fanotify()`
在 suppression 为 true 时本地返回，不再构造 event 或查询 fanotify-local no-notify `Mutex`。

**Design Note:** 本轮没有把 `NO_NOTIFY_GUARDS` 改成 task-local / preempt-safe 状态。该方向会扩大
task/fanotify 状态模型，而当前 blocker 的根因是 hook 边界过宽。若后续仍发现必须在
preemption-disabled 上下文提交用户可见事件，再按新的 write set 扩展评估 task-local
suppression 或 lock-free 查询方案。

**Validation:**

- `git diff --check`：通过。
- `just build`：通过；dev build 无新增 warning。
- `just fmt kernel --check`：仍只失败在 generated `kconfig_defs.rs` / `platform_defs.rs`
  既有格式噪声；本轮 touched source 未出现在 rustfmt diff。
- `mdbook build docs`：通过。
- `./scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64.log`：
  通过脚本完成 rootfs rebuild、release kernel build、QEMU 和 fanotify profile。日志中未再出现
  `panic`、`Mutex cannot be locked`、`preemption is disabled` 或 `Kernel panic`；glibc 与 musl
  均完整跑完 fanotify01-23，summary 分别为 attempted=23 passed=4 failed=19，
  合计 attempted=46 passed=8 failed=38。失败仍来自已知 D6/Stage 5 缺口或测试分类问题：
  multi-record read、permission/FID/pidfd/evictable/debugfs unsupported 被 harness 记为 fail、
  bind mount/overlay/fdinfo/proc sysctl 等后续语义。

### 2026-06-10 - Post-D5 cleanup and review

**Phase:** Gate D / post-D5 cleanup

**Scope:** 用户要求在 D5 已完成后清理会干扰后续阶段的遗留内容。本轮只处理 canonical
RFC entry 和 `fs::fanotify` owner module 内的必要清理；`FanPathKey` 按用户要求保留，等待后续
单独审查。背景材料中的历史编排文字保持原样，不作为当前执行状态来源。

**Change:**

- RFC entry 把 “当前只有 syscall number、缺少 handler / group fd / registry / VFS dispatch”
  改写为 RFC 提升时的历史背景，并补充 D5 后的源码级基线与 D6 FID/name negative gate 边界。
- `event.rs` 去掉未使用的 `FanEventKind`、`FanEvent.kind` 和 `FanEvent::kind()`；queue item
  是否需要 event fd 由 `FanEventTarget::{NoFd, Path}` 直接表达。
- `file.rs` 去掉未使用的 `enqueue_synthetic()`，并把 path-fd read submit 与 permission-response
  注释改成当前不变量语义。
- `group.rs` 去掉未使用的 `init_flags` group 存储、getter 和构造参数；`FAN_NONBLOCK` /
  `FAN_CLOEXEC` 仍只在 fd 安装边界映射到 opened-description status / fd flags。
- `queue.rs` 去掉未使用的 `FanQueue::is_empty()`；`types.rs` 去掉未使用的
  `FanTarget::class()`，但不触碰 `FanPathKey`。
- `mod.rs` 和 `registry.rs` 中旧的 D4/D5/Gate A 注释改成 construction-time no-notify guard 与
  registry-lock matching 语义；保留 `group_from_file()`、`open_group_file()`、
  `description_ops()`、`notify_path_event()` 和 `observed_file_description_ops()` 这些真实 facade。

**Review:** 主控复审确认本轮是行为保持清理，没有新增 syscall ABI、registry ownership、
queue lifetime 或 VFS hook 语义。`api::*` re-export 只经 `api/mod.rs` 暴露两个 syscall handler，
没有把 raw ABI parser types 扩到模块外。未发现新的 Apollyon / Keter / Euclid blocker。

**Validation:**

- `rg`：确认 `FanEventKind`、`FanEvent::synthetic`、`enqueue_synthetic`、`init_flags()`、
  `_init_flags`、`FanQueue::is_empty`、`FanTarget::class` 以及 owner-module 当前注释中的
  `Stage 1` / `D4` / `Gate A` 残留均已清理；`FanPathKey` 仍保留。
- `git diff --check`：通过。
- `mdbook build docs`：通过。
- `just build`：通过；仅剩既有 `anemone-kernel/src/sync/mono.rs` unused-import warning。

**Result:** Post-D5 cleanup 源码级闭合。D6 FID/name negative gate 仍未启动。

### 2026-06-10 - FANOTIFY-042 corrective repair

**Phase:** Gate D / D5 corrective repair

**Issue:** `FANOTIFY-042` 指出 D5 基础事件源仍停留在 `fs::File` / `*_opened()` bridge：
runtime repair 已经把 direct `File` helper 从 fanotify 可见面排除，但实现仍让 `fs::File`
import fanotify、持有 notification suppression policy，并通过 `read_opened()` /
`write_opened()` / `append_opened()` 让 `FileDesc` 恢复用户 fd 事件。这会把用户可见 fd policy
混入内核内部 opened object handle。

**Change:**

- `fs::File` 删除 fanotify import、`notify_fanotify()`、`*_with_notify()` suppression 参数和
  `*_opened()` wrapper；普通 `File::{read,read_at,write,write_at,append,...}` 只做 backend I/O、
  writable mount 检查和 metadata update。
- `FileDesc::{read,read_at,write,write_at,truncate}` 只做 fd 权限 / path-only / writable mount
  检查并调用普通 `File` helper；`FileDesc::truncate()` 不再提交 fanotify event。
- `fs/api/read_write` 在共享 syscall helper 成功边界提交 `FAN_ACCESS` / `FAN_MODIFY`。顺序 I/O
  在用户态 copy 边界成功后提交；vectored I/O 使用不重复提交的内部 helper，按最终成功总字节数
  提交一次，partial success 返回 total 时也只提交一次。direct read-user hook 通过通用
  opened-description 能力位声明是否属于 ordinary `FAN_ACCESS` source；fanotify group fd 的
  control read-user 关闭该能力，避免 `fanotify_mark(pathname == NULL, dfd=fanotify_fd)` 后
  `read(fanotify_fd)` 递归入队。
- `sendfile` 作为单独 fd I/O 边界覆盖：input fd 成功读出总字节数大于 0 时提交
  `FAN_ACCESS`，output fd 成功写入总字节数大于 0 时提交 `FAN_MODIFY`；失败且无成功字节时不提交。
- `ftruncate` 在 syscall 成功后提交 `FAN_MODIFY`，带 opened-description suppression；`truncate(path)`、
  `fallocate` grow 和 `O_TRUNC` 保持在各自 API 成功边界提交。
- `FAN_CLOSE_WRITE` / `FAN_CLOSE_NOWRITE` 保持 opened-description final-release callback，不移到
  `sys_close(fd)`。
- 删除 `NoNotifyGuard`、`NO_NOTIFY_GUARDS` 和 `current_task_notifications_suppressed()`；
  `notify_path_event()` 只检查 event 自带 suppression。fanotify event fd 仍安装
  `OpenedFileDescriptionOps { notification_suppressed: true, ... }`，用于后续 I/O / close 抑制自激。

**Review:** 只读 explorer 复核当前 emission points 与残留搜索，并指出 shared read-user 成功后若
无 owner-provided 边界，会让 fanotify group fd 的 control read 变成可标记的 recursive
`FAN_ACCESS` source。修复后 shared helper 仍不 downcast fanotify private state，而是读取通用
opened-description 能力位；fanotify group fd 显式关闭该能力。主控复审确认 `fs::File` / backend
`FileOps` 不再承载 fanotify policy，close 仍由 fd lifecycle owner 覆盖 close、dup3 覆盖、
close_range、exec close-on-exec 和 exit cleanup。

**Docs:** `FANOTIFY-042` 已在 tracking issues 中标为 Neutralized；RFC index、invariants、
implementation 同步为 user-visible fd/API event boundary 与 opened-description suppression 模型。

**Validation:**

- `rg -n "fanotify|FanHookEvent|FanMask|notify_path_event|notification_suppressed|read_opened|write_opened|append_opened|NoNotifyGuard|current_task_notifications_suppressed" anemone-kernel/src/fs/file.rs`：无命中。
- `rg -n "NoNotifyGuard|current_task_notifications_suppressed|NO_NOTIFY_GUARDS|read_opened|write_opened|append_opened|read_at_opened|write_at_opened|append_at_current_end_opened" anemone-kernel/src`：无命中。
- `just fmt kernel`：通过。
- `git diff --check`：通过。
- `mdbook build docs`：通过。
- `just build`：通过；仅剩既有 `anemone-kernel/src/sync/mono.rs` unused-import warning。
- `./scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64.log`：通过脚本完成 rootfs rebuild、release kernel build、QEMU 和 fanotify profile。glibc 与 musl 均完整跑完 fanotify01-23，summary 分别为 attempted=23 passed=4 failed=19，合计 attempted=46 passed=8 failed=38。失败仍来自 D6/Stage 5 暂缓项或测试设施分类问题；日志关键字审计未发现 `panic`、`Kernel panic`、`Mutex cannot be locked`、`preemption is disabled`、`interrupts are disabled`、`NoNotifyGuard` 或 `current_task_notifications_suppressed`。

**Result:** `FANOTIFY-042` 源码级修复与文档状态同步完成。D6 FID/name negative gate 仍未启动。
