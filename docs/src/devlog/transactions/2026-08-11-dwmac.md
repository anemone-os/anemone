# DWMAC 多后端与 Loongson 2K1000 事务日志

**状态：** Active / R0 / Gate 1
**日期：** 2026-08-11
**负责人：** Anemone maintainers, Codex
**RFC：** [RFC-20260811-dwmac R0](../../rfcs/dwmac/index.md)
**实施计划：** [Gate 1--4](../../rfcs/dwmac/implementation.md)
**适用修订：** R0
**Contract Cutover：** None；全部 target delta 保持 Not Cut Over

## 边界

本 transaction 记录 R0 acceptance，以及后续逐 Gate 的实现、review、validation、停止与 cutover 事实；不复制
RFC target 或实施计划。当前唯一授权是 Gate 1：保持 JH7110 DWMAC4 可见行为，将现有实现整理到 DWMAC
owner，并建立 Loongson 2K1000 irqchip-owned `IrqSense` source truth 与 one-shot request expectation。Gate 1
不接入 DWMAC1000、不发布新 netdev 能力、不改变 current contracts，完成后必须停止。

## R0 acceptance and Gate 1 authorization - 2026-08-11

**Decision:** 接受 RFC Draft 为 R0。R0 固定 per-node compatible dispatch、DWMAC4/DWMAC1000 concrete
ownership、DWMAC1000 normal descriptor 与 32-bit DMA admission、irqchip-owned `IrqSense`、CSR5 W1C、Route A
firmware handoff、PHY P1、success-order publication，以及正文定义的 non-goals、acceptance 和停止条件。

**Current baseline:** 本 checkpoint 同步公共 RFC 导航，并把当前 2K1000 board DTS 已有的两个
`local-mac-address` 纳入 accepted input baseline；后续 Gate 的 protected boundary 禁止继续修改 DTB。current
contracts 保持不变，2K1000/JH7110 hardware evidence 均 Not Run。

**Authorization:** 只授权 Gate 1。Route A、PHY、DWMAC1000 descriptor/DMA/CSR5、2K1000 node attach 和
contract cutover 属于 Gate 2--4，不得自动进入。

**Stop conditions:** 以 RFC `Implementation Boundary` 和 Gate 1 `Stop / Exit` 为准；任何 target、owner、
handoff、failure、cleanup、public API/ABI/visible semantics/current contract/acceptance/validation claim 变化，
或未解释的 JH7110 hardware regression，均停止并回 RFC review。

**Validation:** `git diff --cached --check` passed。`mdbook` 未安装，因此文档构建 Not Run；本 checkpoint
不把 kernel build 或 hardware 结果写成 acceptance evidence。

## Gate 1 execution

尚未完成。实现、独立 review、source/KUnit/build 与 hardware regression evidence 将在本节原地追加；Gate 1
完成后 transaction 保持 Active，并明确记录后续 Gate 未获授权。
