# 2026-06-22 至 2026-07-05

## 使用说明

- 本文件覆盖 2026-06-22 至 2026-07-05 的开发日志。
- 新条目按时间顺序追加。
- 条目格式使用模板页中的开发日志模板。

## Entries

## 2026-06-23 - User-test LTP runner 结构拆分

**Date:** 2026-06-23
**Authors:** doruche, Codex
**Area:** anemone-apps/user-test / LTP runner / test infra

**Summary:** `user-test` 的 LTP runner 从单文件 `src/ltp.rs` 拆成 `src/ltp/` 目录模块，
以分离静态配置、fixture、profile 解析、runner 协调、case 生命周期、输出兼容、heartbeat、
output filter、wait-loop probe 和时间策略；本轮目标是行为保持，不改变 profile 语义、
timeout 策略、fixture 内容或非 LTP rootfs preparation。

**Validation:** `just fmt user-test --check`、`just xtask app build user-test --arch riscv64`、
`just xtask app build user-test --arch loongarch64`、`git diff --check -- anemone-apps/user-test docs/src`
和 `mdbook build docs` 通过；用户侧确认 `user-test` 检验通过，QEMU / LTP runtime 未由
agent 侧运行。

**Related:** [User-test LTP runner 结构拆分小迭代记录](./changes/2026-06-23-user-test-ltp-structure-cleanup.md), [LTP post-summary hang](../register/open-issues.md#ane-20260616-ltp-post-summary-hang)

## 2026-06-23 - User-test LTP judge 输出兼容

**Date:** 2026-06-23
**Authors:** doruche, Codex
**Area:** anemone-apps/user-test / LTP runner / judge output

**Summary:** `user-test` 的 LTP runner 将 judge-visible group marker 收敛到 root 生命周期，
只暴露 `ltp-glibc` / `ltp-musl` 两个外层 group；内部 LTP group 改为普通进度日志。
output filter 继续保留 child 原始输出，同时对窄 LTP result-record tag 追加 judge-colored
marker，并在缺 `Summary:` 且已有 result tag 时补标准 Summary。

**Validation:** `just fmt user-test --check`、`just xtask app build user-test --arch riscv64`、
`just xtask app build user-test --arch loongarch64`、`git diff --check` 和合成 judge 样本通过；
active profile 审计为 29 个 group、833 个唯一 case name、0 个重复。agent 侧未运行完整
QEMU / LTP runtime。

**Related:** [User-test LTP judge 输出兼容小迭代记录](./changes/2026-06-23-user-test-ltp-judge-compat.md), [User-test LTP runner 结构拆分](./changes/2026-06-23-user-test-ltp-structure-cleanup.md), [LTP post-summary hang](../register/open-issues.md#ane-20260616-ltp-post-summary-hang)

## 2026-06-29 - VFS Direct User I/O 事务启动

**Date:** 2026-06-29
**Authors:** doruche, Codex
**Area:** VFS / FileOps / FileDesc / syscall read-write / user access

**Summary:** `vfs-direct-user-io` RFC 进入实现准备阶段，新增事务日志并完成阶段 0 前置审计：
确认公共 RFC、tracking issues、事务索引和 mdBook 导航已连接；当前 `FileDescOps::read_user`
仍只作为 opened-description transaction hook 使用，`pwritev2(flags != 0)` 仍独立 fail-closed，
ramfs/ext4 regular file 的现有 page/frame copy 路径已记录为后续 direct-user read/write
gate 的锁序审计基线。

**Validation:** 阶段 0 不运行 QEMU / LTP；文档检查和 baseline build 结果见事务日志。

**Related:** [VFS Direct User I/O 事务日志](./transactions/2026-06-29-vfs-direct-user-io.md), [RFC-20260629-vfs-direct-user-io](../rfcs/vfs-direct-user-io/index.md)
