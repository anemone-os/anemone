# 2026-07-06 至 2026-07-19

## 使用说明

- 本文件覆盖 2026-07-06 至 2026-07-19 的开发日志。
- 新条目按时间顺序追加。
- 条目格式使用模板页中的开发日志模板。

## Entries

## 2026-07-06 - Sched Wait Preempt Arming 事务启动

**Date:** 2026-07-06
**Authors:** doruche, Codex
**Area:** scheduler / wait core / kernel preempt / latch / iomux

**Summary:** `sched-wait-preempt-arming` RFC 进入实现阶段，新增事务日志并完成阶段 0：
确认裸 `schedule()` caller、`Latch::begin_current()` direct users、direct wait helper、
`Event::listen*()` users 和 `Mutex::lock()` nested-wait 诊断落点；随后由 implementation
subagent 补 `Location::caller()` origin、begin-side assert、no-nested-wait helper 和
`Mutex::lock()` slow-path 诊断，并由独立 review subagent 关闭 review gate。

**Validation:** `git diff --check`、`mdbook build docs` 和 `just build` 通过；`just build`
仅保留 cargo cache warning。QEMU / LTP 未运行。

**Related:** [Sched Wait Preempt Arming 事务日志](./transactions/2026-07-06-sched-wait-preempt-arming.md), [RFC-20260618-sched-wait-preempt-arming](../rfcs/sched-wait-preempt-arming/index.md)

## 2026-07-09 - Sched EEVDF-lite 事务启动与阶段零收口

**Date:** 2026-07-09
**Authors:** doruche, Codex
**Area:** scheduler / fairness / runtime accounting / scheduler class

**Summary:** `sched-eevdf-lite` RFC 接受进入实现，新增事务日志并完成阶段 0：
确认当前 scheduler class 仍是旧 RR/Idle trait 与单文件 `RunQueue` / `SchedEntity`
形状，`need_resched` 仍是 bool，trap / idle / IPI runnable-arrival plumbing 尚未 typed；
这些旧形状均已归入 1A / 1B / 2A / 2C / 3 的后续 gate，阶段 0 未发现需要回到 RFC review 的
停止条件。

**Validation:** `git diff --check` 和 `mdbook build docs` 通过；agent 未运行 QEMU / LTP。

**Related:** [Sched EEVDF-lite 事务日志](./transactions/2026-07-09-sched-eevdf-lite.md), [RFC-20260622-sched-eevdf-lite](../rfcs/sched-eevdf-lite/index.md)

## 2026-07-06 - Sched Wait Preempt Arming 阶段三收口

**Date:** 2026-07-06
**Authors:** doruche, Codex
**Area:** scheduler / wait core / kernel preempt / latch / iomux

**Summary:** `sched-wait-preempt-arming` 阶段 3 采用 source-review first 收口：scheduler/trap/wait-core
entry split 独立 review 未发现 Apollyon / Keter / Euclid finding；总控 source audit 确认
finite timeout、source-backed / no-source iomux、direct latch users、Event timeout、clock/signal
direct wait 和 `Armed` 术语上下文仍符合 RFC 边界。用户侧多次 iozone throughput 复核未再复现
wait begin 后无 timeout/source/finish 的卡死。

**Validation:** 本条记录用户侧 iozone 复核；agent 侧未运行 QEMU / LTP / iozone / 定向 trace。
Event timeout trace、source-backed finite-timeout iomux trace 和 deferred-count fairness trace
明确未运行，后续若 workload 或 trace 显示 `PrePark` setup 长窗口，需要回到 RFC review。

**Related:** [Sched Wait Preempt Arming 事务日志](./transactions/2026-07-06-sched-wait-preempt-arming.md), [RFC-20260618-sched-wait-preempt-arming](../rfcs/sched-wait-preempt-arming/index.md)

## 2026-07-08 - Sched Split Zombie Tail Feedback

**Date:** 2026-07-08
**Authors:** doruche, Codex
**Area:** scheduler / task exit / kernel preempt

**Summary:** `sched-wait-preempt-arming` 收口后的单核 `kernel_preempt` 试运行暴露
`schedule_preempt cannot preempt zombie current task` panic。复核确认旧 exit 尾部先发布
`TaskSchedState::Zombie`、再进入 no-return schedule，中间可被 trap-tail preempt 观察到 zombie
current。本次反馈已回写 RFC：最终 `Runnable -> Zombie` 发布归 scheduler core 的 noirq
`schedule_zombie_never_return()` 事务；`schedule_preempt()` 仍不接受 zombie current。

**Validation:** `just fmt kernel`、`just build`、目标源码 `git diff --check`、全仓
`git diff --check` 和 `mdbook build docs` 通过。agent 未运行 QEMU / LTP / iozone runtime
复现。

**Related:** [Sched Wait Preempt Arming 事务日志](./transactions/2026-07-06-sched-wait-preempt-arming.md), [RFC-20260618-sched-wait-preempt-arming](../rfcs/sched-wait-preempt-arming/index.md)

## 2026-07-12 - Sched EEVDF-lite Stage 3 Runtime Stop

**Date:** 2026-07-12
**Authors:** doruche, Codex
**Area:** scheduler / fairness / runtime accounting / performance

**Summary:** 用户运行证据证明 Stage 3 的 monotonic minimum-`vruntime` eligibility
contract 会大量制造 singleton eligible set，并形成 yield 后 same-task dispatch feedback；相同
read-write result multiset 下，EEVDF profile 约为 RR 的 3.3 至 3.5 倍。RFC 已公开 factual
evidence packet，重开 `EEVDF-001` / `EEVDF-018` / `EEVDF-004` / `EEVDF-020`，并把
修复顺序固定为 R1 weighted FairClock、R2 membership / service lag、R3a accounting / request、
R3b coordinate rebase。default EEVDF 目标不回退，也不接受 forced handoff 或 penalty tuning 窄修。

**Validation:** 本条记录既有 user-run exact-yield / runtime evidence；本轮 `git diff --check`、
新增 background whitespace 检查与 `mdbook build docs` 通过。agent 未运行 kernel build、QEMU
或 LTP；后续 runtime 责任与 gate 见事务日志。

**Related:** [Stage 3 eligibility 回归证据](../rfcs/sched-eevdf-lite/backgrounds/stage3-eligibility-regression-20260711.md), [Sched EEVDF-lite 事务日志](./transactions/2026-07-09-sched-eevdf-lite.md), [RFC-20260622-sched-eevdf-lite](../rfcs/sched-eevdf-lite/index.md)

## 2026-07-12 - Sched Split Pending Resched Pick Acknowledgement

**Date:** 2026-07-12
**Authors:** doruche, Codex
**Area:** scheduler core / kernel preempt / pending resched

**Summary:** `sched-wait-preempt-arming` 的 post-close feedback 关闭了 processor pending-resched
lifecycle 缺口。`PendingResched` 现在被定义为面向下一次 owner-CPU full pick 的合并 latch；
`local_pick_next()` 在成功 selection 后、`set_next_task()` 前统一 acknowledgement。block、yield、
zombie 与 same-task full pick 都不会把旧 cause 带到下一轮；`DeferredPreempt` 与 wait no-switch abort
仍不清除，destructive take 的 caller 继续在 deferred 后恢复 snapshot。未引入事件队列、epoch 或
task-owned cause history。

**Validation:** `just build`、当前 113 项 rv64 pretest KUnit、`git diff --check` 和
`mdbook build docs` 通过；pending-resched clear 通过 owner-boundary source audit 验证，不保留只测试
字段赋值的 wrapper / KUnit。有时限启动在 KUnit 和 init 成功后由主机 timeout 终止，随后 signal LTP
不属于本 correction 的验收范围。`just fmt kernel --check` 仅被既有生成文件
`kconfig_defs.rs` / `platform_defs.rs` whitespace drift 阻塞，输出不包含本次 `processor.rs`；
本 gate 未修改生成文件。

**Related:** [Sched Wait Preempt Arming 事务日志](./transactions/2026-07-06-sched-wait-preempt-arming.md), [RFC-20260618-sched-wait-preempt-arming](../rfcs/sched-wait-preempt-arming/index.md)

## 2026-07-12 - Sched Split Aborted-wait Surface Correction

**Date:** 2026-07-12
**Authors:** doruche, Codex
**Area:** scheduler core / wait handoff / scheduler class surface

**Summary:** source audit 确认 wait completion 只有 already-completed no-switch abort 与 parked
handoff 两条 production 路径；此前为假设中的第三条 abort-park requeue 保留的 processor、
`RunQueue`、`Scheduler` trait 与 class implementation 全部无 caller。本次 post-close correction
删除该死 surface，并同步 EEVDF canonical 文本；它不属于 R1-R3b，也不改变任何 reachable
scheduler / wait-core 行为。

**Validation:** `just build`、`mdbook build docs`、`git diff --check` 和全树 production source 零符号审计通过。`just fmt kernel --check` 只报告未触碰的 generated definitions whitespace drift；未运行 QEMU / LTP。host rootfs / DTB 前置边界见 scheduler-core 事务日志的 `KETER-009` closure。

**Related:** [Sched Wait Preempt Arming 事务日志](./transactions/2026-07-06-sched-wait-preempt-arming.md), [Sched EEVDF-lite 事务日志](./transactions/2026-07-09-sched-eevdf-lite.md)

## 2026-07-12 - Sched EEVDF-lite R1 Failure 与延期关闭

**Date:** 2026-07-12
**Authors:** doruche, Codex
**Area:** scheduler / EEVDF / runtime acceptance

**Summary:** R1 在独立 validation line 中把 actual min-floor eligibility 替换为 weighted FairClock，
但用户 signal 运行仍记录 `1,233,143` 次 yield self-pick 与 `1,232,735` 次
`self_only_eligible`，百万级 feedback 继续主导，命中既定 gate failure signal。R2 / R3a / R3b
未启动；RFC 以 Closed/deferred 收口，明确不是 Completed。production default 已恢复为 RR，默认
`user-test` / pretest rootfs 不再自动接入 `eevdf-test`。

**Boundary:** `eevdf.rs` 归档源与历史实现不删除，但不再接入 production class graph；method-first `Scheduler` trait、`RunQueue`
transaction facade、typed `PendingResched`、class-specific `SchedEntity`、fresh clone / constructor、
`Nice` 与 priority syscall 整理继续保留。`EEVDF-001` / `EEVDF-018` / `EEVDF-004` /
`EEVDF-020` 保持未解决 Keter；未来继续时必须重新打开 RFC review，不能从 R2 直接续跑。

**Related:** [Sched EEVDF-lite RFC](../rfcs/sched-eevdf-lite/index.md), [事务日志](./transactions/2026-07-09-sched-eevdf-lite.md), [Stage 3 evidence](../rfcs/sched-eevdf-lite/backgrounds/stage3-eligibility-regression-20260711.md)

## 2026-07-12 - Sched RT Class 事务启动与前置 Gate 收口

**Date:** 2026-07-12
**Authors:** doruche, Codex
**Area:** scheduler / realtime / FIFO / RR / scheduler class

**Summary:** `sched-rt-class` RFC 接受进入实现并建立事务日志。Scheduler-Core source audit
证明 full-pick acknowledgement、deferred restore、wait no-switch abort、parked handoff 和
removed abort-park surface 均满足 RT class 前置合同。文档层 review 暴露的 class identity / dispatch、
production constructor、RR quantum 来源和 review-gate 切分问题已折回 canonical implementation，
Checkpoint 1 改为不保留 legacy alias / 双 queue fallback 的原子 class 切换。

**Validation:** 前置 Gate 是 docs / source-audit checkpoint；`git diff --check` 与
`mdbook build docs` 结果见事务日志。agent 未运行 kernel build、KUnit、QEMU 或 LTP。

**Related:** [Sched RT Class 事务日志](./transactions/2026-07-12-sched-rt-class.md), [RFC-20260711-sched-rt-class](../rfcs/sched-rt-class/index.md), [IRQ-off heap allocation open issue](../register/open-issues.md#ane-20260622-irq-off-heap-allocation)

## 2026-07-13 - Sched RT Class 第一版收口

**Date:** 2026-07-13
**Authors:** doruche, Codex
**Area:** scheduler / realtime / FIFO / RR / scheduler class

**Summary:** Checkpoint 1 已完成共享 `Realtime` class 原子切换、两个 compile-time selector build、focused KUnit、source audit 和独立 review。用户随后在 RT/RR 默认配置下完整运行整套 LTP 测例，作为真实用户态 workload 与长链路调度集成证据，Checkpoint 2 据此关闭，RFC 与 transaction 完成第一版收口；该记录不表示每个 LTP case 都通过。

**Boundary:** FIFO 用户态 no-timeslice、explicit-yield 与 block/wake 专项 smoke 本轮未运行，按用户裁定不作为第一版关闭条件，也不写成 PASS 或已知缺陷。RT/FIFO selector build 与 FIFO-focused KUnit 证据保留。noirq `VecDeque` allocation 仍按 register limitation 跟踪；ABI policy syscall、动态 policy transaction、bandwidth control 和不同 priority runtime ordering 留给 follow-up。

**Related:** [Sched RT Class 事务日志](./transactions/2026-07-12-sched-rt-class.md), [RFC-20260711-sched-rt-class](../rfcs/sched-rt-class/index.md), [当前限制](../register/current-limitations.md#ane-20260713-sched-rt-noirq-bucket-allocation)

## 2026-07-13 - Sched Fair / Stride 事务启动

**Date:** 2026-07-13
**Authors:** doruche, Codex
**Area:** scheduler / fair class / stride / test assets

**Summary:** `sched-fair-stride` RFC 已接受进入实现并建立事务日志。当前只启动阶段 0：先按
canonical stop contract 审计 live scheduler class、Kconfig、constructor、yield、tick 与 test/rootfs
owner surface；审计通过后才迁移 `eevdf-test` 资产，阶段结果和验证证据继续追加到事务日志。

**Validation:** 事务入口建立时尚未运行阶段 0 验证；agent 不运行 QEMU / LTP。

**Related:** [Sched Fair / Stride 事务日志](./transactions/2026-07-13-sched-fair-stride.md), [RFC-20260713-sched-fair-stride](../rfcs/sched-fair-stride/index.md), [IRQ-off heap allocation open issue](../register/open-issues.md#ane-20260622-irq-off-heap-allocation)

## 2026-07-13 - Sched Fair / Stride 阶段零收口

**Date:** 2026-07-13
**Authors:** doruche, Codex
**Area:** scheduler / fair class / stride / test assets

**Summary:** 阶段 0 source audit 未命中停止条件；既有通用公平性 workload 已从 `eevdf-test`
单一迁移为 `fair-test`，保持四组 workload、阈值与 fixed-nice barrier，并安装到三份公共
`user-test` rootfs。scheduler production graph、Kconfig selector、`user-test` 与运行时接线未修改。

**Validation:** `fair-test` 的 RISC-V / LoongArch 双架构 build 与 format check 通过；最终文档与
whitespace 结果见事务日志。agent 未运行 QEMU / LTP / `fair-test` runtime。

**Related:** [Sched Fair / Stride 事务日志](./transactions/2026-07-13-sched-fair-stride.md), [RFC-20260713-sched-fair-stride](../rfcs/sched-fair-stride/index.md)

## 2026-07-14 - Sched Fair / Stride 第一版收口

**Date:** 2026-07-14
**Authors:** doruche, Codex
**Area:** scheduler / fair class / stride / LTP

**Summary:** 阶段 0 与 Checkpoint 1-2 已完成 `fair-test` 资产迁移、稳定 Fair/Stride identity、pass/heap/lifecycle实现、三 selector default cutover、focused KUnit、source audit和 review。用户随后在 Fair default下完整运行当前 `all` LTP profile；同次运行的 127 项 KUnit与 `fair-test` 四组 workload全部通过，LTP整体运行到末尾并汇总 attempted 1663、passed 932、failed 516、infra_failed 2、skipped 214。该记录不表示所有 LTP case均通过。

**Performance:** 用户报告 Fair相比 RT/RR baseline约慢 13–14%，并接受其仍处于同一量级、未出现稳定多倍 whole-profile退化；精确绝对耗时未提供给 agent。Checkpoint 3据此关闭，RFC与 transaction完成第一版收口。

**Boundary:** IRQ-off `BinaryHeap` allocation继续由既有 register issue跟踪。用户的 `fair-test`调用与少量 LTP case exclusions仍是未提交的 validation-only工作树改动，不进入本次文档提交；动态调度属性、第二 Fair backend、actual-runtime accounting与跨 CPU fairness留给 follow-up。

**Related:** [Sched Fair / Stride 事务日志](./transactions/2026-07-13-sched-fair-stride.md), [RFC-20260713-sched-fair-stride](../rfcs/sched-fair-stride/index.md), [IRQ-off heap allocation open issue](../register/open-issues.md#ane-20260622-irq-off-heap-allocation)

## 2026-07-14 - RFC Semantic Revision Workflow

**Date:** 2026-07-14
**Authors:** doruche, Codex
**Area:** docs / RFC workflow / agent workflow

**Summary:** RFC 版本管理统一收口到仓库 Git 与轻量语义修订：Git 保存全部文本 diff，RFC 只用 `Draft`、`R0`、`R1` 标记已接受的 contract 版本。`index.md` / `invariants.md` 原地维护 consolidated canonical，`implementation.md` / `tracking-issues.md` 保留增量历史；Closed RFC 的后续语义修订需要代码工作时建立新 transaction，不延长旧的 Completed transaction。

**Boundary:** 本轮不为单个 RFC 建仓库，不引入默认 amendment 文档或版本化 canonical 副本，也不批量 retrofitting 既有 RFC。核心目标、主要 owner、整体方案或大部分证明边界变化时仍使用 follow-up RFC。

**Validation:** `git diff --check`、RFC workflow skill frontmatter 解析和 `mdbook build docs` 通过。

**Related:** [RFC semantic revision workflow 小迭代记录](./changes/2026-07-14-rfc-semantic-revisions.md), [RFC 工作流](../rfc-workflow.md#git-历史与语义修订), [RFC 模板](../rfc-template.md)

## 2026-07-14 - Sched RT Class R1 修订启动

**Date:** 2026-07-14
**Authors:** doruche, Codex
**Area:** scheduler / realtime / RR rotation / scheduler core contract

**Summary:** `sched-rt-class` 建立可验证 R0 baseline，并接受 R1 semantic revision。R1 删除 class-visible `ReschedCause::{Tick, RunnableArrival}` continuation：processor pending 只表示一次 full pick 尚未完成，RR expiry 的队尾 rotation 由 RR entity 的 `rotation_due` 在 request 产生时提交，再由 preempt / yield / handoff / block / exit lifecycle 消费或清除。R0 Completed 事务保持关闭，R1 使用独立 transaction。

**Boundary:** pending take / caller restore / successful full-pick acknowledgement、wait-core、current-outside-ready-queue、priority-first RT algorithm 与 `Realtime > Fair > Idle` 不变；Fair / Idle 只允许 trait 机械适配。若实现需要 core generation/token、改变 acknowledgement、把 rotation 移出 RT owner、重做 membership 或修改 Fair 算法，必须停止并回到 RFC review。

**Validation:** D0 只修订 canonical docs、cross-RFC shared contract、transaction 与导航；kernel build、KUnit、QEMU 和 LTP 尚未运行。文档验证与独立 review 结果写入 R1 transaction。

**Related:** [Sched RT Class R1 事务日志](./transactions/2026-07-14-sched-rt-class-r1.md), [RFC-20260711-sched-rt-class R1](../rfcs/sched-rt-class/index.md), [Tracking Issues](../rfcs/sched-rt-class/tracking-issues.md)

## 2026-07-14 - Sched RT Class R1 实现收口

**Date:** 2026-07-14
**Authors:** doruche, Codex
**Area:** scheduler / realtime / RR rotation / scheduler core contract

**Summary:** R1 由 `39ba07a9` 完成：scheduler core pending 收窄为 processor-owned single-bit full-pick snapshot，class transaction 不再接收 Tick / RunnableArrival cause；RR entity 的 `rotation_due` 在 expiry 时提交，并由 preempt / yield / handoff / block / exit lifecycle 消费或清除。Fair / Stride 与 Idle 只有 trait 机械适配。

**Validation:** `fair` / `rt_rr` / `rt_fifo` 三个 selector 的 `just build` 通过；重建清洁 pretest rootfs 后，RT/RR QEMU KUnit `131/131` 全部通过；source audit、`git diff --check`、repository format check 与独立 review 关闭。Reviewer 的唯一 Euclid 已在提交前修复，最终无剩余 Apollyon / Keter / Euclid。full-pick acknowledgement 与 trap-tail deferred restore 由 owner-local source proof 覆盖，不写成直接 KUnit 执行。

**Boundary:** pretest 在 KUnit 后固定执行的 `fair-test` 不适用于 RT/RR compile-time default，未计入 R1 runtime pass，也未扩展 rootfs / harness 写集。R1 不重复 R0 的整套 LTP；动态 policy/priority、RT bandwidth、跨 CPU migration 与 archived EEVDF 仍为独立后续边界。

**Related:** [Sched RT Class R1 事务日志](./transactions/2026-07-14-sched-rt-class-r1.md), [RFC-20260711-sched-rt-class R1](../rfcs/sched-rt-class/index.md), [Tracking Issues](../rfcs/sched-rt-class/tracking-issues.md)
