# 2026-05-25 至 2026-06-07

## 使用说明

- 本文件覆盖 2026-05-25 至 2026-06-07 的开发日志。
- 新条目按时间顺序追加。
- 条目格式使用模板页中的开发日志模板。

## Entries

## 2026-05-28 - `close_range` 目录化与 `UNSHARE` 文件表拆分

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** fs / syscall / task / user-test / docs

**Summary:** 落地 `close_range` syscall，并把 `close` 与 `close_range` 统一放进 `fs/api/close/` 目录模块。`close_range` 支持 `CLOSE_RANGE_UNSHARE` 和 `CLOSE_RANGE_CLOEXEC` 两个 Linux flag，`first > last` 返回 `EINVAL`，`close_range02` 在 rv64 `fs-fd` 中通过。

**Motivation / Symptom:** 这次的重点不是单纯补一个批量关闭接口，而是把 `CLOSE_RANGE_UNSHARE` 语义补完整。`close_range` 如果只在现有共享 fd 表上批量改状态，会把通过 `CLONE_FILES` 共享同一张表的其他 task 一起影响掉，和 Linux 的“先 unshare 再操作”语义不一致。

**Change:** 将 `close` 拆成目录模块：`close/mod.rs` 只负责模块组织，`close/close.rs` 放单 fd close，`close/close_range.rs` 放 `CloseRangeFlags` 和 `sys_close_range`。`Task::files_state` 改成“当前 task 持有一个可替换的文件表句柄，句柄再指向共享的 `FilesState`”，这样 `UNSHARE` 可以让当前 task 替换成 `fork()` 出来的新表，而不会改动其他共享者；`CLOEXEC` 则只批量设置 `FD_CLOEXEC`。`clone(CLONE_FILES)` 路径也同步改成共享这个句柄。

**Validation:** `just build` 通过；`scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-fs-fd.log` 中 `close_range02` 通过，`close_range01` 失败点是测例环境拿不到 device，属于 infra 问题。

**Follow-up:** 如果后续还要补其他 fd 批量操作，可以继续沿这个目录结构拆分；当前只关心 `close_range` 的语义闭合。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-26 - signal restorer 兼容 glibc 2.3.5 / loongarch 观察

**Date:** 2026-05-26
**Authors:** doruche, GitHub Copilot
**Area:** signal / ABI / user-test / docs

**Summary:** 确认当前用户态测例的信号 ABI 仍以 legacy `sa_restorer` 路径为准，主目标是兼容 `musl 1.2.2` 的 `pthread_cancel` 测例；`glibc 2.3.5` 仅作为 `sigaction` / `sa_restorer` 形状对照参考。同时暂不引入面向新内核头的条件编译分支，避免为了新头文件语义切换破坏现有回归。

**Motivation / Symptom:** 现有失败点首先出现在 `musl 1.2.2` 的 `pthread_cancel` 测例；随后再拿 `glibc 2.3.5` 的 `sigaction` 结构和 `sa_restorer` 语义做交叉对照。现代 LoongArch / asm-generic 头文件不再鼓励新架构继续定义 `SA_RESTORER`，但当前阶段更重要的是保住既有 libc-test 兼容面，而不是把用户态 ABI 切到一套新的条件分支。

**Change:** 保持内核侧对 restorer 路径的兼容处理，继续按测例需要提供 signal return trampoline；文档层面明确本阶段不引入针对新内核头的条件编译兼容层，避免把 ABI 选择复杂化。

**Validation:** `musl 1.2.2 pthread_cancel` 测例通过；用户确认完整 rv64 测试链路可跑通。

**Follow-up:** 如果后续要同时覆盖新旧内核头的用户态 ABI，需要单独设计可配置的 signal UAPI 适配层，并补足 glibc / musl 两侧的回归矩阵。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-27 - 进程组与会话 stage-1 主干

**Date:** 2026-05-27
**Authors:** doruche, Codex
**Area:** task topology / process group / session / signal / wait

**Summary:** 以 `69bff4b` 作为尚未引入进程组和会话的代码基线，在其后建立 stage-1 进程组/会话主干：`TaskTopology` 增加 session 与 process-group authority，`ThreadGroup` 缓存 `pgid` / `sid` / `has_executed`，并接入 `setpgid`、`getpgid`、`setsid`、`getsid`、process-group `kill` 和 `wait4` 的进程组选择语义。

**Motivation / Symptom:** 在 `69bff4b` 之前，任务拓扑只有 task / thread-group / parent-child 关系，无法表达 POSIX process group、session、fork/exec 竞争窗口，也无法让 shell、BusyBox 或后续 job-control 相关测例按 PGID/SID 观察进程。`kill(-1, sig)` 另有一个锁序遗留问题，需要在广播目标快照后再递送信号。

**Change:** 引入 `ProcessGroup` 和 `Session` 一等对象，保持 `TaskTopology` 为唯一 membership authority；fork 继承父进程的 PGID/SID，exec 只标记 `has_executed`，exit 在最后一个 thread 离开时清理 process-group/session membership。syscall 层实现 stage-1 `setpgid` / `getpgid` / `setsid` / `getsid`，`kill(0, sig)` 与 `kill(-pgid, sig)` 走 process-group 广播，`wait4(0, ...)` 与 `wait4(-pgid, ...)` 按 child `ThreadGroup::pgid()` 选择。`kill(-1, sig)` 后续改为先快照除 init 和调用者自身外的 thread group，再释放 topology 锁逐个递送，避免在 topology 锁内进入 signal 递送路径。

**Validation:** 针对该语义新增 `pg-test` 用户态覆盖；2026-05-27 的 `kill(-1, sig)` 补齐后，`just build` 通过。本文档更新时未重新跑完整 rv64 `user-test` 链路。

**Follow-up:** 继续补齐 `waitid`，再接 controlling tty、foreground/background process group、orphaned process group 的 `SIGHUP` / `SIGCONT` 规则，以及完整 shell job-control 行为。更广泛合入前仍需要 rv64 端到端回归。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-27 - rv64 LTP 白名单语义收敛与 `madvise(DONTNEED)` 修复

**Date:** 2026-05-27
**Authors:** doruche, Codex
**Area:** mm / fs / syscall / user-test / docs

**Summary:** 针对 `rootfsconfig-rv` 下的 rv64 LTP 白名单复跑，补齐一批明显的语义缺口：显式整理 `SysError -> errno` 映射，收紧用户指针校验与路径长度返回码，补上 `tmpfs` 挂载别名和 `MAP_POPULATE` / `madvise` 常量，修正 heap 页边界可访问判定，并让 `MADV_DONTNEED` 真正丢弃匿名页内容后在下次 fault 时回零。

**Motivation / Symptom:** `build/user-test-rv64.log` 和 `build/ltp-undebug.log` 暴露出若干测例失败：`chdir04` 的 `ENAMETOOLONG` / `EFAULT` 不对，`brk01` 因 heap 页边界判定过严发生 fault，`brk02` 在新 VMA 上 `mprotect` 返回 `EPERM`，`madvise01/02/03/05/10` 分别卡在未定义的 advice 行为、匿名页零填充和 `MAP_POPULATE` 兼容上。

**Change:** 统一补了 `BadAddress` / `NameTooLong` 等 errno 语义，用户指针检查不再随意落到其他错误；`chdir` 路径组件按 `MAX_FILE_NAME_LEN_BYTES` 返回 `ENAMETOOLONG`；`tmpfs` 通过 `ramfs` 兼容挂载；`MAP_POPULATE` / `MAP_NONBLOCK` 和 `MADV_*` 常量进入 ABI；`madvise(DONTNEED)` 增加 discard 路径，`AnonObject` / `ShadowObject` 支持 discard 后重 fault；`brk` / heap 保护按页粒度处理，避免把新 heap 页误判成不可访问。

**Validation:** `just build` 通过；使用 `scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img` 跑完白名单后，结果为 `passed=19 failed=2 skipped=1`。剩余失败只剩 `chdir01` 的设备资源不足，属于测试环境问题，不是内核语义缺口。

**Follow-up:** 若后续要继续压低失败数，需要先清理 `chdir01` 的设备池 / 测试环境约束，再看是否要把这类 infra failure 进一步拆成单独的验证流程。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-27 - PR 99 语义回看：`mremap` 固定目标校验与 `madvise` discard 收口

**Date:** 2026-05-27
**Authors:** doruche, Codex
**Area:** mm / uspace / user-test / docs

**Summary:** 根据 PR 99 的 Copilot 反馈，修正了两个明确的语义问题：`mremap` 只在 `MREMAP_FIXED` 时校验并消耗 `new_addr`，避免非 fixed 场景被无关参数拦截；`VmObject::discard_range` 的默认实现改为 hint 级 no-op，使 `madvise(DONTNEED)` 在不支持专用 discard 的 backing 上不会误报错误。

**Motivation / Symptom:** PR 99 的 review 意见指出，`mremap` 当前会无条件验证 `new_addr`，而 `madvise(DONTNEED)` 在某些 backing 上会被默认 `discard_range` 直接转成 `InvalidArgument`。前者是明显的参数语义问题，后者会把 hint 语义抬成硬错误，和 `madvise` 的预期不符。

**Change:** `mremap` 入口改为只在 `MREMAP_FIXED` 路径上校验 `new_addr` 的用户态地址与页对齐；`VmObject` 默认 `discard_range` 改成忽略未实现的 discard 请求，并补了注释说明这是 hint 级行为。另把 heap 上 `mprotect` 快路径未来 fault 不持久的问题记入 register 以便后续收敛。

**Validation:** 代码修改后重新走构建与 rv64 user-test 验证链路。

**Follow-up:** heap `mprotect` 的持久化问题仍需后续单独处理，不在这次简单语义修复范围内。

**Related:** [活动登记册](../register.md), [开放问题](../register/open-issues.md)

## 2026-05-27 - `read` / `readlinkat` 参数语义收口

**Date:** 2026-05-27
**Authors:** doruche, Codex
**Area:** fs / syscall / user-test

**Summary:** 顺手修掉了两处 LTP 里很直接的参数语义问题：`Fd` syscall 参数现在把越界/非法 fd 统一映射为 `BadFileDescriptor`，`read()` 的坏 fd 测例会回到 EBADF；`readlinkat()` 的无效 fd 和显式路径分支也恢复了正常 errno，空路径分支则单独暴露出 `openat` 里还没补齐 `O_NOFOLLOW` / `O_PATH` symlink 语义。

**Motivation / Symptom:** `read02` 里坏 fd 走成了 `EINVAL`，`readlinkat02` 的无效 fd 也落错了 errno。`readlinkat01` 的空路径则不是单点 errno 问题，而是后端还没有真正保住 `O_PATH | O_NOFOLLOW` symlink fd 的语义。

**Change:** `Fd::try_from_syscall_arg` 改为在 fd 非法时返回 `BadFileDescriptor`。`readlinkat` 保留了空路径分支入口，但它后面仍依赖 `openat` 的 `O_NOFOLLOW` / `O_PATH` 支持，因此这条语义单独记到 register。

**Validation:** 已重新构建并重跑 rv64 user-test 链路；`read02` 和 `readlinkat02` 已通过，`readlinkat01` 空路径仍失败，原因已单独登记。

**Related:** [活动登记册](../register.md), [开放问题](../register/open-issues.md)

## 2026-05-27 - `write` / `pwrite` LTP 语义收口

**Date:** 2026-05-27
**Authors:** doruche, Codex
**Area:** fs / syscall / vectored IO / user-test

**Summary:** 针对新加入的 write 相关 LTP 测例，修正 `pwrite04` / `pwrite04_64` 暴露的 `O_APPEND + pwrite` 语义，并补上 `pwritev2` 的 stage-1 syscall 路径，使 `pwritev201` / `pwritev202` 及 64 位变体回到预期 errno 和偏移行为。

**Motivation / Symptom:** `pwrite04` 系列把带 `O_APPEND` 的 fd 传给 `pwrite`，Linux 兼容语义是写入仍追加到 EOF，但 positioned write 本身不推进当前 file offset；之前实现把这条路径拒成 `EINVAL`。`pwritev202` 系列则同时覆盖 `pwritev2` 的非法 offset、非法 flags、坏 iovec、坏 fd 和 pipe `ESPIPE` 等错误路径，当前内核缺 syscall 入口和 `offset == -1` 的 current-position 语义。

**Change:** `FileDesc::write_at` 现在先校验文件是否可 seek，再在 `O_APPEND` 场景通过文件对象追加到当前 EOF，并保持 fd cursor 不变；普通 positioned write 仍按传入 offset 写入。read/write syscall 公共层增加 hi/lo offset 解码和 `offset == -1` 的 current-position 处理；rv64 / loongarch syscall 表加入 `pwritev2` 入口，stage-1 实现支持 `flags == 0`，非零 flags 统一返回 `EOPNOTSUPP`。

**Validation:** `just build` 通过；使用 `scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64.log` 复跑后，`pwrite04`、`pwrite04_64`、`pwritev201`、`pwritev201_64`、`pwritev202`、`pwritev202_64` 均通过。剩余 write 相关项不再是这四个语义错误：`pwritev03*` 和 `writev03` 主要受设备池 / CPU 数量测试条件影响，`write04` 仍依赖 `mknod` / FIFO syscall 路径，`write05` 仍缺 `EPIPE` 后的 `SIGPIPE` 投递，`writetest` 的 fault 需要另行定位。

**Follow-up:** `pwritev2` 的 `RWF_*` flags 仍只是明确拒绝，后续如果要支持 `RWF_APPEND`、per-IO sync、nowait / hipri 等语义，需要单独扩展并回归。`write05` 的 `SIGPIPE` 问题已登记为开放问题。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md), [开放问题](../register/open-issues.md)

## 2026-05-28 - `CLONE_VFORK` 主路径落地

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** task / clone / execve / exit / docs

**Summary:** 完成 `CLONE_VFORK` 的主路径接线：在 `Task` 上加入 child-owned 的 `vfork_done` 事件，`clone(CLONE_VFORK)` 在 child 发布后等待该事件，`execve` 与 `exit` 分别在完成点唤醒父进程。这样 vfork 的父阻塞与 child 退出/切换到新映像的解除条件收敛到同一条事件路径上。

**Motivation / Symptom:** 之前内核只有普通 `clone` / `fork` 的返回路径，没有把 `CLONE_VFORK` 的“父进程必须等待 child 先完成 `execve` 或退出”这条语义单独闭合起来。用户态传入 `CLONE_VFORK` 后，父子调度关系仍会回到普通 clone 的默认行为。

**Change:** `Task` 增加 `vfork_done: Event` 字段并在创建路径中初始化；`clone` 的 flag 白名单加入 `VFORK`，在发布 child 后对其 `vfork_done` 做 uninterruptible wait；`execve` 在切换到新映像并恢复到用户态前 publish `vfork_done`，`exit` 在发布 `child_exited` 之后也 publish 一次，保证 child 无论是执行新映像还是直接退出都能解除父等待。

**Validation:** `just build` 通过。

**Follow-up:** 后续如果要补 `vfork` 的更细分边角行为，再单独看 `CLONE_PARENT` / `CLONE_THREAD` 等组合，以及用户态测例覆盖。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-28 - fs 组 `getcwd` 语义收口

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** fs / syscall / ramfs / procfs / user-test

**Summary:** 针对新加入的 fs 组 `getcwd` LTP 用例，修掉了三个直接语义问题：坏用户指针现在按 `EFAULT` 返回而不会内核态写空指针 panic；`getcwd` 返回值恢复为相对进程 root 的绝对路径；`openat` 在非 create 路径不再校验被 libc 传入的无意义 `mode` 参数。同时给 `ramfs` 补了 regular-file rename 的 stage-1 实现，使 `getcwd04` 不再卡在 `EOPNOTSUPP`。

**Motivation / Symptom:** `getcwd01` 最初在无效用户缓冲区上没有稳定返回 `EFAULT`，而是先报错码不对，随后触发内核页错误。完整 fs 组日志还暴露了 `getcwd02` 期望 `/tmp` 却拿到错误路径、`musl getcwd03` 因返回路径不是绝对路径而失败、`getcwd04` 因 `rename` 未实现而 `TBROK`。`build/ltp-debug.log` 进一步显示 musl `realpath` 会在非 `O_CREAT` 的 `openat` 上传入垃圾 `mode`，Linux 语义应忽略该参数。

**Change:** 用户地址校验先拒绝越过 `USPACE_TOP_ADDR` 的地址并把非法地址统一映射到 `BadAddress`；`Task::rel_cwd()` 对非根 cwd 返回 `"/" + rel`，避免把 chroot/root-relative 路径变成相对路径；`openat` 的 syscall 入口把 `mode` 当作原始 `u32`，只在 `O_CREAT` / `O_TMPFILE` 分支解析权限位。`ramfs` 的 `rename` 现在支持同一 superblock 内非目录项改名和覆盖普通目标，支持 `RENAME_NOREPLACE`，目录 rename 仍明确拒绝。

**Validation:** `just build` 通过；复跑 `scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-rv64.log` 后 QEMU 正常关机。结果为 `attempted=12 passed=8 failed=4`：glibc 侧 `getcwd01`、`getcwd02`、`getcwd03`、`getcwd04` 全部通过；musl 侧 `getcwd01`、`getcwd03`、`chdir04` 通过。剩余失败中，`chdir01` 仍是已登记的设备池 infra 问题，`musl getcwd04` 是测例要求至少 2 个 online CPU 的条件问题，`musl getcwd02` 已从旧的 `openat EINVAL` 前进到 `realpath()` 依赖 `/proc/self/fd/<n>` 时返回 `ENOENT`。

**Follow-up:** `/proc/<tgid>/fd` 不做针对 `getcwd02` 的临时补丁，后续等 procfs/fd 目录、fd 生命周期和 fd symlink readlink/open/stat/readdir 语义一起作为系统性框架引入。`ramfs` 目前只补 regular-file rename，完整 directory rename 也单独登记为后续限制。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md), [开放问题](../register/open-issues.md)

## 2026-05-28 - ROFS 直接写路径与 `mkdirat02` 收口

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** fs / mount / VFS / syscall / user-test

**Summary:** 针对 fs 组新增的 `mkdirat02`，补齐 `mount(MS_RDONLY)` 到 `MountFlags::RDONLY` 的传播，并把只读挂载上的直接写操作统一映射到 `ReadOnlyFs -> EROFS`。最初只在 `vfs_mkdir_at` 上补了检查以解除 `mkdirat02` 的 TFAIL，随后把同类直接写入口扩展到 VFS 目录项修改、普通文件 open-for-write / write / truncate、`chmod` / `chown` / `utimensat` / `fallocate`。

**Motivation / Symptom:** `build/user-test-rv64.log` 中 `mkdirat02` 的两个 EROFS 分支失败：在只读 `mntpoint` 下创建新目录时意外成功；在只读挂载下目标已存在时返回 `EEXIST`，而 LTP 期望 `EROFS` 优先。日志同时显示测试框架已经把 `mntpoint` 按只读重新挂载，但内核此前完全忽略 mount flags，因此 VFS 创建路径看不到 ROFS 状态。

**Change:** ABI 层加入 Linux `MS_RDONLY` 常量；`sys_mount` 解析该 flag 并保存为 `MountFlags::RDONLY`，其他 mount flags 继续作为 unsupported flags 打日志后忽略。VFS 层新增 `Mount::ensure_writable()` 并在 `touch`、`mkdir`、`link`、`symlink`、`unlink`、`rename`、`rmdir` 前检查目标 mount；普通文件内容写入、open-for-write、truncate/ftruncate、fallocate、chmod/chown、utimensat 也在直接修改前检查只读挂载。

**Validation:** `just build` 通过；复跑 QEMU 后 `build/user-test-rv64-mkdirat02-fix.log` 中 glibc/musl 的 `mkdirat02` 均通过，两个 EROFS 分支返回 `ReadOnlyFs -> EROFS (30)`，两个 ELOOP 分支仍通过。fs 组仍有其他与本次 ROFS 无关的失败，例如 `mkdir03` / `mkdir04` 受缺失 syscall 或 credentials 路径影响。

**Follow-up:** 当前 ROFS 仍是 stage-1 的直接写路径防护，不承诺 shared writable mmap、writeback、remount/bind/move mount 或完整 Linux mount flag 矩阵；该限制已登记到 current limitations。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-28 - LTP pipe 语义收口

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** fs / pipe / fcntl / signal / user-test

**Summary:** 针对 rv64 LTP pipe 组，补齐低成本 pipe 语义：无读者写 pipe 时投递 `SIGPIPE` 并返回 `EPIPE`，pipe 阻塞等待可被信号打断，`PIPE_BUF` 大小写入不再因为边界判断卡死；同时接入 `O_NONBLOCK`、`F_GETPIPE_SZ`、`F_SETPIPE_SZ(0)` 和 pipe `FIONREAD`。

**Motivation / Symptom:** `build/user-test-rv64.log` 与 `build/ltp-debug.log` 中，`pipe02` 观察到 child 未被 `SIGPIPE` 杀死，`pipe08` 看到 `sigpipe_cnt == 0`，`pipe11` 超时后还出现无法杀掉进程的现象；`pipe12` / `pipe15` 的 `F_GETPIPE_SZ` 返回 `EINVAL`，`pipe2_01` / `pipe2_04` 则暴露 `O_NONBLOCK` 和 `F_SETFL` 路径缺失。

**Change:** pipe 写端 no-reader 路径现在向当前任务投递 `SIGPIPE` 并保持 `EPIPE` 返回；读写端在等待期间检查未屏蔽信号，使 `SIGKILL` 等信号能打断阻塞等待。pipe 写入的容量边界改为允许正好 `PIPE_BUF` 的原子写入，并为非阻塞 pipe 返回 `EAGAIN` 或部分写入。fd 的 file status flags 改为保存在共享 `ProcFile` 上，`pipe2(O_NONBLOCK)` 和 `fcntl(F_SETFL)` 能更新同一 open file description；`fcntl` 增加 `F_GETPIPE_SZ`、`F_SETPIPE_SZ` stage-1 处理，`ioctl(FIONREAD)` 可返回 pipe 当前可读字节数。`pipe(NULL)` 的坏用户指针也恢复为 `EFAULT`。

**Validation:** `just build` 通过。第一次运行 `scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/user-test-pipe-fix.log` 命中已知 event 竞态的 `pg-test` 内核断言；按要求重跑 `build/user-test-pipe-fix-rerun.log` 后 QEMU 正常关机。复跑结果为 glibc pipe 组 `attempted=17 passed=14 failed=3`，musl pipe 组 `attempted=17 passed=14 failed=3`，总计 `attempted=34 passed=28 failed=6`。其中 `pipe02`、`pipe05`、`pipe08`、`pipe11`、`pipe12`、`pipe2_01` 已通过；`pipe2_04` 的核心 `EAGAIN` 检查通过，但测例后续仍因缺少 `/proc/<pid>/stat` 失败。

**Follow-up:** 剩余 pipe 组失败集中在 procfs/测试设施可观察入口：`pipe07` 依赖 `/proc/self/fd`，`pipe15` 依赖 `/proc/sys/fs/pipe-user-pages-soft`，`pipe2_04` 依赖 `/proc/<pid>/stat`。动态 pipe resize 和 `O_DIRECT` packet-mode pipe 仍按 stage-1 限制登记。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-28 - `execveat` 接线与 exec errno 语义收口

**Date:** 2026-05-28
**Authors:** doruche, Codex
**Area:** task / execve / syscall / user-test / docs

**Summary:** 实现 kernel 侧 `execveat` syscall，并修正 LTP `execve03` 暴露的两个直接 exec errno 语义问题：`argv` / `envp` 允许传入 `NULL`，可执行目标缺少 execute bit 时返回 `EACCES`。

**Motivation / Symptom:** exec 组日志显示 `execveat01` / `execveat02` 需要 `SYS_EXECVEAT` 入口；`execve03` 的有效路径分支全部提前返回 `EFAULT`，根因是测例传入 `envp == NULL`，而当前 syscall 参数解析把 NULL 字符串数组当成坏地址。`execve03` 的 no-execute 文件分支还需要返回 `EACCES`。同一轮日志中的 `execve_child shouldn't be executed` 来自 `execve04`，根因是缺少 `ETXTBSY` writer/executable 排斥账本。

**Change:** `execveat` 支持 `AT_EMPTY_PATH` 与 `AT_SYMLINK_NOFOLLOW`，相对路径按 `dirfd` 解析，空路径可直接执行 fd 指向的对象。exec binfmt 调度改为传递已解析 `PathRef`，使 `execveat(fd, "", ..., AT_EMPTY_PATH)` 能复用现有 ELF/shebang loader。`execve` / `execveat` 对 `argv == NULL` 和 `envp == NULL` 使用空数组语义，现有空 argv 会继续补 `argv[0] == ""`。binfmt 调度在加载 regular 文件前检查 execute bit，缺失时返回 `AccessDenied -> EACCES`。`ETXTBSY` 不在本次修复范围内，已登记为后续系统性 VFS busy 账本问题。

**Validation:** `just build` 通过。此前用户提供的 `build/user-test-rv64.log` 中，`execveat01`、`execveat02` 已通过；`execve03` 的失败点明确对应 NULL envp 与 execute-bit 语义；`execve04` 仍按登记项等待系统性修复。

**Follow-up:** 后续实现 inode/open-file writer 与 executable-open 排斥后，重新验证 `execve04`；如果要进一步接近 Linux，还需要补 credentials 相关的 execute 权限判断细节。

**Related:** [活动登记册](../register.md), [开放问题](../register/open-issues.md)

## 2026-05-29 - LTP memory `mmap` / `mremap` errno 语义收口

**Date:** 2026-05-29
**Authors:** doruche, Codex
**Area:** mm / mmap / mremap / syscall / user-test / docs

**Summary:** 根据 `etc/ltp-mmap-mremap-munmap-analysis-20260528.md` 的分类，先修复不需要结构性改造的 mmap / mremap errno 与参数语义问题。`mmap06`、`mmap08`、`mmap15`、`mmap17`、`mmap20` 和 `mremap03` 在 glibc memory 组中回到 PASS。

**Motivation / Symptom:** `build/ltp-debug.log` 显示多项失败只是 syscall 边界语义不对：file-backed mmap 权限失败从 `EPERM` 暴露给用户，`O_WRONLY` fd 还能被 `MAP_SHARED | PROT_WRITE` 映射；invalid fd 与 zero length 的检查顺序导致 `mmap08` 得到 `EINVAL` 而不是 `EBADF`；高地址 `MAP_FIXED` 被当作普通用户指针提前校验成 `EFAULT`；`MAP_FIXED_NOREPLACE` overlap 的内部 `AlreadyMapped` 被全局映射为 `EFAULT`；`MAP_SHARED_VALIDATE` 下未知 flag 返回 `EINVAL`；`mremap03` 的未完整 old range 被 reservation / permission 错误抢先暴露成 `EPERM`。

**Change:** `mmap` 的 `addr` 参数改为目标 VA 描述符，只在 VMA range 逻辑中判断可用性；file-backed mmap 先查 fd，再处理 zero length，并要求 fd 可读，权限失败统一返回 `AccessDenied -> EACCES`。`MAP_FIXED_NOREPLACE` 的 `AlreadyMapped` 只在 mmap syscall 边界映射为 `AlreadyExists -> EEXIST`，不改全局 `SysError`。`MAP_SHARED_VALIDATE` 下解析到未知 aux flag 时返回 `NotSupported -> EOPNOTSUPP`。`mremap` 在 syscall 边界把 old range 未覆盖或撞到 reservation 的内部错误收口为 `BadAddress -> EFAULT`。用户态 `mmap-test` 的 `MAP_FIXED_NOREPLACE` overlap 期望同步改成 `EEXIST`。

**Validation:** `just build` 通过，仅保留既有 `sync/mono.rs` unused import warning。使用 `./scripts/run-user-test-rv64.sh rootfsconfig-rv etc/sdcard-rv.img build/ltp-debug.log` 复跑 memory profile；KUnit 全部通过，glibc memory 组完成并汇总为 `attempted=43 passed=31 failed=12`。本次目标项 `mmap06`、`mmap08`、`mmap15`、`mmap17`、`mmap20`、`mremap03` 均通过。musl memory 组在 `madvise01` 中途触发 `anemone-kernel/src/sched/processor.rs:131` 的 `task.status() == TaskStatus::Runnable` 断言，未能跑完整组。

**Follow-up:** 剩余 mmap / munmap / mremap 失败仍是已分类的结构性或设施缺口：`mmap001` 的 file-backed sparse/hole fault、`mmap13` 的 EOF 后 `SIGBUS` 分流、`mremap01` 的 file-backed grow backing 保留、`/proc/self/maps` / `/proc/self/pagemap` / `/proc/<pid>/status`、`/dev/zero` mmap、`MAP_GROWSDOWN` 与 `getrlimit` / rlimit 基础设施。musl memory 的 scheduler 断言单独登记为开放问题。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md), [开放问题](../register/open-issues.md)

## 2026-05-29 - procfs `/proc/<tgid>/cmdline` 与 `/proc/<tgid>/stat` stage-1

**Date:** 2026-05-29
**Authors:** doruche, Codex
**Area:** procfs / task / mm / compatibility

**Summary:** 为动态 `/proc/<tgid>` 子树补上 `cmdline` 与 `stat` 两个只读入口。`cmdline` 复用 `environ` 的用户栈 range 暴露模型；`stat` 输出 Linux 兼容的 52 字段格式，并先接入当前已有的低成本真实数据源。

**Motivation / Symptom:** 之前 pipe 组的 `pipe2_04` 已经暴露出 `/proc/<pid>/stat` 缺失会阻断测例后半段；同时 procfs 进程子树已有 `environ`，但还没有记录 argv 字符串区间，因此缺少 Linux 常见的 `/proc/<pid>/cmdline`。`stat` 是固定字段位置 ABI，不能只输出部分字段，否则用户态按序号解析会整体错位。

**Change:** `UserSpace` 增加 `cmdline_range`，在 ELF 初始栈构造时围绕 `push_arg_strings()` 标记 argv 字符串区域，fork 时与 `env_range` 一起继承。新增 `/proc/<tgid>/cmdline` 文件，按目标 leader 的地址空间直接复制 NUL 分隔 argv blob。`Task` 增加 `create_instant`，`UserSpace` 增加 cheap `vsize_bytes()` 接口；新增 `/proc/<tgid>/stat` formatter，填充 52 个字段，其中 pid、ppid、pgrp、session、leader status 粗映射、thread 数、CPU usage ticks、starttime、vsize、cmdline/env range、exit signal/code 使用当前真实来源，rss、fault、tty、ELF segment、signal bitmap、realtime/delay/guest 等字段暂用集中注释标明的 stage-1 占位值。

**Validation:** `just build` 通过；`git diff --check` 通过。构建仍保留既有 `sync/mono.rs` unused import warning，与本次 procfs 改动无关。

**Follow-up:** `/proc/<tgid>/stat` 的 resident RSS、fault 统计、ELF segment 边界、真实 signal bitmap、tty/job-control 字段和更完整调度字段仍是 stage-1 缺口；后续应按各自子系统补真实数据源，而不是在 stat formatter 里继续堆临时猜测。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-05-29 - SysV shm ABI 布局与 `shmctl` 元数据收口

**Date:** 2026-05-29
**Authors:** doruche, Codex
**Area:** mm / SysV shm / ABI / syscall / user-test

**Summary:** 针对 rv64 shm 组日志里暴露的低成本语义偏差，修正 SysV shm 在 rv64 / loongarch64 上的 asm-generic 64-bit Linux ABI 布局，并把内核内部 SysV shm 元数据从 Linux UAPI struct 中拆出来。`shmctl07` 的 `SHM_LOCKED` 状态检查和 `shmctl08` 的 `IPC_SET` mode / ctime 检查在当前日志中通过。

**Motivation / Symptom:** `build/user-test-rv64.log` 显示 `IPC_STAT` / `SHM_STAT` 的基础字段已能返回，但 ABI struct 仍按旧的窄字段布局表达，和 rv64 / loongarch64 共用的 asm-generic 64-bit SysV IPC UAPI 不一致。与此同时，内核 `ShmSegment` 直接保存 Linux `ipc64_perm` 布局，会把 ABI padding、字段宽度和内部语义状态绑死在一起；这在同时考虑 rv64 和 loongarch64 时容易继续扩大 syscall 边界外泄。

**Change:** `anemone-abi` 中 `ShmInfo`、`ShmIdDs`、`IpcPerm` 改为 asm-generic 64-bit 布局，包含 `u64` 尺寸字段、`u32 mode` 和保留 padding。内核侧新增内部 `ShmPerm` / `ShmPermUpdate`，`shmget` 创建和 registry 只维护语义字段，`shmctl` 在 syscall 边界做 Linux ABI 转换。`IPC_SET` 只提取 uid/gid/mode 的语义更新，`SHM_STAT` / `SHM_STAT_ANY` 按 Linux shmid 低 index 位解码目标，`SHM_LOCK` / `SHM_UNLOCK` 维护可见的 `SHM_LOCKED` mode bit，但不冒充真实页锁定。

**Validation:** `git diff --check` 通过；`just build` 通过，仅保留既有 `sync/mono.rs` unused import warning；`just app build shm-test --arch riscv64` 和 `just app build shm-test --arch loongarch64` 通过。现有 `build/user-test-rv64.log` 中，`shmctl07`、`shmctl08`、`shmat03`、`shmat04`、`shmdt01`、`shmdt02` 以及多项 `shmt*` 基础项通过。

**Follow-up:** 剩余 shm 组失败不再作为这次小修继续扩大：`shmctl02`、`shmctl04`、`shmget04`、`shmat02` 受缺失 credentials / euid 权限模型影响；`shmctl03`、`shmget02`、`shmget03`、`shmget05`、`shmget06` 依赖 procfs/sysctl 或可解析 kernel config；`shmctl05`、`shmctl06` 是当前 profile 下的架构/ABI TCONF 项；`shmat01` 的只读 fault 检查还会经过 `getrlimit(RLIMIT_CORE)` 辅助路径。这些按当前限制登记，后续应分别收敛对应基础设施。

**Related:** [活动登记册](../register.md), [当前限制](../register/current-limitations.md)

## 2026-06-01 - Sched Wait Refactor 事务日志启动

**Date:** 2026-06-01
**Authors:** doruche, Codex
**Area:** scheduler / event / timer / signal / docs

**Summary:** 为 `Event` wake race 对应的 scheduler wait 大型重构引入事务日志模式，并为 `sched-wait-refactor` 建立首个事务日志页面。

**Motivation / Symptom:** 这类迁移跨 wait core、Event、timeout、signal、runqueue placement 和文档验证，双周 devlog 单条记录无法稳定承载不变量、阶段边界、旁路审计和可观测性证据。

**Change:** 在 devlog 约定中加入 `docs/src/devlog/transactions/` 事务日志模式；新增事务日志模板和索引；创建 [Sched Wait Refactor 事务日志](./transactions/2026-06-01-sched-wait-refactor.md)，记录当前计划收口、可观测性要求和后续阶段。

**Validation:** 文档结构更新；未运行构建。

**Follow-up:** 后续每个迁移阶段把阶段结果、旁路分类、debug/trace 证据和验证摘要追加到事务日志，不在双周 devlog 中展开全部细节。

**Related:** [事务日志](./transactions/index.md), [Sched Wait Refactor 事务日志](./transactions/2026-06-01-sched-wait-refactor.md)

## 2026-06-02 - Cred Merge 事务日志启动

**Date:** 2026-06-02
**Authors:** doruche, Codex
**Area:** credentials / task / VFS / exec / syscall ABI / user-test / docs

**Summary:** 为 `dev/drc/merge-cred` 上的 credentials merge 建立事务日志和多 agent 编排计划，用来跟踪 `origin/main` credentials 系统并入本地分支的阶段性证据。

**Motivation / Symptom:** 这次 merge 的主要风险不是普通文本冲突，而是远端 credentials 体系与本地 typed `openat`、fd model、`execveat` / `PathRef`、sched wait refactor、mm/shm 修复和新版 LTP group 拆分之间的语义冲突。双周 devlog 单条记录无法承载每个阶段的冲突地图、worker 边界、审查结论和临时兼容层说明。

**Change:** 新增 [Cred Merge 事务日志](./transactions/2026-06-02-cred-merge.md)，并把它加入事务日志索引和 mdBook Summary。事务日志记录 merge 原则：credentials 语义以 `origin/main` 为准，credentials 以外的本地修复默认以当前分支为准；LTP group/profile 形状以本地新拆分为准，已有本地 group 不重拆、不合并、不改名，当前缺失的 `credentials` / `chmod` / `chown` 可以作为新增 group 单独引入；总控 agent 可以在 merge 专用分支上用 git 做本地 checkpoint / 阶段提交；LTP 由用户手动执行，不交给 agent。后续该计划已提升为公开 [RFC-20260602-cred-merge](../rfcs/cred-merge/index.md)，事务日志不再引用个人草稿作为 canonical source。

**Validation:** 文档结构更新；未执行 merge、构建或 LTP。

**Follow-up:** 后续 merge 阶段由总控 agent 按计划追加事务日志：实际冲突地图、worker 集成结果、P0/P1 review 结论、构建 gate 和用户提供的 LTP 日志归类都记录到该事务页。

**Related:** [事务日志](./transactions/index.md), [Cred Merge 事务日志](./transactions/2026-06-02-cred-merge.md), [RFC-20260602-cred-merge](../rfcs/cred-merge/index.md)

## 2026-06-03 - RFC 目录结构与事务日志规范

**Date:** 2026-06-03
**Authors:** doruche, Codex
**Area:** docs / RFC / devlog / workflow

**Summary:** 将 RFC 目录结构和实现期事务日志要求写成公开文档规范，并统一现有 RFC 的背景材料目录与链接。

**Motivation / Symptom:** `sched-wait-refactor` 与 `cred-merge` 已经都采用目录级 RFC 加事务日志的形状，但规范页和模板仍保留旧的 `background/` 目录名，也没有把“进入实现阶段必须建立事务级 devlog”写成硬性流程。`sched-wait-refactor` 背景材料文件名还带有 `event-` 前缀，容易把 wait/wake 协议问题误导成单一 Event 模块问题。

**Change:** 更新 [公开草案与 RFC](../rfcs.md)、[RFC 模板](../rfc-template.md)、[开发日志](../development-log.md) 和 [文档框架](../framework.md)：目录级 RFC 统一使用 `index.md`、`implementation.md`、可选 `invariants.md`、可选 `backgrounds/`；RFC 一旦进入实现阶段，必须创建 `docs/src/devlog/transactions/YYYY-MM-DD-<short-slug>.md`，并在 RFC、事务日志索引、双周 devlog 和 mdBook Summary 中双向链接。现有 `cred-merge` 与 `sched-wait-refactor` 的背景目录统一为 `backgrounds/`；`sched-wait-refactor` 背景文件去掉误导性的 `event-` 文件名前缀，改为 wait/wake 协议层命名。

**Validation:** 文档链接结构更新；未修改代码。

**Follow-up:** 后续新增 RFC 按该模板建立目录；实现开始时同步建立事务日志，不再让 devlog 或 register 引用个人草稿作为 canonical source。

**Related:** [公开草案与 RFC](../rfcs.md), [RFC 模板](../rfc-template.md), [开发日志](../development-log.md), [事务日志](./transactions/index.md)

## 2026-06-03 - Sched Latch RFC 与事务日志启动

**Date:** 2026-06-03
**Authors:** doruche, Codex
**Area:** scheduler / wait core / iomux / poll / select / docs

**Summary:** 将 `sched-latch` 草稿提升为公开 RFC，并建立对应事务日志，用来跟踪 `poll` / `select` 从 busy polling 迁移到 wait-core latch OR wait 的阶段性实现证据。

**Motivation / Symptom:** `ppoll` / `pselect6` 的阻塞语义仍处于 stage-1 限制，当前 busy-poll 路径不能提供 Linux 风格可观察睡眠状态，也无法闭合已登记的 iomux 睡眠可观测性问题。这个迁移跨 scheduler wait core、iomux、fd source、pipe、syscall outcome mapping 和 LTP 验证，需要目录级 RFC 与事务日志承载不变量、阶段 gate 和旁路审计。

**Change:** 新增 [RFC-20260603-sched-latch](../rfcs/sched-latch/index.md)，目录包含 [不变量需求](../rfcs/sched-latch/invariants.md)、[迁移实施计划](../rfcs/sched-latch/implementation.md)、[Tracking Issues](../rfcs/sched-latch/tracking-issues.md) 和背景材料索引。新增 [Sched Latch 事务日志](./transactions/2026-06-03-sched-latch.md)，并把 RFC 与事务日志加入 mdBook Summary 和事务日志索引。公开 devlog 只引用公开 RFC 作为 canonical source。

**Review:** 协议审查未发现新的 Apollyon / Keter 硬障碍。单轮 wait identity、owner-bound `Latch`、producer no-return / fail-closed capability、source register gate、source wake detach、cleanup 非正确性支柱、final readiness scan 和 `ppoll` / `pselect6` 统一 outcome mapping 均已在 RFC 中闭合；原 Keter 风险保留为 implementation gate。软件工程审查发现的 `PollWaiter` / `poll_waiters` 旧草稿残留、pipe 无效 waiter 队列和 syscall wait loop 复制风险已写入实施计划的工程指导。

**Validation:** 文档结构更新；未修改代码，未运行构建或 QEMU / LTP。

**Follow-up:** 按事务日志从阶段 1 开始实现：先建立 `sched::latch` 原语，再定义 typed poll source 注册协议，迁移 pipe source、`ppoll`、`pselect6`，最后执行 `PollWaiter` / `poll_waiters` / `yield_now()` 旁路审计和 rv64 / LTP 验证。

**Related:** [RFC-20260603-sched-latch](../rfcs/sched-latch/index.md), [Sched Latch 事务日志](./transactions/2026-06-03-sched-latch.md), [事务日志](./transactions/index.md)

## 2026-06-04 - IOCTL Loop 事务日志启动

**Date:** 2026-06-04
**Authors:** doruche, Codex
**Area:** syscall ABI / VFS / devfs / block device / loop / mount / LTP / docs

**Summary:** 为 `ioctl(2)` 基础设施与第一阶段 loop 设备闭环建立事务日志，用来跟踪 VFS ioctl 分发、通用 block `BLK*` ioctl、静态 loop 设备池、loop 私有 ioctl 和 `mount -t ext4 /dev/loopN` 最小闭环验证准备。

**Motivation / Symptom:** 这次实现跨 syscall ABI、VFS file ops、devfs 发布、block registry、loop 状态机、mount source lookup 和 LTP `.needs_device` 基础设施。双周 devlog 单条记录无法承载 `IoctlCtx` 边界、block private hook、loop backing file 生命周期、flag 策略、review gate 和用户提供的 LTP 日志归类。

**Change:** 新增 [IOCTL Loop 事务日志](./transactions/2026-06-04-ioctl-loop.md)，并把它加入事务日志索引和 mdBook Summary。RFC 页首与 Tracking Issues 已链接到该事务日志。总控 agent 当前只启动 Agent 0 做只读前置审计；后续 worker 必须按 [Agent 编排建议](../rfcs/ioctl-loop/backgrounds/agent-orchestration.md) 的顺序、write set 和 review gate 串行推进。

**Validation:** 文档结构更新；未修改生产代码，未运行构建、QEMU 或 LTP。

**Follow-up:** 等待 Agent 0 输出当前 ioctl / VFS / block / mount 落点审计。通过后进入 Agent 1：实现 UAPI 常量、loop ABI 结构、`IoctlCtx` 和 `FileOps::ioctl` 分发，并在 Gate 1 review 前保持 `just build` 可通过。

**Related:** [RFC-20260603-IOCTL-LOOP](../rfcs/ioctl-loop/index.md), [IOCTL Loop 事务日志](./transactions/2026-06-04-ioctl-loop.md), [事务日志](./transactions/index.md)

## 2026-06-04 - LTP ioctl 组直接修复收口

**Date:** 2026-06-04
**Authors:** doruche, Codex
**Area:** ioctl / block device / devfs / user-test / docs

**Summary:** 根据 rv64 LTP ioctl 组日志归类，收口不需要新架构阶段的小缺口，并把仍未完成的 ioctl 组能力登记为当前限制。

**Motivation / Symptom:** `ioctl05` 已经通过 `BLKGETSIZE*` 与 `lseek(end)`，但 EOF 后 1 字节 read 被 block `/dev` 对齐检查错误拒绝为 `EINVAL`；`ioctl06` 缺 `BLKRASET` / `BLKRAGET`；`ioctl07` 先死在 `/dev/urandom` 不可见；`ioctl_loop01..07` 因 LTP 模块元数据检查无法识别 built-in loop 而没有进入测试正文。

**Change:** 调整统一 block devfs read 顺序，在设备 EOF 处先返回 `0`，再检查非 EOF read 的块对齐。补充 `BLKRASET` / `BLKRAGET` ABI 常量，并在 block registry 记录每个 block device 的通用 readahead 值，由统一 block devfs ioctl 读写。`/dev/urandom` 注册后现在发布到 devfs。user-test LTP fixture 新增 `/lib/modules/6.6.32/modules.dep` 与声明 loop built-in 的 `modules.builtin`，让 LTP 的 `needs_drivers = "loop"` 能越过前置检测。

**Boundary:** 按本轮 scope 未改 `ioctl02` group entry。未发布 `/dev/loop-control`，未实现 loop 扩展 ioctl、loop sysfs、partscan、random `RNDGETENTCNT` 或 `/proc/sys/kernel/random/entropy_avail`；这些残留按 [当前限制](../register/current-limitations.md) 继续跟踪。

**Validation:** 见 [IOCTL Loop 事务日志](./transactions/2026-06-04-ioctl-loop.md) 的本轮记录；本条不重复展开构建输出。

**Related:** [IOCTL Loop 事务日志](./transactions/2026-06-04-ioctl-loop.md), [当前限制](../register/current-limitations.md), [rv64 LTP ioctl 运行证据](../rfcs/ioctl-loop/backgrounds/ltp-ioctl-rv64-20260604/index.md), [RFC-20260603-IOCTL-LOOP](../rfcs/ioctl-loop/index.md)

## 2026-06-04 - LTP runner loop 模块元数据路径修复

**Date:** 2026-06-04
**Authors:** doruche, Codex
**Area:** user-test / LTP runner / runtime switching / loop fixtures

**Summary:** 修正 rv64 user-test LTP runner 中 loop built-in 模块元数据的可见性问题，让 `needs_drivers = "loop"` 在 glibc 与 musl 两套 runtime 下都能看到 `modules.dep` / `modules.builtin`，从而越过错误的 `loop driver not available` 前置失败。

**Motivation / Symptom:** runner 原先把 LTP fixture 写到 `/lib/modules/6.6.32/`，但 rv64 的 `switch_runtime()` 会在运行每套 LTP root 前把 `/lib` 整体替换成指向 `/{glibc,musl}/lib` 的 symlink。结果 fixture 安装阶段打印了 `/lib/modules/6.6.32/modules.dep` 与 `modules.builtin` 已写入，进入 `ioctl_loop01` 后同一路径又因活动 runtime 切换变成 `ENOENT`，LTP 仍报 `loop driver not available`，没有进入 loop discovery 或 ioctl 正文。

**Change:** user-test LTP fixture 继续保留 `/lib/modules/6.6.32/` 安装，同时新增 `/glibc/lib/modules/6.6.32/` 与 `/musl/lib/modules/6.6.32/` 两套安装目标。这样 `/lib` 被切到 glibc 或 musl runtime 后，LTP 的固定路径 `/lib/modules/<uname.release>/modules.*` 仍能看到声明 built-in loop 的元数据。

**Validation:** `just xtask app build user-test --arch riscv64` 通过；`git diff --check` 通过；二进制字符串确认包含 glibc/musl 两套模块元数据路径和 `kernel/drivers/block/loop.ko`。用户复跑最新 `build/user-test-rv64.log` 后，`ioctl_loop01` 已越过 driver availability 检查并打印 `Found free device '/dev/loop0'`。

**Follow-up:** 新日志中的剩余失败已经推进到 loop 后续能力层：`parted` 不存在或失败被 LTP 记为 `TCONF`，随后 `ioctl_loop01` 读取 `/sys/block/loop0/loop/partscan` 时返回 `ENOENT` 并 `TBROK`。这是 loop sysfs / partscan 可观察面缺口，不再是 LTP runner 的模块元数据 false-negative。日志中的 unknown syscall 123 对应 asm-generic `sched_getaffinity`，当前未阻止该用例继续执行到 loop/sysfs 失败点。

**Related:** [当前限制](../register/current-limitations.md), [RFC-20260603-IOCTL-LOOP](../rfcs/ioctl-loop/index.md)

## 2026-06-04 - PROC TGID FD RFC 与事务日志启动

**Date:** 2026-06-04
**Authors:** doruche, Codex
**Area:** procfs / fd / path visibility / LTP / docs

**Summary:** 将 `proc-tgid-fd` 草案提升为公开目录级 RFC，并建立事务日志，用来跟踪 `/proc/<tgid>/fd` 的第一阶段实现证据。

**Motivation / Symptom:** 当前限制 `ANE-20260528-PROC-TGID-FD-FRAMEWORK-PENDING` 仍会阻塞 musl `getcwd02` 和 LTP `pipe07`：前者依赖 `readlink("/proc/self/fd/<n>")` 返回真实路径，后者依赖 `/proc/self/fd` 目录枚举。该改动跨 procfs 动态 tgid 子树、fd table 快照、path visibility、pipe 显示名和 VFS symlink 行为，需要公开 RFC 固定阶段边界和 review gate。

**Change:** 新增 [RFC-20260604-proc-tgid-fd](../rfcs/proc-tgid-fd/index.md)，目录包含 [迁移实施计划](../rfcs/proc-tgid-fd/implementation.md) 和 [Tracking Issues](../rfcs/proc-tgid-fd/tracking-issues.md)。新增 [PROC TGID FD 事务日志](./transactions/2026-06-04-proc-tgid-fd.md)，并把 RFC 与事务日志加入 mdBook Summary 和事务日志索引。

**Review:** 草案中的 Keter / Euclid design review 项已经折回 RFC 主文档和实施计划；Tracking Issues 当前没有开放问题，只保留 Neutralized 记录。第一阶段固定为 `same-tgid only`、不实现完整 magic-link open、不实现 `fdinfo`，且 root 外路径不得 fallback 成全局路径。

**Validation:** 文档结构更新；未修改生产代码，未运行构建、QEMU 或 LTP。

**Follow-up:** 按事务日志先进入阶段 0：新增 fd number 只读快照 API；随后阶段 1 落地 `/proc/<tgid>/fd` 目录、数字 fd lookup、`readlink()` 和 `getattr()`，再重新分类 musl `getcwd02` 与 LTP `pipe07`。

**Related:** [RFC-20260604-proc-tgid-fd](../rfcs/proc-tgid-fd/index.md), [PROC TGID FD 事务日志](./transactions/2026-06-04-proc-tgid-fd.md), [当前限制](../register/current-limitations.md)

## 2026-06-04 - RFC 工作流固化

**Date:** 2026-06-04
**Authors:** doruche, Codex
**Area:** docs / RFC / agent workflow

**Summary:** 将中型及以上功能迭代的私有草案、文档层 review、公开 RFC、事务 devlog 和实现阶段 handoff 流程固化为仓库规范，并新增 repo-local agent skill。

**Motivation / Symptom:** 近期 `sched-latch`、`ioctl-loop`、`proc-tgid-fd` 等工作已经形成稳定流程：先在个人 gitignored 草稿区按 RFC 形状打磨方案和不变量，经多轮 design review 后提升到 `docs/src/rfcs/`，再用事务 devlog 跟踪实现证据。该流程已经影响多项大型迭代，不能只存在于个人习惯、聊天上下文或 agent 记忆里。

**Change:** 新增 [RFC 工作流](../rfc-workflow.md)，定义适用范围、私有草案、草案成形、文档层 review、公开 RFC 提升、事务 devlog、实现阶段和收口边界。更新 [公开草案与 RFC](../rfcs.md)、[RFC 模板](../rfc-template.md) 和 mdBook Summary，让流程规范与模板互相链接。新增 `.agents/skills/anemone-rfc-doc-workflow/SKILL.md`，约束 agent 在 RFC 草案整理、review、promotion、tracking issues 和 transaction devlog 任务中的执行步骤。

**Validation:** `git diff --check` 通过；`mdbook build docs` 通过。未修改生产代码，未运行构建、QEMU 或 LTP。

**Follow-up:** 后续中型及以上迭代默认先按 RFC 工作流完成文档层收口；如果实现期发现 accepted contract 错误，应先回到 RFC 文档层修正，再继续推进代码。

**Related:** [RFC 工作流](../rfc-workflow.md), [RFC 模板](../rfc-template.md), [公开草案与 RFC](../rfcs.md)

## 2026-06-05 - FileOps Seek 与字符设备 ioctl 事务启动

**Date:** 2026-06-05
**Authors:** doruche, Codex
**Area:** VFS / syscall ABI / devfs / character device / loop / docs

**Summary:** `fileops-seek-char-ioctl` RFC 进入实现阶段，新增事务日志并按 agent orchestration 启动 Agent 1 与 Gate 1 review agent。

**Motivation / Symptom:** 当前 `FileOps::validate_seek` 同时服务 `lseek(2)`、positioned I/O 和块设备 offset 校验，`File::read_at()` / `File::write_at()` 仍通过 `validate_seek + dummy cursor` 复用普通 `read` / `write`，导致 seek intent、backend positioned I/O capability、loop backing handle 和字符设备 seek / ioctl 所有权边界混在一起。

**Change:** 新增 [FileOps Seek and Char Device ioctl 事务日志](./transactions/2026-06-05-fileops-seek-char-ioctl.md)，并把 RFC 页首、Tracking Issues、事务索引、mdBook Summary 和当前双周 devlog 连接到同一条实现记录。总控完成只读前置审计后，按 [Agent 编排建议](../rfcs/fileops-seek-char-ioctl/backgrounds/agent-orchestration.md) 只启动 Agent 1 和 Gate 1 review agent，不启动后续 worker。

**Boundary:** Agent 1 只允许做阶段 1A mechanical API sweep 与 fail-closed 默认实现；不得迁移 `sys_lseek()` 外部语义，不得改 loop backing handle，不得实现 `CharDev` seek / ioctl hook。Gate 1 reviewer 只读审查阶段 1A diff。

**Validation:** 用户明确要求跳过 baseline `just build`；本条未运行构建、QEMU 或 LTP。

**Related:** [RFC-20260605-fileops-seek-char-ioctl](../rfcs/fileops-seek-char-ioctl/index.md), [FileOps Seek and Char Device ioctl 事务日志](./transactions/2026-06-05-fileops-seek-char-ioctl.md), [事务日志](./transactions/index.md)

## 2026-06-05 - 小迭代工作流自描述边界补强

**Date:** 2026-06-05
**Authors:** doruche, Codex
**Area:** docs / devlog / qdev / RFC workflow / agent workflow

**Summary:** 调整小迭代记录规范，让不值得升级 RFC 的局部迭代也能在单份记录里自描述问题、解决方案和本迭代内部 tracking issues。

**Motivation / Symptom:** 新引入的小迭代 / qdev 工作流仍偏向事后事实记录，无法自然承载“问题是什么、局部解法是什么、还有哪些待关闭 concern”。这导致 qdev 草案需要强行塞进旧格式；但这些问题尚未大到必须进入正式 RFC。

**Change:** 更新 [开发日志](../development-log.md)、[小迭代记录入口](./changes/index.md)、[模板](../templates.md)、[文档框架](../framework.md) 和 [RFC 工作流](../rfc-workflow.md)，明确小迭代记录可以包含 `Problem`、`Solution` 和内联 `Tracking Issues` 章节；同时固定升级边界：一旦需要仓库级 contract、非平凡不变量、跨阶段实施计划、独立 `tracking-issues.md`、多轮文档层 review 或多个 agent/checkpoint 编排，就自然升级为 RFC。同步更新 repo-local agent workflow skill 与 `.codex/AGENTS.md` 中的 qdev 说明。

**Boundary:** 未修改 `etc/qdev` 下现有草案；后续由用户自行按新格式对齐。

**Validation:** 文档结构更新；未修改生产代码，未运行构建、QEMU 或 LTP。

**Related:** [开发日志](../development-log.md), [小迭代记录入口](./changes/index.md), [模板](../templates.md), [RFC 工作流](../rfc-workflow.md)

## 2026-06-05 - Block devfs byte I/O 与 loop mkfs setup 修正

**Date:** 2026-06-05
**Authors:** doruche, Codex
**Area:** block devfs / loop / LTP compatibility

**Summary:** 修正 `/dev/<block>` 普通块特殊文件 byte seek/read/write，让 loop-backed
`mkfs.ext2` setup 不再因合法非块对齐 offset 被 block devfs 直接拒绝。

**Change:** 新增 [Block Byte I/O Loop Mkfs 小迭代记录](./changes/2026-06-05-block-byte-io-loop-mkfs.md)。
本轮在 block devfs 内增加有界 byte I/O adapter，保留 `BlockDev` 后端块对齐契约；loop
`SET_FD`、`SET_STATUS*` 和 `CLR_FD` 的状态提交进入每设备 `io_lock`；块特殊文件上的
`O_DIRECT` open / `F_SETFL` 以 `EINVAL` fail-closed。review 发现的 `LOOP_CLR_FD`
临时引用误判风险已通过 transient ref 计数修复。

**Validation:** `just fmt kernel` 通过；`just build` 通过，仅保留既有 `sync/mono.rs`
unused import warning；`git diff --check` 通过。未运行 rv64 `mkfs.ext2 /dev/loop0`
smoke、`fanotify13` 或 setup-heavy LTP。

**Related:** [Block Byte I/O Loop Mkfs 小迭代记录](./changes/2026-06-05-block-byte-io-loop-mkfs.md), [当前限制](../register/current-limitations.md), [IOCTL Loop 事务日志](./transactions/2026-06-04-ioctl-loop.md)

## 2026-06-06 - Signal 临时 Mask Restore 事务启动

**Date:** 2026-06-06
**Authors:** doruche, Codex
**Area:** signal / wait-core / syscall ABI / iomux / docs

**Summary:** `signal-temp-mask-restore` RFC 进入实现阶段，新增事务日志并按 agent
orchestration 只启动 Agent 0 前置审计。

**Motivation / Symptom:** `rt_sigsuspend`、`ppoll` 和 `pselect6` 的临时 signal mask
不能在 syscall body 中早恢复，否则 trap-return `handle_signals()` 可能看不到触发信号；
但 handler sigframe 又必须保存进入 temporary window 前的旧 mask，避免 `rt_sigreturn()`
恢复到临时 mask。

**Change:** 新增 [Signal Temporary Mask Restore 事务日志](./transactions/2026-06-06-signal-temp-mask-restore.md)，并把 RFC 页首、Tracking Issues、事务索引、mdBook Summary 和当前双周 devlog 连接到同一条实现记录。总控完成只读落点刷新后，按 [Agent 编排建议](../rfcs/signal-temp-mask-restore/backgrounds/agent-orchestration.md) 只启动 Agent 0 前置审计，不启动阶段 1A 或后续写入型 worker。

**Boundary:** Agent 0 只读审计当前 signal / iomux / syscall ABI 落点，不改代码，不运行构建、QEMU 或 LTP。Gate 0 未确认前，不进入 `TaskSigMaskState` storage / ordinary current-mask API 写入阶段。

**Validation:** `git diff --check` 通过；`mdbook build docs` 通过。未修改生产代码，未运行 kernel 构建、QEMU 或 LTP。

**Related:** [RFC-20260606-signal-temp-mask-restore](../rfcs/signal-temp-mask-restore/index.md), [Signal Temporary Mask Restore 事务日志](./transactions/2026-06-06-signal-temp-mask-restore.md), [事务日志](./transactions/index.md)

## 2026-06-07 - Signal 临时 Mask Restore Stage 1B Checkpoint

**Date:** 2026-06-07
**Authors:** doruche, Codex
**Area:** signal / wait-core / syscall ABI / docs

**Summary:** `signal-temp-mask-restore` 已完成阶段 1A / 1B 并通过 Gate 1 / Gate 2
review；当前停止在 Stage 2 signal delivery 接入前。

**Change:** 阶段 1A 建立 `TaskSigMaskState` storage 与 ordinary current-mask API；
阶段 1B 建立 `TemporarySigMaskToken`、restore slot identity 和 delayed-restore helper
contract。RFC 入口、invariants、implementation、agent orchestration 与事务日志已同步为
Gate 2 passed / Stage 2 pending。

**Boundary:** 尚未接入 `perform_signal_action()` 的 `sigmask_to_save_for_signal_frame()`、
`handle_signals()` cleanup、signal-owned classifier、`rt_sigsuspend`、iomux typed outcome
或 `rt_sigtimedwait` waited-set 修复。下一步只能启动 Agent 3 阶段 2，不得跳过 delivery
commit / cleanup gate。

**Validation:** `git diff --check` 通过；`mdbook build docs` 通过；`just build` 通过，仅有既有
`anemone-kernel/src/sync/mono.rs` unused import warning。未运行 QEMU / LTP。

**Related:** [RFC-20260606-signal-temp-mask-restore](../rfcs/signal-temp-mask-restore/index.md), [Signal Temporary Mask Restore 事务日志](./transactions/2026-06-06-signal-temp-mask-restore.md)
